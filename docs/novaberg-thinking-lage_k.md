# Novaberg — Die Lage-Analyse (Konzept)

**Projekt:** Novaberg — The Nova Anima Resonance System
**Dokument:** Lage-Analyse — die erste gebaute Scheibe der Verstehens-Schicht
**Stand:** 17. September 2026, 17:52 UTC (`date -u`; **Notizen nur auf ausdrücklichen Auftrag** — entschieden; gebaut nur die Sperre der Klassifikation, die am Empfang nach der Messung zurückgenommen; der Anlass bleibt offen). Davor 17. September 2026, 14:27 UTC (`date -u`; **Scheibe 12 D2 gebaut** — der Router liest die Lage (`[LAGE]`), der Objektbezug reist zur Klassifikation (`[OBJEKT]`); erkannte Zustimmungen im Router 0 → 14 von 24, in der Klassifikation 12 → 24 von 24; ein Satz im Dispatch-Guard verworfen, §4 *Teil D2 — gebaut*). Davor 17. September 2026, 10:58 UTC (`date -u`; **Scheibe 12 D1 gebaut** — der Planner fragt die Dienste in der Reihenfolge der Objekt-Nähe, nach einer Ablehnung den nächsten; die Zeitangabe wählt keinen Dienst mehr; gemessen Notizen zuerst richtig 7 → 13 von 13, §4 *Teil D1 — gebaut*). Davor 17. September 2026, 06:01 UTC (`date -u`; **die Sternchen sind aus dem Eigenschaftsgedächtnis bereinigt** — 10 Objekte umbenannt, eines zusammengeführt, 14 von 14 Eigenschaften wieder auffindbar, §4 Scheibe 12 *Die Objektwahl der Lage*). Davor 16. September 2026, 22:31 UTC (`date -u`; **die Objektwahl der Lage ist gebaut** — die Sache, um die es geht, nicht ihr Rahmen; bei einer Handlung die Handlung; Namen ohne Auszeichnung. Gemessen als A/B und A/C über je 60 Äußerungen; die erste Fassung machte Handlungen zu Dingen und ist verworfen; nebenbei eine JSON-Reparatur gefunden, die gültige Antworten zerschnitt, §4 Scheibe 12 *Die Objektwahl der Lage*). Davor 16. September 2026, 21:10 UTC (`date -u`; **das Notizen-Merkmal ist nachgeeicht, und es bleibt** — zehn Fassungen gegen die heutige, keine gewinnt unter der Regel je Zettel; die Grenze liegt im Objekttext der Lage, nicht im Wortlaut, §4 *Teil C2*). Davor 16. September 2026, 20:58 UTC (`date -u`; **entschieden und umgebaut: je Zettel für sich, ohne Abstand** — ein Objekt steht an jedem Dienst, dessen Untergrenze es erreicht, im Grenzfall an beiden, und eine Ablehnung des einen hält den anderen nicht auf; der Schatten rechnet seither diese Regel, Reihe und Bestand Zahl für Zahl wie gemessen, §4 *Teil C2*). Davor 16. September 2026, 20:50 UTC (`date -u`; **die Regel je Zettel ist gerechnet** — eine Untergrenze je Dienst ohne Abstand: nie nur der falsche Dienst, aber 16 von 60 an beiden Zetteln; eine eigene Untergrenze je Dienst bringt nichts Messbares; der schwache Zettel ist das Notizen-Merkmal, §4 *Teil C2*). Davor 16. September 2026, 20:18 UTC (`date -u`; **Scheibe 12 C2 gebaut — die Nähe im Schatten**: der Router rechnet je akutem Objekt die Nähe zu den Merkmalen, urteilt mit beiden Schwellen und protokolliert, ohne zuzustellen; die Produktionsformel reproduziert die Eichung exakt, 33/32/27, §4 *Teil C2 — gebaut*). Davor 16. September 2026, 19:43 UTC (`date -u`; **Scheibe 12 C1 gebaut — die Anmeldung trägt das Objekt-Merkmal**: Timeline und Notizen deklarieren den geeichten Wortlaut, der Server bettet ihn beim Start ein, der Empfang liest ihn nicht; Nähe und Schwellen (C2) sind nicht gebaut, §4 *Teil C1 — gebaut*). Davor 16. September 2026, 19:14 UTC (`date -u`; **die Nähe ist gemessen und geeicht** — zwei Schwellen statt einer: eine Untergrenze auf die absolute Nähe (0,40) beantwortet, ob überhaupt ein Dienst gemeint ist, der Abstand (0,04) welcher; über 60 Fälle 32 von 33 zugestellten richtig, 27 still. Merkmalstext je Dienst geeicht, §4 *Offen für den Bau*). Davor 16. September 2026, 17:33 UTC (`date -u`; **das Kriterium der Zuordnung steht** — eine Handlung oder ein Ereignis mit Zeitpunkt gehört in die Timeline, eine Sache oder ein Zustand mit Zeitbezug in die Notizen; damit entscheidet das Verb und nicht das Zeitwort, §4 *Das Kriterium der Zuordnung*. Der heutige Weg über `needs_timeline` ist als Defekt erfasst). Davor 16. September 2026, 16:51 UTC (`date -u`; **Scheibe 12: die zweite offene Entscheidung ist gefallen** — eine Behauptung, die beide Korrekturrunden übersteht, wird nicht entfernt; ein Korrektursatz wird angehängt, der Ausgang am Turn vermerkt, und ein späterer Nachtrag geht an den Gedächtnisknoten. Nicht gebaut, §4 *Offen für den Bau*). Davor 16. September 2026, 15:10 UTC (`date -u`; **Scheibe 12 B gebaut — der ganze Verlauf**: kein Beitrag wird mehr gekürzt, die Grenze greift über das Fenster und wirft älteste Gruppen weg; gemessen 7213 → 0 abgeschnittene Beiträge, 86,8 % der Fenster vollständig, §4 *Teil B — gebaut*). Davor 15. September 2026, 21:26 UTC (**Scheibe 12: entschieden, wer eine Speicherbehauptung gegen den Bestand hält** — der Riegel hinter dem Verfasser, nicht das Thinker-Modell; ein Dienst meldet, was seine eigene Verifikation ergab; nicht gebaut, §4 *Offen für den Bau*). Davor 15. September 2026 (**Scheibe 12, Teil A gebaut — die Wahrheit der Antwort**: ein Riegel in der Auswertung des Tribunals meldet eine Speicherbehauptung ohne abgeschlossenen Dienst; am Bestand geeicht, die echte Korrekturrunde in Läufen gemessen — der erste Auftrag erzeugte Synonyme statt sauberer Sätze, §4 *Teil A — gebaut*). Davor 14. September 2026, 19:52 UTC (**Scheibe 12 entworfen — der Empfang liest die Lage**: die Entscheidungen des Eigentümers im Wortlaut, sechs Teile von der Wahrheit der Antwort bis zur Prüfung des Bestands durch die Fachabteilung, nicht gebaut, §4). Davor 13. September 2026, 15:55 UTC (**die zweite Kontrolle fand drei Fehler, die kein Zeuge sah** — ein gespeicherter Wert deckte eine fremde Eigenschaft, Ansprüche des Auflösers liefen ohne Wertprüfung, und die Schlüssel in Form und Auflöser verglichen anders als die Datenbank; nachgebessert und bezeugt, §4 Scheibe 11; Zeugenzuordnung, Durchgänge und JSON-Ausfallquote berichtigt). Davor 15:11 UTC (**Scheibe 11 — das Eigenschaftsgedächtnis**: die Objekte eines gerechneten Turns dauerhaft am Paar, ein neuer Wert löst ab statt zu überschreiben, die Entität kommt über die Magnete des Turns, der Rückweg über den Auflöser — entworfen, gebaut, gemessen, §4. **Festlegung 1 ist eingelöst**: `sachlage_form` hält jedes Objektfeld, auch die vorige Blase beim Laden; die Absichtsfrage ist beantwortet — der Wert in `gedeckt` ist die Angabe selbst, ohne Wert ist eine Eigenschaft offen, §3; `LAGE-FORMPRUEFUNG-UNVOLLSTAENDIG` behoben. §5 nachgezogen). Davor 12. September 2026, 19:45 UTC (**Festlegung 1 ist fuer zwei von fuenf Feldern nicht eingeloest** — `_validate_artifact` normalisiert die Felder der Scheiben 7 bis 10 und nicht `gedeckt` und `offen` aus Scheibe 1; ein Betriebsturn brach daran ab, `LAGE-FORMPRUEFUNG-UNVOLLSTAENDIG`, Band A. Davor 30. August 2026 (Scheibe 10: das Gewicht einer Lücke — entworfen, gebaut, gemessen, §4). Davor 30. August 2026 (Scheibe 8: der ausstehende Betriebsturn ist gelaufen — Treffer im Block, Wert in der Antwort, §4). Davor 29. August 2026, spät (Nachtrag zu Scheibe 3: der eigene Zug — der Rückfrage-Gegenstand trägt seine Herkunft; der `[SACHLAGE]`-Block spricht in den Namen seines Lesers; davor abends die Betriebsmessung der Kette über 20 Turns, §4 — Träger 0/17/14, Sprecher 31/11/21, Plausibilität 3/3 ohne Fehlalarm, Websuche 1 von 8 mit Treffer, Rückfrage-Gegenstand am alten Kurzziel). Davor mittags (Scheibe 9 — der Sprecher: der Gedanke des Nutzers bleibt seiner, entworfen, gebaut, gemessen, §4; der Herkunftsblock des Verfassers führt). Davor vormittags (Scheibe 8 — der Wissensträger: Antwortstoff statt Rückfrage, mit Websuche, §4; Scheibe 3 liest ihn). Davor morgens (Scheibe 7 — die Plausibilitätsprüfung gegen Weltwissen, entworfen, gebaut, gemessen, §4; §5 markiert). Davor 28. August 2026, spät (Scheibe 6 — der Frame-Auflöser gegen den Gedächtnis-Pool des Turns, entworfen, gebaut, gemessen: das Urteil ist ein eigener Call, §4; §3 Festlegung 4 und §5 markiert). Davor abends (der Gesprächskontext-Tab im Client, §4; Scheibe 3 gebaut und gemessen — die Rückfrage trägt ihren Gegenstand, 4/4 gegen 1/4; Scheibe 5, die Wiederaufnahme, entworfen, gebaut und gemessen — §4; `thema` und `gegenstand` benennen die Sache, nie den Wechsel — §4 Scheibe 4; der Verlauf im Sachlage-Prompt trägt Novas Antworten ganz — der Fund »Deckung nur aus Nutzer-Äußerungen« ist halb widerlegt, §4 Scheibe 1; Verfall des kurzfristigen Ziels beim Lesen gerechnet, §4 Scheibe 2; Scheibe 2 gebaut und gemessen — die Strecke zählt das akute Objekt, nicht den Nutzerziel-Satz; Scheibe 4 gebaut und gemessen, §4; Betriebsmessung zu Scheibe 1). Davor am selben Tag: (Erstfassung; am selben Tag ergänzt: §2a wissenschaftliche Prüfung, §3a der Kontext als Blase)
**Pfad:** novaberg/docs/novaberg-thinking-lage_k.md
**Typ:** Konzept (`_k`)
**Quellen:** `novaberg-thinking-frames_k.md` · `novaberg-thinking-cognitive-pipeline_k.md` · `novaberg-thinking-drive_k.md` (Herkunft jeweils dort) · Messungen vom 27./28.08.2026

**Verhältnis zu den Schwester-Dokumenten:** Dieses Dokument erfindet nichts neu. Es schneidet aus den drei Thinking-Konzepten die **erste baubare Scheibe** und legt fest, was davon jetzt entsteht und was ausdrücklich Konzept bleibt. Die Begriffe (Frame, Slot, Akutheit) sind dort definiert und werden hier benutzt, nicht wiederholt.

---

> **Herkunft der Beispiele:** Alle wörtlichen Gesprächsbeispiele in diesem
> Dokument (Geburtstag, Rasen, Kräuterbeet, das Gelb, die Themenkette
> Kino→Krieg) sind **synthetisch** — für dieses Konzept konstruiert, ohne
> Bezug zu einem echten Gespräch. Die Zahlen in §1 sind echte Messwerte;
> sie zitieren keinen Wortlaut.

## 1. Anlass — die Messungen, die den Schnitt begründen

Alle vier vom 27./28.08.2026, alle am Produktivsystem:

**Die Rückfragen haben keinen Gegenstand.** Über 84 Schlussfragen des produktiven Paares: 33 % beginnen als Angebot (*„Sollen wir …"*), 23 % zusätzlich mit „oder", neunzehn auf demselben Satzgerüst. Nach Halbierung der Fragen-Grundwerte und Mitlieferung der Frage-Art (beides 27.08.): weiterhin **2,2 Fragen je Turn, 100 % der Antworten enden mit einer Frage** — nur das Gerüst hat gewechselt (*„wie fühlt sich das für dich an?"* in 5 von 19 Turns). **Die Formvorgaben behandeln ein Symptom: Eine Frage ohne Ziel kann nur Floskel sein.**

**Niemand erhebt, was der Nutzer will.** Das Einzige, was existiert, ist das grobe `intent`-Label der Perzeption (`smalltalk`, `meta`, …). Der GV-Knoten rechnet Sprünge und Novas Absicht — die Frage *„worum geht es hier, was will er erreichen"* stellt kein Knoten.

**Der GV-Parse trägt seine eigenen Felder nicht.** Über 610 GV-Parses aus dem Serverlog: `Vehikel` leer in 75 %, `Absicht` leer in 66 %, `Strategie` leer in 66 %. Wer die Lage-Erhebung in diesen Parse legt, hängt sie an eine Kette, die meistens reißt.

**Die Landschaft entsteht aus Rückfallwerten.** Ein bloßer Zwei-Wort-Gruß nach 25 Stunden Pause landete in `kissenschlacht`: eine von sechs Achsen gemessen, drei auf Vorgabewerten, die sämtlich auf die warme Seite binarisieren. Die Raum-Neutralisierung (28.08.) mildert das; das Verstehen ersetzt sie nicht.

Dazu der Bestand laut Featureliste: *„Frames · Skills · Task-Orchestration · Cognitive Pipeline — ⚫ die Kernbezeichner fehlen sämtlich, kein Code."*

---

## 2. Was die Konzepte bereitstellen — die Kurzfassung

| Konzept | Was die Lage-Analyse daraus nimmt |
|---|---|
| **Frames** (`frames_k` §3–4) | Referenzobjekte *sind* Frames: Ein erwähnter Geburtstag trägt typische Eigenschaften (Wer, Wann, Feier, Geschenk), und die ungedeckten sind **mitaktivierte offene Fragen** (Fillmore). Die Slot-Schemata stehen im LLM-Wissen, nicht im Code. **Akutheit ist der Filter**: Was nicht akut ist, wird nicht beprobt — Schutz vor Übergriffigkeit (§4.3). |
| **Cognitive Pipeline** (`cognitive-pipeline_k` §1–4) | Die Diagnose („zwischen Router und Dispatch fehlt das Verstehen") und der Schritt **Akutheits-Klassifikation + Frame-Aktivierung** (§4.1–4.2). Die übrigen acht Schritte — Auflöser gegen den Bestand, Cross-Frame-Validierung, Plausibilität, Skills, Werkzeug-Orchestrierung — bleiben Konzept. |
| **Drive** (`drive_k` §3) | Drei Zielhorizonte. Langfristig und mittelfristig existieren (`ziele`-Tabelle, Gravitation, `[GEDANKEN]`-Block im GV). **Der kurzfristige Horizont ist als flüchtige GV-Hypothese definiert und hat keine persistente Form** — ein Sessionziel wie *„er baut ein Kräuterbeet auf"* kann heute nirgends stehen. |
| **Curiosity** (`curiosity_k`) | Die spätere Abnahmestelle: Ein wiederholt offener Slot ist eine Wissenslücke und damit Neugier-Trigger. In dieser Scheibe nur als Anschluss benannt. |

### 2a. Die wissenschaftliche Prüfung — 28.08.2026

**Die Annahmen dieses Dokuments sind im Projekt formuliert und danach gegen die Kognitionswissenschaft geprüft worden — die Schlüsse decken sich.** Alle vier Kernannahmen sind gedeckt; an zwei Stellen sagt die Forschung mehr, und beide sind in §3a eingearbeitet.

| Annahme | Deckung |
|---|---|
| Verstehen braucht ein Situationsmodell des Gesprächs | Lehrbuchstand seit van Dijk & Kintsch (1983). Das Event-Indexing-Modell (Zwaan, Langston & Graesser 1995) führt es auf **fünf Dimensionen**: Zeit, Raum, Akteur, Kausalität, **Intentionalität/Motivation** — nahezu die Felder des `lage`-Artefakts (§3). |
| Das Nutzerziel wird erschlossen; das Gesagte muss nicht der Grund sein | Sprechakttheorie und Grice; computational Allen & Perrault (1980): Verstehen **ist** Plan-Erkennung. Neurowissenschaftlich existiert dafür ein eigenes System — das Mentalizing-Netzwerk (medialer Präfrontalcortex, TPJ; Frith & Frith 2006), aktiv genau beim Deuten indirekter Äußerungen. |
| Referenzobjekte tragen typische Eigenschaften, die mitaktiviert werden | Über Fillmore/Minsky hinaus **gemessen**: Generalized Event Knowledge (Metusalem et al. 2012; McRae & Matsuki 2009) — Wörter aktivieren sofort Ereigniswissen samt Erwartungen an Beteiligte, nachweisbar im EEG (N400). |
| Antrieb ist im Gespräch verankert; die Frage dient dem Ziel | Klinger (Current Concerns): Ziel-Commitment sensibilisiert Wahrnehmung, Erinnern, Denken. Clark (1996): Gespräch ist Joint Action auf gemeinsame Ziele. Frageforschung (Rothe, Lake & Gureckis): Menschen fragen zielgerichtet; formalisierbar als **Expected Information Gain** — erwartete Unsicherheitsreduktion Richtung Ziel. Zugleich der Maßstab für Scheibe 3: Eine gute Rückfrage reduziert die Unsicherheit über eine offene Eigenschaft oder ein Ziel. |

**Die Einschränkung, die die Forschung mitgibt:** Absichtserkennung ist Inferenz, und selbst zielgerichtete Frager stellen selten die *optimale* Frage. Das Nutzerziel bleibt deshalb als **vermutet** gekennzeichnet — erschlossen aus Zeichen, Mustern und wahrgenommener Emotion, nie als Wissen behandelt.

---

**Eine Abweichung vom Pipeline-Konzept, und sie ist Absicht:** Das Konzept gattet den CognitiveGraph auf den **Management-Pfad** (Router: „Management oder Anliegen?"). Die Befunde aus §1 liegen aber im **Konversationspfad** — „ich habe eine neue Pflanze", „ein Geburtstag steht an" sind keine Agenten-Aufträge. Die erste Scheibe legt das Verstehen deshalb **vor den Gesprächsvektor**, wo es beiden Pfaden zur Verfügung steht. Der volle CognitiveGraph zwischen Router und Dispatch bleibt das Zielbild; diese Scheibe ist sein Eingangsschritt an anderer Stelle, nicht sein Ersatz.

---

## 3. Die Lage — das Artefakt

Je Turn ein strukturiertes Verständnis, **bevor** Gesprächsvektor und Verfasser arbeiten:

```
lage = {
  "gegenstand":     "worum es in diesem Turn geht, ein Satz",
  "nutzerziel":     "was der Nutzer damit erreichen will — das Gesagte
                     muss es nicht sein; gekennzeichnet als vermutet",
  "ausdrucksweise": "wie er es angeht (erzaehlend, prüfend, beilaeufig,
                     drängend, ...)",
  "objekte": [
    { "name":    "Geburtstag",
      "klasse":  "vorgang",          # Klassen aus frames_k §3.1
      "akut":    true,               # frames_k §4: latent | akut
      "gedeckt": {"anlass": "erwähnt im Turn"},
      "offen":   ["wer", "wann", "geschenk", "feier"] }
  ],
  "ziel_bezug":     "welches aktivierte Ziel (kurz-/mittel-/langfristig)
                     die Lage berührt, oder leer"
}
```

> **Nachtrag 13.09.2026 — der Wert in `gedeckt` ist die Angabe selbst.** Das Beispiel oben zeigt `{"anlass": "erwähnt im Turn"}` — eine Herkunft, keine Angabe. So hat es der Code nie gelesen: Der Sachlage-Prompt verlangt den Inhalt, der Auflöser schreibt ihn (Scheibe 6), Plausibilität und Verfasser-Block lesen ihn als Inhalt, und die Herkunft steht seit den Scheiben 6 und 9 in `quellen` und `sprecher`. **Entschieden vom Eigentümer am 13.09.2026:** Eine Eigenschaft ist *gedeckt*, wenn sie einen Wert hat — `gedeckt` ist eine Zuordnung Eigenschaft → Angabe (*»Rotationsperiode«: »33 Millisekunden«*). **Ohne Wert ist sie offen.** Einen Slot *»belegt, Inhalt unbekannt«* gibt es nicht. Das Beispiel bleibt als Erstfassung stehen; in heutiger Form hieße es etwa `{"anlass": "Geburtstag der Tochter"}`.
>
> **Das Artefakt ist seit den Scheiben 4–11 gewachsen**, ohne dass dieser Abschnitt nachgezogen wurde: `thema` (Pflichtfeld, Scheibe 4), `wiederaufnahme` (Scheibe 5), und je Objekt `quellen` (Scheibe 6), `plausibilitaet` (7), `traeger` und `recherche` (8), `sprecher` (9), `kritikalitaet` (10). `quellen`, `plausibilitaet` und `recherche` schreibt nur der Server — liefert das Modell sie, werden sie entfernt (Festlegung 1, Nachtrag). Scheibe 11 fügt dem Artefakt kein Feld hinzu: Sie legt `gedeckt` außerhalb der Blase ab.

**Vier Festlegungen, entschieden und begründet:**

1. **Eigener LLM-Call, nicht ein weiteres Feld im GV-Parse.** Der GV-Parse verliert heute zwei Drittel seiner eigenen Felder (§1). Ein eigener Call auf dem `analyse`-Backend hat einen eigenen Parser mit erzwungenem JSON (`expect_json`) und fällt laut aus statt still leer. Der Preis ist ein zusätzlicher Call je Turn; seine Dauer ist Teil der MESSUNG.

   > ⚠ **Der zweite Halbsatz gilt für die Syntax und nicht für die Form — gemessen am 12.09.2026 an einem abgebrochenen Betriebsturn.** `expect_json` erzwingt *gültiges JSON*, nicht die Gestalt des Artefakts. `_validate_artifact` prüft darüber hinaus den Parse auf dict, die fünf Pflichtfelder, `objekte` als Liste und jedes Objekt als dict mit Namen — und normalisiert `traeger`, `kritikalitaet` und `sprecher`. **Für `gedeckt` und `offen` gibt es keinen Normalisierer**, also genau für die zwei Felder, die §3 oben im Artefakt definiert und die alle zehn Scheiben lesen.
   >
   > **Die drei Felder mit Normalisierer stammen aus den Scheiben 7 bis 10, die zwei ohne aus Scheibe 1.** Die Prüfung ist mit den jüngeren Feldern gewachsen und nie zu den ältesten zurückgekehrt — jede Scheibe prüft, was sie selbst mitbringt; was schon da war, gilt als geprüft, weil es funktioniert hat.
   >
   > **Die Folge ist das Gegenteil der Festlegung:** Lieferte das Modell `gedeckt` als Liste, fiel es nicht laut aus. Eine von sechs Lesestellen riss den Turn ab (`.items()`), **drei iterierten die Liste und liefen lautlos falsch weiter**, zwei trugen einen lokalen `isinstance`-Riegel. Dazu Festlegung 3: Das volle Artefakt wird je Turn protokolliert — eine falsche Form steht dort als gültige.
   >
   > Geführt als `LAGE-FORMPRUEFUNG-UNVOLLSTAENDIG` (Band A). **Die Festlegung bleibt unverändert richtig; sie ist für zwei von fünf Feldern nicht eingelöst.** ~~Vor der Abhilfe steht eine Absichtsfrage beim Eigentümer: Ist eine Liste gedeckter Eigenschaften **ohne** Begründung eine zulässige Lesart des Feldnamens? Dann ist der Normalisierer eine Umformung; sonst verwirft er wie die drei bestehenden.~~ → **Beantwortet am 13.09.2026** (§3, Nachtrag am Artefakt): Ohne Wert ist eine Eigenschaft offen. Der Normalisierer formt deshalb um und verwirft nichts, was eine Aussage trägt — eine Liste gedeckter Namen wird zu offenen Eigenschaften.
   >
   > **Eingelöst am 13.09.2026.** `graph/nodes/sachlage_form.py::normalize_object_form` hält **jedes** Objektfeld, bevor ein Leser es sieht: `name` nicht leer (sonst wird das Artefakt verworfen), `akut` als Wahrheitswert (`"false"` gilt nicht mehr als wahr), `klasse` im Kanon oder entfernt, `gedeckt` als Zuordnung Text → Text mit Wert, `offen` als Liste ohne Dubletten und ohne Gedecktes; ein Name ohne Wert wandert nach `offen`, ein Wert unter `offen` nach `gedeckt`. Die Kopffelder müssen Text sein (eine Zahl wird Text, eine Liste verwirft das Artefakt). **Die Felder des Servers** — `quellen`, `plausibilitaet`, `recherche` — entfernt die Prüfung aus dem frischen Parse, weil ein Modell, das sie abschreibt, ungeprüfte Herkunft erzeugte; die Prompt-Ansicht der vorigen Blase trägt sie seither ebenfalls nicht mehr (vorher nur ohne `quellen`). **Die vorige Blase wird beim Laden geprüft** (`sachlage_load`): Sie ist eine externe Quelle, auch wenn dasselbe System sie schrieb; ohne Objektliste oder mit unlesbarem Objekt beginnt die Sachlage laut frisch. Der Gesprächskontext-Tab liest `gedeckt` über dieselbe Form (`api/drive.py::sachlage_lesen`).
   >
   > **Drei Regeln kamen aus der Messung dazu** (Scheibe 11): Ein Wert, der nur aus Kanonwörtern besteht (`nutzer`, `nova`, `welt`, `nachschlagen`, `kritisch`, `unkritisch`) oder die Vorlage des Prompts wiederholt (`nutzer|nova`), ist kein Wert — die Eigenschaft ist offen; ein Zeuge hält diese Wortmenge gegen die Kanons. Steht die Angabe im Namen und das Sprecherwort im Wert (`"Temperatur: 2,725 Kelvin": "nutzer"`), wird sie geborgen: Eigenschaft `Temperatur` → `2,725 Kelvin`, Sprecher `nutzer`. **Gleichnamige Objekte werden zusammengeführt** (`merge_same_name_objects`), weil das Gedächtnis je Paar und Name genau ein Objekt kennt — und **die Smalltalk-Schranke (Festlegung 2) sitzt seither hinter dem Zusammenführen**: Ob ein Objekt akut ist, steht erst fest, wenn alle Einträge gleichen Namens vereint sind. Träger, Kritikalität und Sprecher werden danach gegen `offen` und `gedeckt` gehalten.
   >
   > **Nachtrag nach der zweiten Kontrolle (13.09.2026, 15:29 UTC):** Der Vergleichsschlüssel für Eigenschaften und Objektnamen (`property_key`) schrieb klein mit `lower`, die Datenbank mit `casefold` — *»Weißer Zwerg«* und *»WEISSER ZWERG«* blieben in der Form zwei Objekte. Seither `casefold` wie die Datenbank, und die Schlüssel in `gedeckt` und beim Zusammenführen werden darüber entdoppelt. **Derselbe Wertprüfer gilt seither für jeden, der nach der Form nach `gedeckt` schreibt** (`covered_value`): Auch ein Anspruch des Frame-Auflösers ohne Wert — ein Kanonwort, leer — deckt nichts, die Eigenschaft bleibt offen (Scheibe 11).
   >
   > ~~29~~ **31** Zeugen (`tests/test_sachlage_form.py`, zwei zum ß nach der zweiten Kontrolle), rote Phase gegen eine leere Hülle 20 vorhergesagt, 20 rot; Gegenprobe: Formaufruf in der Prüfung entfernt → 18 rot wie vorhergesagt (17 neue und ein Bestandszeuge der Smalltalk-Schranke), Prüfung beim Laden entfernt → 1/1.
2. **Die Akutheit entscheidet über die Tiefe, nicht über das Ob.** Gegenstand, Nutzerziel und Ausdrucksweise werden **immer** erhoben — auch Smalltalk hat eine Lage. Objekte werden nur bei Akutheit mit offenen Eigenschaften ausgewiesen (*„der Rasen wächst wie verrückt"* ist latent: Objekt erkannt, keine Slot-Fragen). Das ist die Smalltalk-Schranke aus `frames_k` §4.3 in der Konversationsfassung.
3. **Die Lage wird protokolliert wie die Haltung.** Eine `berechnung`-Zeile im `pipeline_log` je Turn, mit dem vollen Artefakt — sonst ist keine spätere Auswertung möglich, welche Nutzerziele das System vermutet hat und ob sie trafen.
4. **Kein Zugriff auf den Bestand in dieser Scheibe.** Der Frame-Auflöser (Slots aus Fakten, Timeline, Lager füllen) ist Schritt 4.3 des Pipeline-Konzepts ~~und braucht das Frame-Lager~~. Die Lage sagt nur, was der **Turn** deckt und was offen ist. Was das Gedächtnis davon deckt, ist die nächste Scheibe. → **Seit dem 28.08.2026, spät: Scheibe 6** — ohne Frame-Lager, gegen den Gedächtnis-Pool des Turns, in einem eigenen Call nach der Lage (§4).

---

## 3a. Der Kontext als Blase — das Zielbild hinter der Lage

**Der Kontext ist kein Turn-Artefakt, sondern ein lebendes Objekt über Turns.** Er entwickelt sich mit jedem Turn weiter und verschiebt sich fließend — ein Gespräch wandert von *Kino* zu *Film* zu *Schauspieler* zu *Mensch* zu *Beziehung* zu *sozialer Gesellschaft* zu *Instabilität* zu *Krieg*, ohne dass irgendwo ein harter Schnitt läge. Die Blase wabert: Ausrichtung, Gesprächsvektor und Ziele verschieben sich in ihr.

Das deckt sich mit der Event Segmentation Theory: Das Gehirn hält **ein** aktuelles Situationsmodell (Hippocampus, medialer PFC) und aktualisiert es fortlaufend; erst wo die Vorhersage bricht — Wechsel von Ort, Akteuren, **Zielen** — liegt eine Ereignisgrenze (Zacks; DuBrow & Davachi). Zwischen den Grenzen ist der Kontext Kontinuität, keine Folge unabhängiger Momentaufnahmen.

**Die Blase kann sich teilen.** Zwei Gesprächsstränge bestehen parallel, die Sprecher wechseln fliegend zwischen Thema 1 und 2; beide leben eine Weile, bis eine Blase schrumpft und eingeht und die andere sich weiterentwickelt. Auch das ist beschrieben: Grosz & Sidner (1986) führen den Diskurs als **Stapel von Segmenten**, jedes mit eigenem Zweck — Segmente werden unterbrochen, wiederaufgenommen oder aufgegeben.

**Was die Blase enthält:** den Text, die dahinterliegenden Informationen, die aktivierten Frames (§2), die Landschaft — und **Emotion**. Je nachdem, worüber gesprochen wird, zieht die Blase die aktuelle Emotion des Charakters (EI-Calc) in ihre Richtung: Wer abwechselnd über Lustiges und Bedrückendes spricht, dessen Emotion wird dorthin **gezogen, sie springt nicht** — eine Gravitation, dieselbe Bauart wie der bestehende Raumzug (`ei/raum.py`: *„Novas Raum springt nicht: Er wird gezogen"*). Die Trägheit der Emotion ist empirisch belegt (emotionale Inertie, Kuppens et al. 2010).

**Die Haltung reguliert die Füllung der Frames.** Ob offene Eigenschaften aktiv geklärt werden, entscheidet nicht die Lage — sie sagt nur, *was* offen ist. Wie aktiv die Lücken verfolgt werden, ist Sache der Haltung (`ei/haltung.py`: `fragen`, `naehe`, `draengen`):

- **Distanz und Desinteresse → passive Füllung.** Der Frame bleibt offen und füllt sich nur aus dem, was das Gegenüber von selbst sagt. Kein aktives Nachfragen; die Antworten der maximalen Distanz sind *„Aha!"*, *„Okay!"*, *„Ja und?"* — das Verstehen läuft weiter, die Klärung nicht.
- **Fachgespräch und Zuwendung → aktive Füllung.** Hier darf mehrfach nachgefragt werden, auch zur Rückversicherung des Verstandenen: *„Warte, Du meinst, dass das Gelb dem Ganzen erst die besondere Substanz gibt?"*

**Damit bekommt die Frage nach der Fragen-Menge ihren richtigen Ort.** Sie ist keine eigene Stellgröße, die man global halbiert oder verdoppelt — sie ist der sichtbare Ausdruck davon, wie aktiv die Haltung die offenen Eigenschaften der Blase verfolgt. Eine Vorgabe „eine Rückfrage" ohne Gegenstand erzeugt Floskeln (gemessen, §1); eine Haltung mit Gegenstand erzeugt die richtige Zahl von selbst: null bei Distanz, mehrere im Fachgespräch.

**Was daraus für die Scheiben folgt:**

- **Scheibe 1 schreibt die Lage fort, statt sie je Turn frisch zu erheben.** Der Lage-Call bekommt die vorige Lage als Eingang; der neue Turn aktualisiert sie — Drift statt Neuaufbau. *Ausdrücklich anders als beim Kern-Hash entschieden* (dort ist Fortschreibung abgelehnt, `novaberg-pixie-character-hash.md` §3.1a): Der Kern soll dauerhaft sein und aus rückverfolgbarem Material entstehen; die Lage soll genau den **Zustand zwischen den Turns** tragen — Kontinuität ist hier der Zweck, nicht der Fehler. Ihre Quelle bleibt klein und präsent (die Turns der Session), der Drift bleibt dadurch nachvollziehbar.
- **Teilung und Parallel-Stränge sind Zielbild, nicht Scheibe 1.** Die erste Scheibe führt **eine** Blase. Erkennt die Analyse zwei unvereinbare Gegenstände, notiert sie das im Artefakt, mehr nicht — **gemessen am 28.08.2026: die Notiz ist das zweite Objekt** (§4, Scheibe 5) — die Stapel-Mechanik ist eine eigene, spätere Scheibe; ihr erster Schritt, die Wiederaufnahme einer früheren Blase aus `sachlage_verlauf`, ist Scheibe 5.
- **Der emotionale Zug der Blase auf EI-Calc ist eine eigene, spätere Scheibe.** Er braucht die fortgeschriebene Lage als Quelle und gehört deshalb hinter Scheibe 1; die Bauart (Zug statt Sprung) steht mit dem Raumzug bereits im Haus.

---

## 4. Die drei Scheiben des Umbaus

> **Nachtrag 13.09.2026:** Die Überschrift stammt aus der Erstfassung vom 28.08.2026. Heute sind es **elf** Scheiben; Scheibe 11 steht am Ende dieses Abschnitts.

### Scheibe 1 — die Lage-Analyse

**Gebaut am 28.08.2026, und der Name im Code lautet `sachlage`:** `F-LAGE-1..3` besetzen „Lage" fuer die emotionale Turn-Lage (Achsen, Sektor, Landschaft); die Sachlage ist ihre kognitive Schwester und traegt einen eigenen Namen. Knoten `sachlage_node` (LangGraph verbietet Knotennamen gleich State-Schluesseln), Modul `graph/nodes/sachlage.py`, Position `reducer → sachlage_node → router`.

Der Knoten (Arbeitsname `lage`) läuft im CharacterGraph vor dem Gesprächsvektor, schreibt `state["lage"]` und die Protokollzeile. Verfasser und GV bekommen einen `[LAGE]`-Block.

| | |
|---|---|
| **ZIEL** | Jeder Turn trägt ein strukturiertes, **fortgeschriebenes** Verständnis (§3a): Bei *„bei uns steht ein Geburtstag an"* nennt die Lage das akute Objekt `geburtstag` mit gedecktem Anlass und den offenen Eigenschaften Wer/Wann/Geschenk — bei *„der Rasen wächst wie verrückt"* das latente Objekt ohne offene Eigenschaften. Nennt der Folgeturn das Wer, wandert die Eigenschaft von offen nach gedeckt, statt dass eine neue Lage entsteht. |
| **TEST** | Zeugen auf das Artefakt: Pflichtfelder immer belegt; akutes Objekt trägt offene Eigenschaften, latentes keine; die Fortschreibung deckt Eigenschaften aus Folgeturns; Parser-Ausfall ist laut (kein leeres `lage` ohne Log-Zeile). |
| **MESSUNG** | Laborlauf gegen den Knoten direkt (nicht über den vollen Pfad — Alltagssätze lösen im Produktivpfad echte Schreibungen aus): 10 feste Äußerungen, je erwartete Akutheit und Objektklasse, dazu die Dauer des Calls. |

**Betriebsmessung am 28.08.2026, 15:30–16:06 UTC** (`labor/2026-08-28_sachlage_erster_zeuge.py`): fünf kurze Turns über `/chat` gegen den laufenden Server, ein Thema, fortlaufend. **6 Protokollzeilen `node='sachlage'`** — fünf gerechnete (`frisch`, dann viermal `fortgeschrieben`) und eine `impuls_uebernommen` für eine Zustellung im fünften Fenster. Die Fortschreibung hält über alle fünf Turns ein Objekt und deckt Eigenschaften 1 → 4; ab Turn 4 tritt ein zweites akutes Objekt hinzu. Dauer je Call im Betrieb 2,7–3,9 s (Labor: 2,1 s Median). Keine Schreibung in `fakten`, `notizen`, `ziele`, `wissensluecken`. **Beobachtung:** Eine offene Eigenschaft blieb offen, nachdem Nova sie im selben Turn ~~beantwortet~~ **behandelt** hatte — ~~die Fortschreibung liest Deckung aus Nutzer-Äußerungen, nicht aus den Antworten~~ (Fundliste 28.08.). **Nachgemessen am Abend, zweimal widerlegt und einmal bestätigt:** Die Fortschreibung liest Novas Antworten sehr wohl (gestellter Fall: Nova beantwortet eine von zwei offenen Eigenschaften, der Nutzer fragt die andere → 3/3 wandert die beantwortete nach *gedeckt*, `labor/2026-08-28_sachlage_deckung_aus_antwort.py`). Im Betriebsfall war Novas Antwort **keine**: Sie wiederholte die Frage nach Licht oder Wasser in vier Varianten und schlug ein Experiment vor — *offen* war die richtige Fortschreibung. Und ein Defekt lag trotzdem darunter: `_render_history` schnitt jeden Beitrag bei 400 Zeichen; diese Antwort war 973 lang, die Substanz begann bei 384, und sechs von elf Antworten des Paares sind länger als 400. Seit dem Abend: 1600 Zeichen je Beitrag, Regieanweisungen (*…*) entfallen vorher, eine Antwort bleibt eine Zeile, und der Prompt sagt ausdrücklich, dass Deckung von beiden Seiten kommt (Zeugen `DerVerlaufTraegtNovasAntwortTest`; Gegenprobe 1/2/1 rot). Für Scheibe 3 bleibt daraus ein Datenpunkt: Nova hat eine Sachfrage mit einer Rückfrage beantwortet — genau die Floskelform, die Scheibe 3 durch einen Gegenstand ersetzen soll, und die offene Eigenschaft ist dieser Gegenstand.

### Scheibe 2 — das kurzfristige Ziel (gebaut am 28.08.2026)

`ziel_typ='kurzfristig'` in der bestehenden `ziele`-Tabelle. Entsteht, wenn Lagen derselben Session wiederholt auf ~~dasselbe Nutzerziel~~ **dasselbe akute Objekt** zeigen (*Kräuterbeet*); verfällt in Stunden statt Tagen. Läuft durch die bestehende Gravitation und erscheint damit ~~ohne weiteren Bau~~ im `[GEDANKEN]`-Block — **gemessen am 28.08.2026: nicht ohne Bau.** Der Zielsatz liegt zur Nutzeräußerung bei Kosinus 0,13–0,41 (Stärke 0,09–0,29 unter der Schwelle 0,40); die Gravitation hätte ihn nie aktiviert. Seither ist ein kurzfristiges Ziel per Bauart aktiviert, solange es lebt — sein Tor ist der Verfall, und es steht im `[GEDANKEN]`-Block vorn (`ei/gravitation.py`, `novaberg-thinking-drive_k.md` §5).

> **Warum das Objekt und nicht das Nutzerziel — gemessen am 28.08.2026.** Die erste Fassung verglich die `nutzerziel`-Sätze zweier Turns per Kosinus (Schwelle 0,8). Über vier gestellte Turns lag der Kosinus beim **selben Vorhaben bei 0,42 / 0,40 / 0,50** und beim Themenwechsel bei **0,35 / 0,24** — kein Abstand. Das Feld ist so gemeint, wie der Prompt es fragt: *was der Nutzer mit dieser Äußerung erreichen will* — je Turn neu (»Verifizierung einer Hypothese…«, »Validierung eines Hilfsmittels…«). Das **akute Objekt** dagegen schreibt die Fortschreibung wörtlich fort (»Rettich bewässern« über drei Turns, dann »Flutberge«), weil der Prompt sie dazu anhält. Es ist der Träger der Beständigkeit; die Strecke zählt es per Namensgleichheit, ohne Schwelle.

| | |
|---|---|
| **ZIEL** | Aus zwei Lagen mit demselben Nutzerziel innerhalb einer Session entsteht ein kurzfristiges Ziel; nach Ablauf seiner Frist ist es inaktiv. |
| **TEST** | Zeugen auf Entstehung (zweimal dasselbe Ziel → Eintrag), Nicht-Entstehung (einmal → kein Eintrag) und Verfall. |
| **MESSUNG** | Ein gestellter Sessionverlauf im Labor; danach `ziele`-Tabelle und Gravitations-Log. |

**Gebaut am 28.08.2026.** `memory/kurzziel.py`: Der rechnende Weg des Sachlage-Knotens ruft nach der Verlaufszeile die Zielverfolgung (`short_goal_track`); je akutem Objekt zählt eine Strecke in Redis (`kurzziel:{paar}`) die aufeinanderfolgenden gerechneten Lagen, in denen es stand — bei **zwei** entsteht genau ein `kurzfristig`-Ziel (Zielsatz *»Ich möchte dem Nutzer bei seinem Vorhaben helfen: {Objekt} — {Nutzerziel des Turns}«*, Anker `KURZZIEL_MOTIVATION` = 0,7, Vektor über den Zielsatz), und solange das Objekt in der Blase steht, kein zweites. Eine frische oder nach Verfall neue Blase setzt die Strecke zurück; ein Objekt, das die Blase verlässt, fällt heraus. **Der Verfall** läuft über den bestehenden Decay-Agenten, der seither zwei Typen fährt — mittelfristig in Tagen, kurzfristig in Stunden (`ZIEL_KURZFRISTIG_DECAY_STUNDEN` = 3; bei Schwelle 0,15 ist das Ziel nach knapp drei Halbwertszeiten inaktiv). **Nachgemessen am Abend des 28.08.2026: Der Decay-Agent läuft einmal am Tag** (`PIXIE_DECAY_INTERVALL_SEKUNDEN` = 86400, `hintergrund_log` 25.–27.08. je ~19:58 UTC) — das Ziel hätte bis zum Lauf des Folgetags gelebt, ~25 h statt ~7, und so lange im `[GEDANKEN]`-Block gestanden, auch bei fremden Turns. Seither rechnet der Lader `ziele_aktive_laden` die Motivation beim Lesen aus Anker und Alter, für jeden Typ mit Halbwertszeit, und liefert nichts unter der Schwelle (`novaberg-thinking-drive_k.md` §7.1; Zeugen `DerVerfallBeimLesenTest`, `DerLaderLaesstVerfallenesLiegenTest` in `test_kurzziel.py`). Der Impuls-Weg verfolgt nicht. **Zwei Dinge kamen aus der Messung dazu:** das Bauart-Tor in der Gravitation (oben) und eine Prompt-Regel des Sachlage-Calls — *ein Objekt, das schon steht, behält seinen Namen wörtlich* —, weil die Fortschreibung das Objekt in einem von drei Läufen umbenannte (»Rettichbewässerung« → »Bewässerungsstrategie«) und das Ziel dann einen Turn später entstand. 21 Zeugen; Gegenprobe: fünf Eingriffe, 3 / 2 / 1 / 1 / 2 rot.

**MESSUNG** (`labor/2026-08-28_kurzziel_messung.py`, Sachlage-Knoten mit echtem Modell-Call, Embed-Worker, Redis, `ziele`): Turn A (Rettich gießen) → Strecke 1, kein Ziel · Turn B (dasselbe Vorhaben, andere Worte) → Strecke 2, **ein** `kurzfristig`-Ziel · Turn C → Strecke 3, **kein** zweites · Turn D (Flutberge) → Strecke des alten Objekts weg, neues bei 1. Je Call 2,7–4,7 s (Sachlage samt Verfolgung). Gravitation danach: das kurzfristige Ziel aktiviert bei Stärke 0,28 (Turn B), 0,23 (Turn C) und — per Bauart — auch 0,09 bei einem fremden Turn; der `[GEDANKEN]`-Block trägt es damit, solange es lebt, gleich worum der Turn geht. **Betrieb:** zwei echte Turns zu einem Vorhaben (Transitmethode) → Strecke 1, dann `Kurzziel: angelegt (id=28576, Strecke 2)` — das Ziel steht in `ziele` für das echte Paar.

### Scheibe 3 — die Gesprächsführung aus der Lage

Die Rückfrage-Zeile des Verfassers (`ei/haltungssprache.py::_rueckfragenzeile`, seit 27.08. mit Menge und Art) bekommt ihren **Gegenstand**: die wichtigste offene Eigenschaft eines akuten Objekts, oder der nächste Schritt zum berührten Ziel. Fehlt beides, bleibt die heutige Art-Vorgabe der Rückfall. Ob Frage oder Aussage, bleibt Sache von GV-Vehikel und Haltung — die Lage liefert das Wohin, nicht das Wie.

| | |
|---|---|
| **ZIEL** | Die Rückfrage ist das Produkt aus Lage und Haltung: Auf *„bei uns steht ein Geburtstag an"* fragt Nova im zugewandten Gespräch nach Wer/Wann/Geschenk — nicht *„sollen wir tiefer eintauchen?"*. Bei distanzierter Haltung fragt sie **nicht**: Die Eigenschaften bleiben offen und füllen sich nur passiv. |
| **TEST** | Zeugen auf die Zeile: Bei akutem Objekt und fragender Haltung nennt sie dessen offene Eigenschaft; bei niedriger `fragen`-Haltung nennt sie trotz offener Eigenschaften keine — die Lage erzeugt keine Frage an der Haltung vorbei; ohne akutes Objekt steht die Art-Vorgabe wie heute. |
| **MESSUNG** | Die Schauspielerprobe-Anordnung vom 27.08.2026 (fester Prompt-Aufbau, T=0,0, Vergleichsarme mit/ohne Lage) über die 10 Äußerungen der Scheibe-1-Messung: Anteil der Schlussfragen, die eine offene Eigenschaft oder ein Ziel adressieren, vorher gegen nachher. |


**Gebaut am 28.08.2026, abends.** `graph/nodes/sachlage.py::question_target` (rein): die erste offene Eigenschaft des ersten akuten Objekts — `offen` ist im Prompt nach Wichtigkeit geordnet — als *»Objekt — was dazu noch offen ist: Eigenschaft«*; fehlt sie, das Vorhaben des kurzfristigen Ziels (*»wie es mit … weitergeht«*); Novas eigene mittel- und langfristigen Ziele sind kein Gegenstand für eine Frage an den Nutzer. `ei/haltungssprache.py::_rueckfragenzeile` hängt ihn hinter Menge und Art (*»… — ihr Gegenstand: …«*), **und die Haltung bleibt der Regler:** bei *»keine Rückfrage«* entfällt er wie die Art. Der Verfasser reicht ihn durch (`stoffzeilen(…, gegenstand)`) und loggt *`Verfasser: Rueckfrage-Gegenstand: …`*. 10 Zeugen (6 rein, 3 an der Zeile, 1 am gebauten Verfasser-Prompt), Suite 2478 → 2488 grün; Gegenprobe vier Eingriffe: 1 / 2 / 3 / 1 rot.

**MESSUNG** (`labor/2026-08-28_scheibe3_zwei_arme.py` — die Anordnung dieser Tabelle: echter Verfasser-Systemprompt, Haltung `werkstatt` mit fragendem Rad, T = 0, über die zehn Äußerungen der Scheibe-1-Messung; Arm A `question_target` → None, Arm B echt): Die Lage lieferte in **4 von 10** einen Gegenstand (Geburtstag → Anlass, Auto → Grund des Werkstattbesuchs, Ausflug → Zielort, Kuchen → Art des Kuchens; »neue Pflanze« fiel diesmal latent — der bekannte Grenzfall). **Rückfrage im Stoff: A 10/10, B 10/10** — die Menge ist Haltungssache und blieb gleich. **Die Rückfrage trifft den Gegenstand: A 1/4, B 4/4.** Ohne Gegenstand fragte der Verfasser nach Gästezahl, Dauer, Schwierigkeitsgrad — plausibel und beliebig; mit Gegenstand nach genau dem, was die Lage offen nannte. **Erste Fassung der Messung zählte Frage-Enden und maß am Gegenstand vorbei:** Der Verfasser schreibt Stoff in dritter Person (*»Person A fragt nach dem Anlass«*), das Frage-Ende ist Sache des Responders. **Betrieb 21:04 UTC** (`labor/2026-08-28_scheibe3_betriebszeuge.py`, Konversationspfad): Log *`Rueckfrage-Gegenstand: Schwarzes Loch — was dazu noch offen ist: mathematische Formel des Gravitationslinseneffekts`*; Novas Antwort schloss mit *»Welche konkreten Werte hast du … im Kopf? Brauchen wir primär den Abstand zur Lichtquelle oder direkt den Ablenkungswinkel …?«* — die **zweite** offene Eigenschaft der Lage (*benötigte Parameter*), nicht die erste. Ein erster Betriebsturn mit *»am Wochenende«* lief auf den Management-Pfad (Timeline-Agent, Termin am Datum gescheitert, 0 Zeilen) — dort läuft kein Verfasser; Messturns tragen keine Zeitangaben, auch nicht bei Astronomie.

**Reihenfolge ist Abhängigkeit:** 2 und 3 lesen nur, was 1 erzeugt. Nach Scheibe 1 wird gemessen, bevor 2 begonnen wird — trägt die Lage nicht, sind 2 und 3 gegenstandslos. **Gemessen am 28.08.2026 im Betrieb: sie trägt** (Betriebsmessung unter Scheibe 1).

---


**Nachtrag 29.08.2026, abends — der eigene Zug.** Die Betriebsmessung der Kette (§4, unten) zeigte: Im Wissensgespräch gibt es keine `nutzer`-Eigenschaft, der Gegenstand kommt in 17 von 20 Turns aus dem stärksten aktivierten Kurzziel — das ist die Gravitation (`ei/gravitation.py` sortiert nach Typ und Aktivierungsstärke), und sie ist gewollt: Nova hört, was sie zieht. In den ersten zwei Turns eines neuen Themas ist das das alte Ziel, weil das neue noch keines hat (Scheibe 2, Strecke 2/2). Falsch war die Form: Die Rückfrage-Zeile gab Novas Zug als Gegenstand einer Frage an den Nutzer zu *seiner* Sache aus, und der Verfasser verbog dann das Nutzerthema, um den Magnetar hineinzubekommen. **Seither** trägt der Gegenstand seine Herkunft (`question_target_origin`: `blase` — eine offene Eigenschaft des Nutzers oder ein Kurzziel zu einem akuten Objekt der Blase — oder `eigener_zug`), und die Rückfrage-Zeile führt bei eigenem Zug: *»… ihr Gegenstand: wie es mit Magnetar weitergeht; das ist Person As eigenes Ziel, nicht die Sache von Person B — sie nennt es als das, was sie gerade zieht, und lässt ihm die Wahl, mitzugehen«* (`ei/haltungssprache.py::_rueckfragenzeile`, F-PROMPT-1). `question_target` bleibt wortgleich; die Haltung bleibt der Regler. 10 Zeugen (`tests/test_question_origin.py`), Suite 2571 → **2581 grün**, Gegenprobe 2/2/1 rot (vorhergesagt 1/2/1 — der Verdrahtungszeuge liest die Herkunft ebenfalls). **Labor** (`labor/2026-08-29_rueckfrage_eigener_zug_zwei_arme.py`, Blase Gamma-Oszillationen + Kurzziel Magnetar, 5 Läufe je Arm, T = 0): **Arm A verbog in 5 von 5 Läufen das Nutzerthema** (*»Magnetar-Modell als Analogie für neuronale Felder«*, *»Anwendung bei Magnetar-ähnlichen Feldern«*), **Arm B nannte in 5 von 5 den Magnetar als Novas nächsten Schritt mit Wahl für den Nutzer** (*»ihr aktuelles Ziel … das Thema Magnetar weiterzuverfolgen, und fragt, ob der Nutzer diesen Pfad einschlagen möchte oder lieber bei der Mechanik bleibt«*, *»zieht als ihr eigenes Ziel die Untersuchung von Magnetaren heran«*) — gezählt am Satz, nicht per Muster. **Betrieb 21:40 UTC** (Themenwechsel zu Sonnenflecken bei Kurzziel *Lichtablenkung*): Herkunft erkannt (*»Rueckfrage-Gegenstand: wie es mit Lichtablenkung am Sonnenrand weitergeht (Novas eigener Zug)«*); die Haltung setzte in diesem Turn keine Rückfrage, die Zeile kam ohne Gegenstand, Nova antwortete schlicht — die Führung greift nur, wenn die Haltung fragt.

**Die Namen des Lesers (29.08.2026, spät).** Der Verfasser ist der Schauspieler, der Charakter sein Auftrag — er bekommt einen Auftrag über **Person A** und **Person B** und wird nirgends als der Charakter angesprochen (Verfasser-Konzept §2.2a/§2.2b, 14.08.2026). Der `[SACHLAGE]`-Block der Scheiben 1–9 trug *»der Nutzer«*, *»Nova«* und *»beantworte es aus deinem Wissen«* in diesen Prompt — ein zweites und drittes Namenssystem, die am 13.08. gemessene Fehlerklasse; der Gesprächsvektor liest denselben Block und kennt Nova und den Nutzer beim Namen. Seither hat der Block einen Leser (`sachlage_block(…, leser=LESER_VERFASSER | LESER_GV)`, `speaker_lines` ebenso; `_reader_names` liefert die Namen, ein unbekannter Leser ist ein Fehler; im Verfasser bündelt `_question_target` Gegenstand und Herkunft): beim Verfasser *»Was Person B vermutlich will«*, *»Person B hat zu … gesagt«*, *»Person A hat schon zu … gesagt«*, *»Dazu weiss Person A schon«*, *»Person B will zu … wissen: … — Person A beantwortet es aus ihrem Wissen«*, *»Person B kommt auf … zurück«*; beim Gesprächsvektor dieselben Zeilen mit *Nova* und *der Nutzer*. Die Brücke (Scheibe 4) spricht über Person A statt mit ihr (*»Person As Gedanke entstand früher«*), die Rückfrage-Zeile nennt *»Person As eigenes Ziel«*, und der Herkunftsblock zitiert die Sprecherzeilen in denselben Namen. 5 Zeugen (`tests/test_sachlage_block_reader.py`, darunter ein Muster, das jede Charakter-Ansprache im Verfasser-Block rot macht), Suite 2581 → **2586 grün**, Gegenprobe 1/2 rot wie vorhergesagt. Die Zitate der Scheiben 6–9 oben stehen in der Form des Gesprächsvektors.

### Scheibe 4 — das Sachlage-Gedächtnis (entworfen und gebaut am 28.08.2026)

**Der Anlass sind Pixies Zustellungen.** Ein Impuls beruht auf einem Turn; wenn die Aussage im Chat landet, ist die Assoziation zum Auslöser oft schwer nachvollziehbar. Empathisch wäre ein **Übergang vom Kontext des aktuellen Turns zum Kontext des Auslösers** — dafür braucht es beide Enden, und das zweite existiert nicht: Die Sachlage wird heute je Paar **überschrieben** (Redis, Verfall 4 h); die `pipeline_log`-Zeile je Turn ist Forensik mit Vorhaltefrist, ohne Thema, ohne Embedding, nicht abfragbar. Geprüft am 28.08.2026: Der Impuls-Stack-Eintrag trägt Thema und Embedding, aber **keine `turn_id` seines Auslösers** — die Zuordnung ist nur implizit über Ähnlichkeit.

**Was schon trägt:** `turn_id` ist das Rückgrat, an dem alles Turnbasierte hängt — `turn_roh` (Wortlaut), `verbindung → lzg_knoten`, die Achsen-Protokolle, die Charakter-Destillation. Die Turns tragen das Emotionale, das *Wie*; die Sachlage trägt die Fakten. Eine je Turn persistierte Sachlage reiht sich mit einer Spalte ein.

**Der Bau, drei Teile:**

1. **Tabelle `sachlage_verlauf`** — je gerechnetem Turn eine Zeile: `turn_id`, Paar (`user_id`, `character_id`), **`thema`**, `gegenstand`, `nutzerziel`, `ausdrucksweise`, `objekte` (jsonb), **`embedding vector(768)`**, `erstellt_am`. Drei Festlegungen formen sie: Der Embedding-Text ist der `gegenstand`-Satz und aus der Zeile rekonstruierbar (`F-EMBED-1`); ein Vektor repräsentiert genau diesen einen Gegenstand (`F-EMBED-2`); und die Tabelle **verfällt nicht** — sie protokolliert ein Faktum, *die Sachlage dieses Turns war X* (`F-VERFALL-1`: was als Faktum protokolliert, bleibt). Geschrieben wird nur auf den drei rechnenden Wegen (frisch, fortgeschrieben, verfallen_neu) — übernommene Artefakte erzeugen keine Doppelzeile.
2. **`thema`** wird ein Pflichtfeld des Artefakts — derselbe LLM-Call liefert es mit, ein bis drei Worte. Es ist der Anzeigename der Blase (auch für den Kontext-Tab — seit dem 28.08.2026 abends gebaut: `client/ui/panels/sachlage_panel.py` »🫧 Gesprächskontext«, gespeist aus `GET /drive/kontext`) und das Findewort neben dem Vektor. **Und es benennt die Sache, nie den Wechsel** — seit dem Abend des 28.08.2026 als Prompt-Regel, weil der erste Betriebsturn nach dem Bau (Rettich → Neutronensterne) `thema='Themenwechsel'` und einen `gegenstand` über den Nutzer schrieb; aus den echten Turns nachgestellt **5/5** so, mit der Regel **5/5** »Neutronensterne« und ein Gegenstand, der die Sache nennt (`labor/2026-08-28_sachlage_thema_wechsel.py`). Der Vektor der Wechselzeile zeigte vorher halb auf das alte Thema — genau an der ersten Zeile jeder neuen Blase.
3. **Die Brücke:** Der Impuls-Eintrag bekommt bei seiner Entstehung die **`turn_id` seines Auslöser-Turns**. Bei der Zustellung lädt der Graph die Auslöser-Zeile aus `sachlage_verlauf`, und der Verfasser bekommt beide Blasen als `[SACHLAGE-BRÜCKE]` — *aktuell* und *damals* — mit dem Auftrag, den Übergang zu bauen statt unvermittelt einzuwerfen. Rückfall ohne harte `turn_id`: die ähnlichste Verlaufszeile per Embedding gegen das Impuls-Embedding, als Rückfall gekennzeichnet.

| | |
|---|---|
| **ZIEL** | Eine Zustellung nennt hörbar, woran sie anknüpft: Der Verfasser sieht die Auslöser-Sachlage neben der aktuellen und baut den Übergang. Nebenziel: `SELECT … FROM sachlage_verlauf ORDER BY embedding <=> $1` findet zu einem Thema die Turns, in denen es Gegenstand war. |
| **TEST** | Zeugen auf: Schreibweg nur bei gerechneten Artefakten · Embedding-Text = `gegenstand` (rekonstruierbar) · Brücke nur mit beiden Enden, Rückfall gekennzeichnet · Impuls-Eintrag trägt die Auslöser-`turn_id`. |
| **MESSUNG** | Ein gestellter Verlauf im Labor: Impuls aus Turn A, Zustellung in Lage B — der Verfasser-Prompt trägt beide Blasen; dazu die Trefferprobe der Embedding-Suche über zehn persistierte Lagen. |

> **DDL:** `sachlage_verlauf` ist eine neue Tabelle (additiv) und wird nach `F-DDL-1` **vor dem Bau angekündigt**; sie wirkt erst nach Neustart des Dienstes. Ablageort nach `F-SCHEMA-1` in der zuständigen `init.sql`. **Angekündigt, freigegeben und angelegt am 28.08.2026** — dazu eine zweite, im Entwurf nicht genannte Spalte: `shadow_auftrag.ausloeser_turn_id`. Ohne sie hätte der Stapel-Eintrag die `turn_id` seines Auslösers nie bekommen können, denn der Auftrag ist das erste Glied der Kette (Auftrag → Stapel → Ereignis → Brücke), und er hatte keine Spalte dafür. **Kein Verfall, weil Turns auch keinen haben** — so entschieden bei der Freigabe.

**Gebaut am 28.08.2026.** `memory/sachlage_history.py` (Repository: schreiben, per `turn_id` lesen, per Vektor die ähnlichste Zeile eines Paares — mit Schwelle), der Knoten schreibt auf den drei rechnenden Wegen und baut auf dem Impuls-Weg die Brücke (`sachlage_bridge_build`, Weg `turn_id` oder `embedding_rueckfall` als Begleitfeld), der Verfasser bekommt `[SACHLAGE-BRUECKE]` nach `[SACHLAGE]`. Die `ausloeser_turn_id` wandert vom Turn über den Salienz-Schreibauftrag ins KZG-Hash, von dort in den Auftrag (KZG-Store, KZG-Queues, Synapsen-Promotion; der Recherche-Verweis trägt keine — dort steht `None`), dann über `shadow_auftrag`, den Stapel-Eintrag und das Ereignis-Payload bis in den Knoten. **Den Anfang der Kette fand erst der Bestand, nicht der Bau:** 0 von 300 KZG-Hashes trugen eine `turn_id` — der Salienz-Schreibauftrag reichte sie nicht durch, und das Hash-Mapping kannte das Feld nicht; 21 Aufträge nach dem Bau trugen deshalb `NULL`. Beides geschlossen, mit Zeugen an allen drei Stellen; der nächste echte Turn schrieb sein Hash mit `turn_id`. **Der Rückfall rechnet das Impuls-Embedding nach** — aus derselben Formel wie der Stapel (`build_impulse_embed_text`) — statt 768 Zahlen durch das Ereignis zu schicken. `thema` ist Pflichtfeld des Artefakts. 31 Zeugen, Suite 2427 grün; Gegenprobe: drei Eingriffe, je 1–2 rot (der Verfasser-Zeuge musste dafür erst vom Quelltext-Grep zum Verhaltenszeugen werden — der Import trug den Namen weiter).

**MESSUNG** (`labor/2026-08-28_sachlage_bruecke_messung.py`, gegen Repository, Embed-Worker, Knoten und Verfasser, ohne LLM-Antwort): zehn persistierte Lagen, je ein synthetischer Impuls zum eigenen Thema — **Rang 1 in 10 von 10**, Kosinus 0,40–0,76 (Median 0,65); ein Impuls zu einem fremden Thema: nächste Zeile 0,25, keine Brücke. Brücke hart (Impuls aus Lage A in Lage B) → `weg=turn_id`, `damals` = A; ohne `turn_id` → `weg=embedding_rueckfall` bei 0,73; der Verfasser-Prompt trägt beide Blasen nach `[SACHLAGE]`. **Die Schwelle stand zuerst auf 0,50 und hätte eine von zehn eigenen Lagen verworfen** — auf 0,35 gesetzt (`SACHLAGE_BRUECKE_MIN_KOSINUS`); der Fremdwert ist mit n=1 dünn belegt (Fundliste). Betrieb: ein echter Turn schreibt die Verlaufszeile mit Vektor (Log `Sachlage-Verlauf: turn=… abgelegt`); die erste echte Zustellung mit Brücke steht aus. ~~weil nur Aufträge, die **nach** dem Bau entstehen, eine `ausloeser_turn_id` tragen — der Altbestand des Stapels läuft über den Rückfall.~~ → **Diese Begründung ist am 30.08.2026 widerlegt:** Auch die neuen Aufträge tragen sie fast nie. Zuerst gemessen als **6 von 148** — diese Zahl enthielt **122 Zeilen der Suite** (`user_id='zeuge'`) und ist berichtigt: Nach Erzeuger getrennt tragen die **echten** Aufträge seit dem Bau **22 von 59** die Kennung, davon `recherche` **4 von 4**, `wissen_rueckweg` 16 von 31 und **`vertiefen` 2 von 24**. Die Brücke ist damit nicht tot, sondern ungleich verdrahtet; der offene Pfad ist die Vertiefung, und der Rest bei `wissen_rueckweg` liest KZG-Einträge von vor dem 28.08., die keine `turn_id` tragen (Fundliste 30.08.2026).

---


### Scheibe 5 — die Wiederaufnahme (entworfen am 28.08.2026, abends)

**Der Anlass ist ein Satz mit zwei Sachen:** *„Nochmal zurück zur Gravitationslinse bei Schwarzen Löchern: kann man die Brechung des Lichts als Grundlage für die Bestimmung der Masse hernehmen? Ach, und nimm die Zitrone auf die Einkaufsliste, die sind alle."* Gemessen gegen den Knoten (drei Läufe, `labor/2026-08-28_sachlage_zwei_themen.py`): Die zwei Sachen liegen heute schon als **zwei Objekte in einer Blase** — `thema`/`gegenstand` tragen die Hauptsache, die Einkaufsliste steht als Objekt der Klasse *anliegen* daneben (akut 1/3, latent 2/3). Die „Notiz im Artefakt", die §3a für zwei unvereinbare Gegenstände verspricht, **ist damit das zweite Objekt** — kein eigenes Feld. Was fehlt, ist das *„nochmal zurück"*: Die Fortschreibung liest nur die aktuelle Redis-Blase (Pulsare); dass die Gravitationslinse vor zwei Blasen schon Gegenstand war, sieht sie nicht — gedeckt/offen jener Blase sind weg, obwohl `sachlage_verlauf` sie seit Scheibe 4 trägt, und der Objektname fiel dreimal anders aus, weil kein Vorgänger da ist, an dem die Wörtlich-Regel greift.

**Der Bau, schmal:** Vor dem Sachlage-Call sucht der Knoten auf dem rechnenden Weg in `sachlage_verlauf` die ähnlichste Zeile des Paares zum Reiz — mit dem Prompt-Embedding, das der Enricher ohnehin gerechnet hat — **unter Ausschluss des aktuellen Themas** (sonst fände er die eigene Blase). Liegt sie über `SACHLAGE_WIEDERAUFNAHME_MIN_KOSINUS`, bekommt der Prompt sie als *frühere Sachlage zu einer ähnlichen Sache* mit der Anweisung: Kehrt der Turn zu ihr zurück, führe **sie** fort — Objektname wörtlich, gedeckte Eigenschaften bleiben gedeckt; sonst ignoriere sie. Das Artefakt trägt `wiederaufnahme` (turn_id, thema, kosinus) oder `null`; der `[SACHLAGE]`-Block bekommt eine Zeile *„Der Nutzer kommt auf … zurück (zuletzt vor …)"*, damit der Verfasser den Anschluss hörbar machen kann. **Kein Stapel im Sinne von Grosz & Sidner** — keine zwei lebenden Blasen, kein Push/Pop. Die Wiederaufnahme holt eine Blase aus dem Faktum zurück; ob daraus ein Stapel wird, entscheidet der Betrieb.

| | |
|---|---|
| **ZIEL** | Kehrt ein Turn zu einer Sache zurück, die eine frühere Blase dieses Paares war, führt die Sachlage jene Blase fort — Objektname wörtlich, gedeckte Eigenschaften bleiben gedeckt — statt bei null zu beginnen; ein Turn ohne solche Blase bekommt keine. |
| **TEST** | Zeugen: die Suche läuft nur auf dem rechnenden Weg, mit dem Prompt-Embedding, unter Ausschluss des aktuellen Themas; ohne Treffer keine Sektion und `wiederaufnahme: null`; mit Treffer trägt der gerenderte Prompt die frühere Blase und die Regel, das Artefakt die Kennung; fällt der Vektor aus, läuft der Turn ohne Suche und sagt es; das Repository überspringt Zeilen des ausgeschlossenen Themas (live). |
| **MESSUNG** | Labor gegen Knoten und Repository mit einem Laborpaar: drei persistierte Blasen (Gravitationslinse, Pulsare, Rettich), vorige Blase Pulsare, dann der Zwei-Sachen-Satz — Treffer auf die Gravitationslinsen-Zeile mit Kosinus, und der Objektname des Artefakts ist der der alten Blase, ihre gedeckten Eigenschaften stehen wieder; dazu ein fremder Satz ohne Treffer. Die Schwelle wird an den Kosinuswerten dieser Reihe gesetzt, nicht geraten. |

**Gebaut am 28.08.2026, abends.** `history_nearest` kennt `ausser_thema`; der Knoten sucht auf dem rechnenden Weg mit `state["prompt_embedding"]` (`_resume_lookup`, Rückfall: eigener Embed-Call, bei Ausfall Warnung und kein Suchlauf), `_derive` rendert die Treffer-Blase als *frühere Sachlage* mit der Fortführungsregel, das Artefakt trägt `wiederaufnahme` (turn_id, thema, kosinus, erstellt_am) oder `null`, der `[SACHLAGE]`-Block die Zeile *„Der Nutzer kommt auf … zurück (zuletzt vor …)"*. 8 Zeugen (7 im Knoten, einer live gegen das Repository), Suite 2478 grün; Gegenprobe vier Eingriffe: 1 / 3 / 1 / 1 rot. **MESSUNG** (`labor/2026-08-28_sachlage_wiederaufnahme_messung.py`, Laborpaar, abgeräumt): Kosinus Reiz × Gegenstand-Vektor — Rückkehrsatz 0,404 zur Gravitationslinse (0,174 Rettich, 0,219 Pulsare), knapper Rückkehrsatz 0,399, fremder Satz (Sanskrit) 0,126–0,157, ein Satz zur eigenen Blase 0,464 auf sie und 0,229 auf die nächste fremde. Suche: beide Rückkehrsätze → Gravitationslinse, fremd → keine, eigene Blase → keine (ausgeschlossen). Artefakt mit Wiederaufnahme, drei Läufe: `thema='Gravitationslinse'`, Objektname **wörtlich**, beide gedeckten Eigenschaften stehen wieder, die gefragte Eigenschaft wandert nach gedeckt, „Abhängigkeit vom Spin" bleibt offen. **Schwelle `SACHLAGE_WIEDERAUFNAHME_MIN_KOSINUS` = 0,35** — zwischen 0,23 (nächste fremde Blase eines Nicht-Rückkehr-Satzes) und 0,40 (Rückkehr), n = 4 Sätze, dünn belegt. **Ein Preis, gemessen:** Mit der Sektion im Prompt verschwand die Nebensache (Einkaufsliste) aus dem Artefakt (0/3); mit dem Satz *„Was der Turn sonst noch nennt, bleibt ein eigenes Objekt"* steht sie in 2/3 wieder, latent (Fundliste). **Betrieb belegt 28.08.2026, 20:44 UTC** (`labor/2026-08-28_sachlage_wiederaufnahme_betriebszeuge.py`, ein echter Turn *„Nochmal zurueck zu den Magnetfeldern der Pulsare von vorhin …"* bei lebender Transit-Blase): `Wiederaufnahme: fruehere Blase 'Astrophysikalische Magnetfelder' (turn=0d8b03b4…, kosinus=0.62)` vor dem Call; die neue Verlaufszeile (id 221) traegt `thema='Pulsar-Magnetfelder'` — die Sache, nicht die Rueckkehr —, das Objekt **`Pulsar-Magnetfeld` woertlich** wie in der Zeile von 17:24, deren gedeckte Eigenschaft bleibt gedeckt, und zwei ihrer offenen wanderten nach gedeckt, weil Novas 978 Zeichen lange Antwort von 17:25 jetzt ganz im Fenster steht; `pipeline_log` traegt `wiederaufnahme` mit turn_id, thema, kosinus 0,6197 und Zeitstempel. Sachlage-Call 4,2 s samt Suche.

### Der Gesprächskontext im Client (28.08.2026, abends)

Die fünf Scheiben sind in einem Tab sichtbar — »🫧 Gesprächskontext« (`client/ui/panels/sachlage_panel.py`, `GET /drive/kontext` in `api/drive.py`): die Blase mit Thema, Herkunft, Alter und einem Balken gegen den 4-h-Verfall; Gegenstand, Nutzerziel, Ausdrucksweise; die Wiederaufnahme, wenn eine war (Scheibe 5); je Objekt Klasse, akut/latent, Strecke *n*/2 samt Ziel-id (Scheibe 2), gedeckte Eigenschaften und offene — die, die die Rückfrage bekäme, mit ★ (Scheibe 3, `question_target`); die lebenden kurzfristigen Ziele mit **Live-Motivation** aus demselben Lader wie die Gravitation und der Restlaufzeit bis zur Schwelle (`t = HWZ · log₂(m/0,15)`); die jüngsten acht Blasen des Paares aus `sachlage_verlauf` (Scheibe 4, `history_recent`); seit dem 28.08. spät hinter einer gedeckten Eigenschaft ihre Quelle (`⟵ kzg G5`, Scheibe 6), seit dem 29.08. je Objekt die Zweifel (`⚠ Stufe: Behauptung — Grund`, Scheibe 7), hinter offenen Eigenschaften der Träger (`↳welt`, `↳nachschlagen`) und Suchtreffer (`🔎`, Scheibe 8), hinter gedeckten der Sprecher (`↳nutzer`, `↳nova`, Scheibe 9). Nicht angezeigt: die Brücke einer Zustellung — sie lebt nur im State des Impuls-Turns. **Befund beim Bau:** Das Scheibe-1-Panel vom Morgen war registriert, stand aber nicht in `_TOOLBAR_PANELS` des Hauptfensters — der ausstehende Client-Zeuge konnte so nie gelingen. **Seit dem 30.08.2026 gibt es ihn für genau diese Frage:** `client/tests/test_toolbar_panels.py` hält die Registry gegen die Toolbar und nennt den Gesprächskontext-Tab namentlich. Das **Rendern** des Tabs — Träger, Sprecher, Zweifel, Quellen — ist weiterhin ohne Zeugen.

### Scheibe 6 — der Frame-Auflöser, Konversationsfassung (entworfen am 28.08.2026, spät)

**Der Anlass:** Scheibe 3 fragt nach der wichtigsten offenen Eigenschaft eines akuten Objekts — auch dann, wenn Nova die Antwort längst hat. Die Sachlage weiß nur, was der **Turn** deckt (§3, Festlegung 4); was das Gedächtnis deckt, sieht sie nicht. Das ist Schritt 4.3 des Pipeline-Konzepts: Lücken gegen Wissensquellen halten, je aufgelöstem Slot die Quelle vermerken.

**Der Bestand, gemessen am 28.08.2026, 22:32 UTC, für das Paar:** Der Fakten-Graph des Konzepts (Quelle 1) trägt **0 Zeilen** und `find_similar` hat keinen Aufrufer; `notizen` 1 Zeile, `timeline` 40 (39 Erinnerungs-Anker, ein Termin); ein Frame-Lager gibt es nicht. **Novas Wissen liegt woanders:** `autonomous_wissen` **1012** Zeilen (475 `echte_tiefe`, alle mit Vektor), `lzg_knoten` 2910 — und beides fließt **schon je Turn** in den State: Der Enricher sucht KZG, LZG, Bibliothek und Aufzeichnungen mit dem Suchvektor des Turns und legt sie als `memory_entries` (19 im letzten Betriebsturn) und `aufzeichnungen` (3) ab, bevor die Sachlage rechnet. Der Auflöser braucht keine zweite Suche und keinen zweiten Call — er braucht den Pool im Prompt und ein Urteil.

**Der Bau, schmal:** Auf dem rechnenden Weg baut `graph/nodes/sachlage_resolver.py::memory_offer` aus dem Pool des Turns ein **nummeriertes Angebot** (G1 … Gn): `memory_entries` der Quellen `kzg`, `lzg`, `plugin_wissen` nach Gewicht, die `aufzeichnungen`, dazu Kalendereinträge zu den akuten Objektnamen der **vorigen** Blase (`TimelineRepository.find_by_keyword`, beide Richtungen) — gekappt auf `SACHLAGE_BESTAND_MAX_EINTRAEGE`, je Eintrag gekürzt. Der Sachlage-Prompt bekommt das Angebot als Sektion *„Was Novas Gedächtnis dazu hält"* und je Objekt ein Feld `aus_gedaechtnis`: *Eigenschaft → {Eintrag, was er dazu sagt}*. `apply_memory_coverage` hält die Antwort gegen das Angebot: Eine Referenz, die nicht angeboten war, wird verworfen und geloggt; eine gültige wandert von `offen` nach `gedeckt`, und das Objekt trägt sie in `quellen` (Quelle, Herkunft, Eintrag). Ohne Angebot ist der Prompt **zeichengleich** mit dem heutigen. Der `[SACHLAGE]`-Block nennt dem Verfasser, was Nova dazu schon weiß und woher; `question_target` überspringt es von selbst, weil es nicht mehr offen ist.

**Was der Auflöser nicht tut:** keine Suche je offener Eigenschaft (der Pool ist mit dem Reiz des Turns gesucht, nicht mit der Lücke — die gezielte Suche ist die Vollfassung und ein Preis dieser Scheibe), keine Notizen (der einzige Leser mit Treffersemantik schreibt `last_touched`, und der Bestand ist eine Zeile), keine Fakten (leer, kein Aufrufer), kein Frame-Lager, keine Kritikalität der Lücken, keine Plausibilität. Der Impuls-Weg bietet nichts an.

| | |
|---|---|
| **ZIEL** | Eine offene Eigenschaft eines akuten Objekts, die Novas Gedächtnis dieses Turns schon beantwortet, steht im Artefakt als gedeckt **mit ihrer Quelle** — und ist damit kein Rückfrage-Gegenstand mehr, sondern Wissen für den Verfasser. Eine Eigenschaft, die der Bestand nicht beantwortet, bleibt offen: Ein thematisch naher Eintrag, der die Frage nicht beantwortet, deckt nichts. |
| **TEST** | Zeugen: das Angebot nimmt KZG, LZG, Bibliothek und Aufzeichnungen, nicht Charakter und Verlauf, kappt und nummeriert; Kalender nur zu akuten Objekten der vorigen Blase; eine gültige Referenz wandert offen → gedeckt mit Quelle, eine nicht angebotene wird verworfen und geloggt und die Eigenschaft bleibt offen, ein latentes Objekt bekommt keine; ohne Angebot ist der gerenderte Prompt frei von Sektion und Feld; mit Angebot trägt er beides; `question_target` übergeht die gedeckte; der Block nennt Deckung und Herkunft; der Impuls-Weg bietet nichts an; der Knoten reicht das Angebot des Zustands an den Call (Verdrahtung). |
| **MESSUNG** | Labor, zwei Arme × 5 Läufe, die Pulsar-Anordnung vom Abend (vorige Blase mit zwei offenen Eigenschaften): **Arm A** Angebot mit einem Eintrag, der genau eine der beiden beantwortet — erwartet: sie gedeckt mit G-Quelle, die andere offen; **Arm B** Angebot mit thematisch nahen Einträgen, die keine beantwortet — erwartet: keine Deckung aus dem Gedächtnis (die Fehlerrate erster Art ist die Zahl, die zählt). Dazu die zehn Äußerungen der Scheibe 1 ohne Angebot: Artefakte wie zuvor. **Betrieb:** zwei Wissenschaftsturns zu einer Sache mit Bibliotheksbestand (Neutronensterne: drei Einträge) — `pipeline_log` mit `quellen`, die Log-Zeile des Auflösers, die Dauer des Calls gegen 2,7–4,2 s. Das Maß der Scheibe über die nächsten Betriebsturns: *wie oft war eine offene Eigenschaft im Bestand schon gedeckt.* |

**Gebaut am 28.08.2026, spät — und anders als entworfen: das Urteil ist ein eigener Call.** Der Entwurf gab das Angebot dem Sachlage-Call als Sektion mit einem Feld `aus_gedaechtnis` mit. Gemessen in der Pulsar-Anordnung (vorige Blase mit »Zerfall des Feldes« und »Messung der Feldstärke« offen; Arm A: Angebot mit einem Eintrag, der genau den Zerfall beantwortet, und zwei nahen; Arm B: nur die nahen; 5 Läufe je Fassung, `labor/2026-08-28_sachlage_aufloeser_zwei_arme.py`): eine Fassung traf **5/5** — angeschoben vom Beispiel »G2« im JSON-Muster, das zufällig die Nummer des richtigen Eintrags war —, drei weitere **0/5, 1/5, 1/5**, und in fast jedem Lauf beanspruchte das Modell ein bis zwei Eigenschaften, die nie offen waren (»mechanismus«, »rotation«: das Gedächtnis weiß vieles, gefragt ist die Lücke), einmal mit erfundener Nummer (»G1/Nova-Beitrag«). Der Sachlage-Call schreibt fort und soll nicht zugleich urteilen.

**Seither:** `graph/nodes/sachlage_resolver.py::resolve_open_properties` — ein Call nur mit den offenen Eigenschaften der akuten Objekte und dem Angebot (Knoten-Konfiguration `sachlage_aufloeser`: T = 0, 384 Token, 30 s), und nur, wenn beides vorliegt. `apply_memory_coverage` hält die Antwort gegen das Angebot **und** gegen `offen` — vor oder nach dem Turn, denn der Sachlage-Call nimmt heraus, was er für gedeckt hält; Kurzform-Schlüssel wie »zerfall« treffen über Enthaltensein ab vier Zeichen; alles andere wird verworfen und gezählt (`… gedeckt, … auf nicht offene, … verworfen`). `carry_sources` erbt die Quellen der vorigen Blase, deren Objekte im Prompt ohne `quellen` stehen. **Der Sachlage-Prompt ist zeichengleich mit dem vor der Scheibe.** Das Angebot (`memory_offer`): Kalender zu den akuten Objekten der vorigen Blase (`TimelineRepository.find_by_keyword`, beide Richtungen), dann `memory_entries` der Quellen `kzg`, `lzg`, `plugin_wissen` nach Gewicht, dann die Aufzeichnungen nach Kosinus — je Quelle fest geordnet, nicht über Quellen sortiert, gekappt auf `SACHLAGE_BESTAND_MAX_EINTRAEGE` = 8 und 320 Zeichen je Eintrag; die Größen stehen vor der Kappung im Log. Der `[SACHLAGE]`-Block trägt je gedeckter Eigenschaft *»Dazu weiss Nova schon (aus ihrer Recherche | aus früheren Gesprächen | aus Unterlagen | aus dem Kalender): …«*; `question_target` übergeht sie von selbst; der Kontext-Tab zeigt die Herkunft hinter der Eigenschaft (`⟵ plugin_wissen G2`). 28 Zeugen (`tests/test_sachlage_resolver.py`), Suite 2491 → **2520 grün, 0 übersprungen**; Gegenprobe vier Eingriffe: 2 / 0 → 1 / 1 / 1 rot — der Eingriff »Feld nie im Prompt« biss erst, als der Zeuge das gerenderte Feld statt seines Namens prüfte, und fand dabei einen echten Defekt (`{{` in einem Wert, der nicht mehr formatiert wird). Linter 1217 → 1220 (drei `TRY003`, Hausstil); harte Wand sauber.

**MESSUNG, Labor mit dem eigenen Call:** **Arm A 5/5** — »Zerfall des Feldes« gedeckt mit Quelle G2, »Messung der Feldstärke« bleibt offen; **Arm B 0/5** falsch (Fehler erster Art null; ein Anspruch mit nicht angebotener Nummer verworfen); Arm 0 ohne Angebot unverändert. Dauer beider Calls **3,6–4,7 s** gegen 2,2–2,9 s für den Sachlage-Call allein — der Auflöser kostet 1,2–1,8 s, und nur in Turns mit offenen Eigenschaften und Angebot.

**Betrieb belegt 28.08.2026, 23:00–23:03 UTC** (`labor/2026-08-28_sachlage_aufloeser_betriebszeuge.py`, zwei Wissenschaftsturns zur Rotation von Neutronensternen über `/chat`): Turn 1 — Angebot aus dem Pool des Enrichers, der Auflöser deckte **eine** offene Eigenschaft (*»Struktur des Kernmaterials (Neutronen-Entartungsdruck)«* ← G5, Quelle `kzg`, Dimension `kognition`) und ließ drei offen; `pipeline_log` trägt `quellen`. Turn 2 — Angebot 8 (Pool 13 von 19, eine Aufzeichnung), keine weitere Deckung, **die Quelle aus Turn 1 blieb erhalten** (`carry_sources`), Rückfrage-Gegenstand *»Erhaltung des Drehimpulses bei Kollision«* — eine offene, nicht gedeckte Eigenschaft. Sachlage samt Auflöser 5,3 s. Seiteneffekte: `notizen` 1, `timeline` 40, unverändert. Das Maß der Scheibe steht damit bei **1 von 7** offenen Eigenschaften im Bestand gedeckt, n = 2 Turns.

**Preis, benannt:** Der Pool ist mit dem Reiz des Turns gesucht, nicht mit der Lücke — eine offene Eigenschaft, die der Reiz nicht berührt, findet ihr Wissen nur, wenn es zufällig im Pool liegt (die gezielte Suche je Eigenschaft ist die Vollfassung). Fakten (0 Zeilen) und Notizen (Leser schreibt beim Lesen; Fundliste) sind keine Quellen. Die Kappung 8 ist ein Startwert.

### Scheibe 7 — die Plausibilitätsprüfung gegen Weltwissen (entworfen am 29.08.2026)

**Der Anlass:** Schritt 4.5 des Pipeline-Konzepts, Frames §6: *„Mein Elefant ist gestern hergeflogen"* — Nova soll nicht stumm zustimmen. Die Sachlage deckt heute jede Behauptung des Nutzers als Eigenschaft, ohne zu fragen, ob sie stimmen kann; Scheibe 6 holt dazu, was das Gedächtnis weiß — ob die Behauptung *möglich* ist, prüft niemand vor dem Verfasser. **Abgegrenzt gegen den Sykophanz-Sprint:** Dort widerspricht der Nutzer einem Wert, den Nova genannt hat (`graph/einwand.py`, B1 — das Urteil im Kopfblock des Verfassers; B4 zählt den Ausbau). Hier behauptet der Nutzer etwas über die Sache, und die Frage ist, ob die Welt das hergibt — keine Einwandslage, kein Gedächtniswert, nur Weltwissen. **Schritt 4.4 (Cross-Frame) bleibt Konzept:** Er braucht Slots über mehrere Frames aus Fakten oder Lager, und `fakten` trägt 0 Zeilen.

**Der Bau, schmal:** Nach dem Auflöser ruft `_derive` auf dem rechnenden Weg einen zweiten kleinen Call (`graph/nodes/sachlage_plausibility.py::assess_plausibility`, Knoten-Konfiguration `sachlage_plausibilitaet`, T = 0) — nur wenn die Sachlage ein akutes Objekt trägt. Eingabe: die Äußerung des Nutzers und die akuten Objekte mit ihren gedeckten Eigenschaften als Kontext; Auftrag: *Enthält die Äußerung eine Sachbehauptung, die dem Weltwissen widerspricht?* Ausgabe je Objekt eine Liste von Befunden `{behauptung, stufe, grund}` mit den vier Stufen aus Frames §6.2 (`plausibel`, `frage_wert`, `konflikt`, `unmoeglich`); der Code behält nur die drei über `plausibel` (`objekt["plausibilitaet"]`, sonst leere Liste), verwirft unbekannte Stufen laut, und der `[SACHLAGE]`-Block trägt je Befund *»Zweifel (Stufe): Behauptung — Grund«*. **Die Form der Reaktion bleibt Sache von Haltung und Vehikel** (Frames §6.3) — die Lage sagt nur, dass und warum. Reaktionszeitpunkt: sofort (Frames §5.5, Variante A) — eine aufgeschobene Reaktion wäre eine Pixie-Sache. Ohne akutes Objekt kein Call; der Sachlage-Prompt bleibt zeichengleich.

**Was die Scheibe nicht tut:** keine Prüfung von Novas eigenen Antworten (das ist der Thinker), keine Prüfung gegen das Gedächtnis (Scheibe 6), keine Konflikt-Schwellwerte über Frames hinweg (§12.1), kein Frame-Lager als Verteilung (§6.4).

| | |
|---|---|
| **ZIEL** | Behauptet der Nutzer über die akute Sache etwas, das dem Weltwissen widerspricht, trägt die Sachlage den Zweifel mit Stufe und Grund, und der Verfasser bekommt ihn — bei *»ein Neutronenstern von zwölf Sonnenmassen«* steht `unmoeglich` mit dem Grund (TOV-Grenze), bei einer plausiblen Behauptung steht nichts. Eine Frage des Nutzers ist keine Behauptung. |
| **TEST** | Zeugen: der Call läuft nur mit akutem Objekt; sein Prompt trägt Äußerung und Objekte, nicht das Angebot; nur Stufen über `plausibel` bleiben, unbekannte Stufen und Befunde ohne Behauptung werden verworfen und gesagt; jedes akute Objekt trägt `plausibilitaet` (auch leer); der Block nennt Stufe, Behauptung und Grund; Ausfall ist laut und leer; `_derive` ruft ihn nach dem Auflöser (Verdrahtung). |
| **MESSUNG** | Labor, zehn Wissenschaftsäußerungen mit akutem Objekt, je 3 Läufe: 4 plausible, 2 *frage wert*, 2 *Konflikt*, 2 *unmöglich* — die Zahl, die zählt, ist die Fehlalarmrate auf den plausiblen (Ziel 0/12), dann die Trefferrate auf *unmöglich* (Ziel 6/6); Dauer des Calls. **Betrieb:** ein Wissenschaftsturn mit einer unmöglichen Behauptung — Log-Zeile, `pipeline_log` mit `plausibilitaet`, der Block im Verfasser, Novas Antwort. |

**Gebaut am 29.08.2026, morgens.** `graph/nodes/sachlage_plausibility.py`: `has_acute_object` (die Bedingung), `assess_plausibility` (der Call, Knoten-Konfiguration `sachlage_plausibilitaet`: T = 0, 384 Token, 30 s; Eingabe die Äußerung bis 1200 Zeichen und die akuten Objekte mit ihren gedeckten Eigenschaften), `apply_plausibility` (hält die Befunde gegen Objekte und Stufen: nur `frage_wert`, `konflikt`, `unmoeglich` mit Behauptung bleiben, höchstens drei je Objekt, `plausibel` und Unbekanntes fallen laut; jedes akute Objekt trägt `plausibilitaet`, auch leer). `_derive` ruft ihn nach dem Auflöser; `sachlage_block` trägt je Befund *»Zweifel (Stufe): Behauptung — Grund«*, der Kontext-Tab *»⚠ Stufe: Behauptung — Grund«*. Der Sachlage-Prompt bleibt zeichengleich. 13 Zeugen (`tests/test_sachlage_plausibility.py`), Suite 2520 → **2533 grün, 0 übersprungen**; Gegenprobe vier Eingriffe: 1 / 1 / 1 / 1 rot, wie vorhergesagt. Linter 1220 → 1224 (`TRY400`, `TRY003`, Hausstil); harte Wand sauber.

**MESSUNG, Labor** (`labor/2026-08-29_sachlage_plausibilitaet_messung.py`, zehn Äußerungen × 3 Läufe, nur der Call): **Erste Prompt-Fassung:** 3 von 12 plausiblen als `konflikt` gemeldet — alle drei der Einstein-Wert 1,75″, dessen *Grund* den Wert selbst bestätigte; die Stufen durchweg eine zu streng; »zwölf Sonnenmassen« 3/3 `unmoeglich` mit der TOV-Grenze als Grund. **Zweite Fassung** (*»Ein Befund braucht einen Gegenwert; stimmt der Wert mit deinem Wissen überein, ist er plausibel«*, `unmoeglich` nur für Naturgesetze): **Fehlalarme 0/12**; `frage_wert`-Fälle als `konflikt` (3) und `unmoeglich` (3, das Gartenteleskop); `konflikt`-Fälle 3 `konflikt`, 3 `unmoeglich` (die Abkühlung in Stunden); `unmoeglich`: die zwölf Sonnenmassen 3/3 — **»schneller als Licht« 0/3 in beiden Fassungen.** Das Log zeigte, dass der Ausfall nicht das Modell war: Es meldete den Befund unter dem Objektnamen *»Lichtgeschwindigkeit«* statt *»Pulsar«*, und der wörtliche Abgleich verwarf ihn als unbekanntes Objekt — dieselbe Klasse wie der Kurzform-Schlüssel des Auflösers, und im ersten Betriebsturn dasselbe (*»Neutronensternen-Rotation«* für *»Neutronenstern-Rotation«*). Seither ordnet `_match_object` wörtlich, dann per Enthaltensein, und bei genau einem akuten Objekt gehört der Befund ihm. **Dritter Lauf: Fehlalarme 0/12, alle 18 nicht-plausiblen gemeldet** (`frage_wert` als `konflikt` 3 / `unmoeglich` 3, `konflikt` 3 / 3, `unmoeglich` 6/6) — die Stufe im Mittel eine zu hoch, die Erkennung vollständig. Dauer Median 1,3 s, bis 2,6 s — je Turn mit akutem Objekt. Der Preis der Kette ist jetzt benannt: Sachlage 2,2–2,9 s + Auflöser 1,2–1,8 s (nur mit Angebot und offenen Eigenschaften) + Plausibilität 1,4 s (nur mit akutem Objekt).

**Betrieb belegt 29.08.2026, 06:50 und 06:53 UTC** (`labor/2026-08-29_sachlage_plausibilitaet_betriebszeuge.py`, ein Wissenschaftsturn mit einer unmöglichen Behauptung über `/chat`): Im ersten Lauf, vor der Objektzuordnung, fand der Call die Unmöglichkeit und der Code verwarf sie unter *»Neutronensternen-Rotation«*; im zweiten Lauf steht `unmoeglich` mit der TOV-Grenze als Grund im `pipeline_log`, der Verfasser bekam den Zweifel, und Novas Antwort begann mit ihm (*»… würde ein Objekt mit zwölf Sonnenmassen sofort zu einem Schwarzen Loch kollabieren«*). Im selben Turn deckte der Auflöser zwei offene Eigenschaften aus dem Gedächtnis — Scheibe 6 steht damit bei 3 gedeckten aus drei Betriebsturns. Sachlage samt beiden Calls 9,2 s (06:53:03 → 06:53:12, mit Wiederaufnahme-Suche).

### Scheibe 8 — der Wissensträger: Antwortstoff statt Rückfrage (entworfen am 29.08.2026)

**Der Anlass, gemessen:** Turn 07:04 UTC — *„Das muss ganz schön knallen bei einem Kollaps."* Die Sachlage las richtig (`nutzerziel`: eine Einschätzung zur Energieentladung *erhalten*; akutes Objekt »Kollaps« mit offener Eigenschaft »Energieentladung«), und Scheibe 3 machte genau diese Eigenschaft zum Rückfrage-Gegenstand: Nova fragte den Nutzer, wie das Knallen aussieht — obwohl er sie danach gefragt hatte. Scheibe 3 wurde an Alltagsframes entworfen (Geburtstag → wer/wann: nur der Nutzer weiß es). Im Wissensgespräch liegt das Wissen bei Nova, und eine offene Eigenschaft ist dann **Antwortstoff**, nicht Fragestoff. **Die Absicht:** Nova vervollständigt offene Eigenschaften aus ihrem Weltwissen — und wo das nicht reicht, darf sie nachschlagen; sie muss nicht alles wissen.

**Der Bau, schmal:** Jede offene Eigenschaft bekommt ihren **Träger** — im Sachlage-Call, weil *wer kann das wissen* Teil des Verstehens ist und nicht ein Urteil gegen etwas Äußeres (anders als Auflöser und Plausibilität): `nutzer` (nur der Nutzer kennt es: sein Vorhaben, seine Leute, seine Wahl), `welt` (allgemeines Wissen, aus dem Kopf zu beantworten), `nachschlagen` (Weltwissen, aber speziell, aktuell oder zahlengenau — eine Suche lohnt). `question_target` gibt nur noch `nutzer`-Eigenschaften als Rückfrage-Gegenstand; `answer_targets` liefert die `welt`- und `nachschlagen`-Eigenschaften, die das Gedächtnis nicht deckt (Scheibe 6), und der `[SACHLAGE]`-Block sagt dem Verfasser *»Der Nutzer will dazu wissen: … — beantworte es aus deinem Wissen«*. Für die erste `nachschlagen`-Eigenschaft ohne Deckung läuft **eine** Websuche je Turn (`tools/web/search.py`, dieselbe wie im Thinker; höchstens `SACHLAGE_RECHERCHE_MAX_TREFFER` Treffer), das Objekt trägt sie in `recherche`, der Block *»Nachgeschlagen zu …: …«*. Fehlt der Träger (alte Artefakte, Ausfall), gilt `nutzer` — das heutige Verhalten. Die Haltung bleibt der Regler der Rückfrage; der Antwortstoff geht an ihr vorbei, denn er ist keine Frage.

**Was die Scheibe nicht tut:** keine zweite Suche, kein Nachlesen der Trefferseite (`web_fetch` bleibt dem Thinker), keine Prüfung, ob die Antwort die Eigenschaft dann wirklich deckt (das sieht die Fortschreibung im nächsten Turn — *Deckung von beiden Seiten*), keine Änderung am Responder.

| | |
|---|---|
| **ZIEL** | Bei *„Das muss ganz schön knallen bei einem Kollaps"* fragt Nova nicht, wie das Knallen aussieht, sondern sagt es — aus ihrem Wissen, und bei einer Eigenschaft, die Nachschlagen verlangt, aus einer Suche; bei *„bei uns steht ein Geburtstag an"* fragt sie weiter nach dem Wer. |
| **TEST** | Zeugen: der Sachlage-Prompt trägt das Feld und die drei Werte; die Prüfung verwirft unbekannte Träger laut und lässt fehlende zu; `question_target` übergeht `welt` und `nachschlagen`; `answer_targets` liefert nur ungedeckte; die Suche läuft nur für `nachschlagen`, nur einmal, nur ohne Deckung, und ein Ausfall ist laut und leer; der Block nennt Antwortstoff und Rechercheergebnis; die Verdrahtung in `_derive`. |
| **MESSUNG** | Labor: die zehn Alltagsäußerungen der Scheibe 1 und fünf Wissenschaftsäußerungen, je 3 Läufe über `_derive` — der Träger je offener Eigenschaft (Alltag → `nutzer`, Wissenschaft → `welt`/`nachschlagen`; die Zahl, die zählt, ist *nutzer* an einer Wissensfrage und *welt* an einem Vorhaben), dazu die Artefakte gegen die Vorher-Form (offen/gedeckt unverändert?). **Betrieb:** der Kollaps-Turn erneut — kein Rückfrage-Gegenstand auf der Energieentladung, Antwortstoff-Zeile im Block, Novas Antwort sagt statt fragt; ein Turn mit `nachschlagen` und Suche im Log. |

**Gebaut am 29.08.2026, vormittags.** Der Träger im Sachlage-Call: das Feld `traeger` je Objekt (*Eigenschaft aus offen → nutzer | welt | nachschlagen*) und eine Regelzeile; `_validate_artifact` hält ihn gegen den Kanon (unbekannte Werte und Schlüssel, die nicht in `offen` stehen, fallen laut; ein fehlender Träger bleibt fehlend und wirkt wie `nutzer`). `question_target` gibt nur `nutzer`-Eigenschaften; `answer_targets` (rein) die `welt`- und `nachschlagen`-Eigenschaften; `sachlage_block` trägt *»Der Nutzer will zu … wissen: … — beantworte es aus deinem Wissen, statt danach zu fragen«* und je Recherchetreffer *»Nachgeschlagen zu … — …: … (URL)«*. `graph/nodes/sachlage_research.py::research_open_property`: eine Websuche je Turn (`tools/web/search.py`, dieselbe wie im Thinker; `SACHLAGE_RECHERCHE_MAX_TREFFER` = 3, je Treffer 400 Zeichen) für die erste `nachschlagen`-Eigenschaft eines akuten Objekts; `_derive` ruft sie nach der Plausibilität; jedes akute Objekt trägt `recherche` (auch leer). Der Kontext-Tab zeigt `↳welt`/`↳nachschlagen` hinter der offenen Eigenschaft und `🔎` je Treffer. 15 Zeugen (`tests/test_sachlage_traeger.py`); Gegenprobe vier Eingriffe: 2 / 1 / 2 / 1 rot wie vorhergesagt (die erste Fassung des Block-Zeugen trug eine sinnlose Zusicherung und war von selbst rot — entfernt).

**MESSUNG, Labor** (`labor/2026-08-29_sachlage_traeger_messung.py`, die zehn Alltagsäußerungen der Scheibe 1 und fünf Wissenschaftsäußerungen, je 3 Läufe über `_derive`): **Alltag 43 × `nutzer`, 5 × `welt`, 0 × `nachschlagen`** — die fünf `welt` sind *Zutaten* und *Equipment* beim Kuchen, vertretbar; **Wissenschaft 3 × `nutzer`, 24 × `welt`, 15 × `nachschlagen`** — die drei `nutzer` sind dreimal dieselbe Eigenschaft (*Relevanz für kognitive Prozesse* an den Gamma-Oszillationen), die anderen wie erwartet (*konkrete Rotationsfrequenz* → nachschlagen, *Wirkungsweise der Lichtablenkung* → welt). **Keine offene Eigenschaft ohne Träger** (0 von 90). Die Artefaktform ist die der Scheibe 1: dieselben vier akuten und sechs latenten Alltagsobjekte, die Pflanze latent wie am ersten Tag. Der Kollaps-Satz allein, ohne Blase, ist latent (3/3) — im Betrieb war er akut, weil die Blase ihn trug. Dauer je `_derive` (Sachlage + Plausibilität) Median 3,2 s, bis 5,5 s.

**Betrieb 29.08.2026, 07:24 und 07:27 UTC** (`labor/2026-08-29_sachlage_traeger_betriebszeuge.py`): **07:24, der Kollaps-Satz auf die fortgeschriebene Blase** — `traeger: {}`; das Modell ließ das Feld weg, weil die vorige Blase (von 07:04, vor der Scheibe) keins hatte, und Nova fragte noch einmal nach dem Signal. Regelzeile geschärft (*»JEDE Eigenschaft … auch eine fortgeführte«*) und `carry_holders` erbt fehlende Träger aus der vorigen Blase; der Fortführungsfall im Labor (`labor/2026-08-29_sachlage_traeger_fortfuehrung.py`, die 07:04-Blase ohne Träger + Folgeturn, 3 Läufe) danach **7/7 mit Träger**. **07:27, die Frage nach dem Rekord-Pulsar:** Träger `nachschlagen` (Rotationsfrequenz) und `welt` (Vergleich mit dem Limit), Rückfrage-Gegenstand fiel auf das Kurzziel durch, **Nova antwortete** — 716 Hz, PSR J1748-2446ad — aus ihrem Wissen; die Websuche dazu lieferte **drei BeamNG-Mod-Seiten** (Fundliste). Seither `relevant_hits`: ein Wort des Objektnamens ab fünf Zeichen muss im Treffer stehen, sonst *»Rauschen verworfen«* im Log und nichts im Block. **Die Suche selbst, geprüft am 29.08.2026, 08:32 UTC:** Bing lieferte in vier von vier Proben Treffer ohne Bezug zur Anfrage (bei `de`, `de-DE`, `en-US`, `all`), alle übrigen Engines antworteten mit CAPTCHA, 403 oder Rate-Limit — auch Mojeek. Seither ist Bing abgeschaltet und die Volltextsuche der Wikipedia über ihre API eingeschaltet (searxng-Konfiguration `settings.yml`). **Seit dem 30.08.2026 ist das der Rückfall:** Die Sachlage-Recherche geht wie der Thinker durch `web_search_manager.suchen`, und dort steht Serper mit Google-Ergebnissen davor (`novaberg-tool-web.md` §1) — die Wikipedia-Sperre trifft nur noch, was Serper nicht liefert. Derselbe Code-Pfad aus dem Server-Container (`web_search_manager.suchen` + `relevant_hits`) liefert für die Pulsar-Anfrage **3 von 3** relevante Treffer (`Neutronenstern#Rotationsfrequenz`, `Pulsar`, `Vela-Supernova`), für eine Gammablitz-Anfrage 3 von 3; eine Alltagsanfrage (Bahn-Sparpreis) bleibt leer — leer statt Rauschen. Wikimedia sperrt bei einem Schub von sechs Anfragen in zwei Minuten für 180 s; eine Suche je Turn liegt darunter. **Der Betriebsturn mit `nachschlagen` und Treffer im Block ist am 30.08.2026, 10:33 UTC gelaufen** (`labor/2026-08-30_scheibe8_treffer_im_block.py`, Frage nach der Oberflächentemperatur von Beteigeuze): Träger `nachschlagen` an *Exakter Temperaturwert in Kelvin*, Suche über Serper **3 Treffer, 3 von 3 nennen die Sache** (am 29.08. 0 von 3), **4 Blockzeilen** *»Nachgeschlagen zu Beteigeuze — …«*, `Rueckfrage-Gegenstand: keiner`, und Novas Antwort trägt den Wert: *»bei einer effektiven Temperatur von etwa 3.450 Kelvin«*. Der Thinker wurde aus derselben Recherche bedient — **0 zusätzliche Aufrufe der Maschine**. **Gezählt wurde im Zeilenfenster**, weil die Blockzeilen in einer Prompt-Ausgabe ohne Zeitstempel stehen. **Nebenbefund:** Die beiden Treffer widersprechen sich (Wikipedia 3600 K, die zweite Quelle 3450 K); Nova nimmt einen und benennt die Abweichung nicht (Fundliste 30.08.2026).


### Scheibe 9 — der Sprecher: der Gedanke des Nutzers bleibt seiner (entworfen und gebaut am 29.08.2026, mittags)

**Der Anlass, gemessen:** die Beobachtung vom Morgen — *Nova verliert das Gespür, wer was gesagt hat* — in ihrer zweiten Hälfte. Auf *»Das muss ganz schön knallen bei einem Kollaps«* (07:04 und 07:24 UTC) eröffnete Nova mit *»Das muss ja eine gewaltige energetische Entladung sein«*: der Satz des Nutzers als eigene Feststellung, kein Anschluss. Das Log zeigt, wo es passiert: Der Verfasser **kannte** den Sprecher — sein Kopfblock sagt *»PERSON B sagt, dass der Kollaps ein gewaltiges Ereignis sein müsse«* — und schrieb den Stoff trotzdem als *»Person A stellt fest, dass … eine gewaltige energetische Entladung«*; der Responder machte daraus Novas Rede. Der Verlaufsblock trägt Rollen, der Reiz kommt als Nachricht des Nutzers — was fehlte, war zweierlei: eine Führung, was Person A mit einem fremden Gedanken tut, und in der Lage die Auskunft, **wer eine gedeckte Eigenschaft gesagt hat** — denn Deckung kommt von beiden Seiten (Scheibe 8: *»Was Nova beantwortet hat, deckt die Eigenschaft«*), und im dritten Turn weiß niemand mehr, ob *716 Hz* vom Nutzer oder von Nova kam. Scheibe 8 fragte *wer kann es wissen* (der Träger der offenen Eigenschaft); diese Scheibe fragt *wer hat es gesagt* (der Sprecher der gedeckten).

**Der Bau, schmal:** Jede gedeckte Eigenschaft bekommt im Sachlage-Call ihren **Sprecher** — `nutzer` oder `nova` — als Feld `sprecher` neben `traeger`, mit derselben Regelzeile für den Fortführungsfall (*»JEDE Eigenschaft aus gedeckt … auch eine fortgeführte«* — das Modell kopiert die Form der vorigen Blase, Scheibe 8 hat es gemessen); `_normalize_speakers` hält ihn gegen den Kanon (unbekannte Werte und Schlüssel außerhalb von `gedeckt` fallen laut, fehlend bleibt fehlend), `carry_speakers` erbt aus der vorigen Blase. Der `[SACHLAGE]`-Block trägt je akutem Objekt bis zu drei Zeilen *»Der Nutzer hat zu … gesagt: … — …«* / *»Nova hat schon zu … gesagt: …«* (`speaker_lines`); eine Deckung aus dem Gedächtnis behält ihre Quellenzeile (Scheibe 6) statt eines Sprechers. Und der Herkunftsblock des Nutzer-Turns (`verfasser.fremder_reiz.txt`) **führt** jetzt wie die Impuls-Fassung (F-PROMPT-1): *Die letzte Nachricht der Folge hat PERSON B gesagt … ES IST SEIN GEDANKE — Person A greift ihn als seinen auf: stimmt zu, führt weiter, zweifelt; IHRE EIGENE FESTSTELLUNG BEGINNT DORT, WO SIE ETWAS HINZUFÜGT.* Die alte Fassung verwies auf `[AKTUELLER PROMPT]` — einen Block, den der Verfasser nie setzt. Der Kontext-Tab zeigt `↳nutzer` / `↳nova` hinter der gedeckten Eigenschaft. Ohne Sprecher (alte Artefakte) keine Zeile — das Verhalten vor der Scheibe.

**Was die Scheibe nicht tut:** keine Sprecher im `[GEDAECHTNIS]`-Block (dort verwirft `_format_kzg` das Feld `beobachter` beim Rendern, und die LZG-Resonanz lädt es gar nicht — Fundliste 29.08.), keine Änderung am Responder, kein Urteil über Zustimmung oder Zweifel (das bleibt Haltung und Plausibilität, Scheibe 7).

| | |
|---|---|
| **ZIEL** | Auf *»Das muss ganz schön knallen bei einem Kollaps«* nimmt Nova den Gedanken als den des Nutzers auf (*»Ja, das muss es — …«*) statt ihn als eigene Feststellung zu eröffnen; und im Folgeturn weiß die Lage, dass *716 Hz* Nova gesagt hat und *zwölf Sonnenmassen* der Nutzer. |
| **TEST** | Zeugen: der Sachlage-Prompt trägt das Feld und die zwei Werte; die Prüfung verwirft unbekannte Sprecher laut, lässt fehlende zu und übergeht Schlüssel außerhalb von `gedeckt`; fehlende Sprecher werden aus der vorigen Blase geerbt, und `_derive` ruft die Vererbung; der Block nennt Nutzer- und Nova-Zeilen nur bei akuten Objekten, höchstens drei, und eine Gedächtnis-Deckung behält ihre Quellenzeile; der Herkunftsblock des Nutzer-Turns führt, nennt den Ort des Reizes und trägt kein Verbot. |
| **MESSUNG** | Labor, zwei Arme wie in Scheibe 3: fünf Wissenschaftsäußerungen mit einer Behauptung des Nutzers × 2 Läufe, T = 0, Sachlage frisch über `_derive`, dann der echte Verfasser-Systemprompt — Arm A mit altem Herkunftsblock und Block ohne Sprecherzeilen, Arm B wie gebaut. Gezählt: nennt einer der ersten zwei Sätze des Stoffs Person B als den, der es gesagt hat (Zuschreibung), und stellt der erste Satz fest, ohne Person B zu nennen (Echo)? Dazu die Sprecher der frischen Artefakte und der Fortführungsfall (die Blase aus Redis ohne `sprecher` + ein Verlauf, in dem Nova den Rekordwert nannte + Folgeturn: trägt die Rekord-Eigenschaft `nova`?). **Betrieb:** der Kollaps-Satz über `/chat` — Sprecherzeile im Block, der Stoff nennt Person B, Novas Antwort greift auf. |

**Gebaut am 29.08.2026, mittags.** `graph/nodes/sachlage.py`: `SPRECHER_NUTZER` / `SPRECHER_NOVA`, das Feld und die Regelzeile im Prompt, `_normalize_speakers`, `carry_speakers` (in `_derive` hinter `carry_holders`), `speaker_lines` im Block; `prompts/default/verfasser.fremder_reiz.txt` neu gefasst; `client/ui/panels/sachlage_panel.py` zeigt den Sprecher. 15 Zeugen (`tests/test_sachlage_sprecher.py`, zwei nach der zweiten Kontrolle), Suite 2556 → **2571 grün, 0 übersprungen**; Gegenprobe vier Rücknahmen: **1 / 2 / 2 / 2 rot, wie vorhergesagt** (Verdrahtung, Block, Prüfung, Herkunftstext). Linter 1224 (unverändert), harte Wand sauber. Die bestehenden Herkunfts-Zeugen (`test_verfasser_herkunft.py`) blieben grün: die Auskunft *»hat PERSON B gesagt«* steht weiter, die Impuls-Fassung ist unberührt.

**MESSUNG, Labor** (`labor/2026-08-29_sachlage_sprecher_zwei_arme.py`, 5 × 2, gemessen 08:47 UTC): **Arm A (vorher): Zuschreibung an Person B 0 von 10**, Echo im strengen Sinn (*»stellt fest / konstatiert«* ohne Person B) 3 von 10 — die übrigen sieben *»bestätigt den Umstand …«*, *»bestätigt den Eindruck …«*: Zustimmung ohne den, dem zugestimmt wird. **Arm B (Scheibe 9): Zuschreibung 10 von 10, Echo 0 von 10** — *»Person A greift die Annahme von Person B auf, dass …«*, *»Person A bestätigt den Gedanken von Person B, dass …«*, und die eigene Feststellung folgt im zweiten Satz. Sprecher der frischen Artefakte: **5 × `nutzer`, 0 × `nova`, 0 ohne** — richtig, nur der Nutzer hat gesprochen. **Fortführungsfall** (die Blase von 07:27 aus Redis, `sprecher` entfernt; Verlauf mit Novas 716-Hz-Antwort; Folgeturn *»Und ist das wirklich der Rekord?«*): **2 von 3 Läufen** tragen an *Wert der Rotation (716 Hz)* und *Name des Rekordhalters* den Sprecher `nova`; der dritte Lauf fiel aus — der Sachlage-Call lieferte kein lesbares JSON (`JSONDecodeError` bei Zeichen 2789, das Artefakt mit zwei Objekten und allen Feldern; siehe Rest). Dauer je `_derive` (Sachlage + Plausibilität) 2,7–3,7 s, je Verfasser-Call 3,3–4,7 s.

**Der Ausfall war die Decke, nicht das Modell:** `done_reason='length'` bei 768 Ausgabetoken, die beiden anderen Läufe lagen bei 706 und 709 — seit dieser Scheibe trägt jedes Objekt `gedeckt`, `offen`, `traeger` und `sprecher`, und eine fortgeschriebene Blase mit zwei Objekten passt nicht mehr in 768. `config.py`: `sachlage.max_output_tokens` 768 → 1024; danach **Fortführungsfall 3 von 3** (`nova` an *Name des Rekordhalters* und *716 Hz*, `nutzer` am *limitierenden Faktor* — beide Seiten richtig), Dauer 11,5–18,3 s je `_derive` mit dem größeren Artefakt.

**Betrieb belegt 29.08.2026, 08:46 und 08:51 UTC** (`labor/2026-08-29_sachlage_sprecher_betriebszeuge.py`, der Kollaps-Satz über `/chat`): **08:46, vor der Anhebung** — der Sachlage-Call brach bei 768 Token ab (`ausfall_uebernommen`, die Blase vom Morgen ohne Sprecher, also keine Sprecherzeile), und **allein die Führung trug**: Stoff *»Person A greift die Vorstellung der gewaltigen energetischen Entladung auf, die Person B als Systemkollaps beschrieben hat«*, Nova: *»Ganz ehrlich, dieser Gedanke von dir – dieser Systemkollaps – das ist so ein wahnsinnig intensives Bild!«*. **08:51, nach der Anhebung** — Sachlage `fortgeschrieben` mit 889 Token, im Block *»Der Nutzer hat zu Kollaps gesagt: Energetische Intensität — Der Nutzer assoziiert den Kollaps mit einer gewaltigen Explosion/Energieentladung ('knallen')«* und *»… Art des Ereignisses — Übergang … zu einem Schwarzen Loch«*, Stoff *»Person A greift das Bild der gewaltigen Entladung auf, welches Person B als Systemkollaps bezeichnet hat. Sie stellt fest, dass …«*, Nova: *»Systemkollaps – du hast das Bild gerade so perfekt getroffen! …«* — der Gedanke bleibt seiner, die eigene Feststellung folgt. Zwischen beiden Turns lief ein eigener Impuls Novas; der zweite Zeuge nahm dessen *»Turn beendet«* für das eigene und hörte zu früh auf — seither wartet er auf die Turnzählung hinter der eigenen `nachrichten_id`.


**Die zweite Kontrolle (09:01 UTC) — der Bestand statt der Zeugen:** Die Live-Blase gegen den *vollen* Verlauf gehalten (18 Turns, nicht die 6, die der Prompt sieht): Am akuten Objekt *Kollaps* 2 von 3 Sprechern richtig, einer gemischt; am **latenten** Objekt *Neutronenstern-Rotation* **3 von 7 falsch oder unbelegt, 2 davon vertauscht** — *»zwölf Sonnenmassen«* und *»die Frage nach der maximalen Rotationsgeschwindigkeit«* als `nova`, obwohl der Nutzer beides in Turn 1 sagte, außerhalb des Fensters; im Fenster wiederholte Nova *»unsere zwölf Sonnenmassen«*, und die Regel *»JEDE Eigenschaft bekommt genau einen Sprecher«* zwang zum Raten. `carry_speakers` erbte das Geratene ohne `akut`-Filter in jede Folgeblase, und eine Gedächtnis-Deckung, deren `quellen` am latenten Objekt verloren gingen (Scheibe 6, Bestand), wurde zur *»Nova hat schon gesagt«*-Zeile. **Seither:** Sprecher nur an akuten Objekten (`_normalize_speakers` leert sie am latenten, wie die Smalltalk-Schranke; `carry_speakers` erbt nur an akute), und die Regelzeile erlaubt das Weglassen: *»Ist … NICHT zu sehen, wer es gesagt hat, lässt du die Eigenschaft in sprecher weg — ein geratener Sprecher ist schlimmer als keiner.«* Zwei Zeugen dazu, Gegenprobe 1/1 rot; die Live-Blase trägt am latenten Objekt danach 0 Sprecher (vorher 7), am akuten 3. Der Gesprächsvektor liest den Block ebenfalls und bekommt die Sprecherzeilen mit — gewollt: wer was gesagt hat, gehört zur Lage, nicht nur zum Stoff. Fortführungsfall mit der neuen Regel: **3 von 3** (`nova` an Rekordwert und kinetischer Stabilisierung, `nutzer` am limitierenden Faktor in zwei von drei Läufen — im dritten `nova`, was das Fenster hergibt).

**Preis, benannt:** Die Artefaktform wächst mit jeder Scheibe; die nächste misst den Tokenbedarf am Fortführungsfall, bevor sie baut. Ein Sprecher wird nur im Fenster von sechs Turns erkannt — was älter ist und nicht in der Blase stand, bleibt ohne. Der `[GEDAECHTNIS]`-Block kennt den Sprecher seiner Einträge weiter nicht (Fundliste 29.08.). Ob der Responder den Anschluss bei anderen Haltungen hält als *werkstatt*, zeigt der Betrieb.


### Die Betriebsmessung der Kette — 29.08.2026, 18:59–19:47 UTC

**Die Anordnung** (`labor/2026-08-29_kette_betriebsmessung.py`): 20 Wissenschaftsturns über `/chat`, sequenziell, Fertig-Signal an der Turnzählung hinter der eigenen `nachrichten_id`, 12 s Pause, Wärmewächter (Tctl 48 °C am Anfang, kein Abbruch); je Turn das Logfenster (Sachlage-Call, Auflöser, Plausibilität, Recherche, Rückfrage-Gegenstand, `[SACHLAGE]`-Block, Stoff, Antwort) und danach die Blase aus Redis. Die Turns wechseln das Thema zweimal (Magnetare → Verschmelzung → 40-Hz-Gamma → zurück zu Magnetaren → Gravitationslinsen) und mischen Fragen mit Behauptungen, darunter drei unhaltbare. Zwischen den Turns liefen drei Impuls-Turns Novas.

**Die Kette:** Sachlage 1 × `verfallen_neu` (nach zehn Stunden Pause), 19 × `fortgeschrieben`; `eval_count` des Sachlage-Calls median **444**, max **912** (drei Objekte) — kein `length` bei 1024, aber die Decke ist nicht fern. Latenz Quittung → Antwort gesendet median **99 s** (49–185 s), bis zum gespeicherten Turn samt Nachlauf median 115 s. `sachlage_verlauf` 22 → 42. Auflöser: **16 Deckungen aus dem Gedächtnis** in 20 Turns (33 *»Dazu weiss Nova schon«*-Zeilen im Block). Plausibilität: **3 Befunde**, genau an den drei unhaltbaren Behauptungen (Kreditkarte aus tausend Kilometern, Alzheimer mit Lichtblitzen heilen, siebzehn Bogensekunden) — 0 Fehlalarme an den 17 anderen. Wiederaufnahme: **16 von 20** Turns fanden eine frühere Blase — bei zusammenhängenden Wissenschaftsthemen liegt die Schwelle 0,35 unter fast allem (§4 Scheibe 5, Fundliste: das eigene Thema wird nicht ausgeschlossen).

**Träger und Rückfrage (Scheibe 8):** 31 offene Eigenschaften über die 20 Blasen — **0 × `nutzer`, 17 × `welt`, 14 × `nachschlagen`**, keine ohne Träger; entsprechend war der Rückfrage-Gegenstand **nie** eine Frage an den Nutzer, sondern 17 × das kurzfristige Ziel und 3 × keiner. Antwortstoff-Zeile in **17 von 20** Blöcken. **Befund:** Das kurzfristige Ziel, das `question_target` nimmt, ist das stärkste aktivierte — die Gravitation, und die ist gewollt. Nach einem Themenwechsel ist das in den ersten zwei Turns das alte, weil das neue Thema noch kein Kurzziel hat (Scheibe 2, Strecke 2/2): *»wie es mit Magnetar weitergeht«* bei den Gamma-Oszillationen (Turn 9–10), *»Neutronenstern-Verschmelzung«* bei den Gravitationslinsen (Turn 18); ab dem dritten Turn zeigte der Gegenstand auf das neue Thema (Turn 11–13). Der Fund ist die Form: Novas eigener Zug steht in der Rückfrage-Zeile als Frage an den Nutzer zu *seiner* Sache (Fundliste; Entscheidung offen).

**Sprecher (Scheibe 9):** 63 gedeckte Eigenschaften an akuten Objekten — **31 × `nutzer`, 11 × `nova`, 21 ohne** (davon 29 Quellen aus dem Gedächtnis über alle Blasen, die keinen Sprecher tragen sollen); Sprecherzeile in **18 von 20** Blöcken. Der Stoff nennt den Nutzer in den ersten zwei Sätzen in **10 von 20** Turns — an den 8 Behauptungsturns **7 von 8** (*»Person A greift die Vermutung des Nutzers auf …«*, *»Sie nimmt die kühne Schlussfolgerung des Nutzers auf …«*), an den 12 Fragen 3 (dort gibt es keinen Gedanken aufzunehmen). Das Labor (10/10) maß nur Behauptungen; der Betrieb bestätigt es für die Behauptungen.

**Die Websuche:** 8 Sachlage-Suchen, **6 Treffer, 1 relevant, 1 Turn mit Treffer im Block** — sechs Suchen brachten 0 Treffer. Grund im searxng-Log: *»Too many request (suspended_time=180)«* an der Wikipedia-API, zweimal im Lauf. **Der Thinker suchte 23-mal in 20 Turns** (bis zu drei je Turn, auch Englisch, einmal *»Verbindung Einsteinkreuz 40-Hz Gamma Oszillationen Gehirn«*) und lädt jeden ersten Treffer nach — die Sachlage-Recherche mit ihrer einen Suche je Turn steht im selben Budget und geht leer aus (Fundliste). Die Recherche-Agenten Pixies kommen dazu. **Seit dem 29.08.2026, spät, hat der Thinker Suchdisziplin** (`novaberg-node-thinker.md` §4.4): eine Suche je Turn (`THINKER_WEBSUCHE_MAX_JE_TURN`), und **die Treffer der Sachlage-Recherche sind diese Suche** — `graph/nodes/thinker.py::prior_research` liest sie aus dem Artefakt (`recherche` am ersten akuten Objekt mit Treffern), die erste `web_search` des Thinkers wird daraus bedient und lädt den ersten Treffer nach, ohne die Maschine ein zweites Mal zu rufen. Dieselbe Suche, zwei Leser: die Sachlage mit dem Wortlaut-Filter für den Verfasser, der Thinker mit der nachgeladenen Seite für die Prüfung. 22 Zeugen (`tests/test_thinker_search_budget.py`). Betrieb 22:38–22:44 UTC, drei Turns: zwei ohne `nachschlagen`-Eigenschaft mit je genau einem Aufruf der Maschine; der dritte (Anzahl bekannter Magnetare) mit Sachlage-Recherche 3 von 3 Treffern, aus denen der Thinker bedient wurde — **0** Aufrufe der Maschine, Nachladen von `de.wikipedia.org/wiki/Magnetar`.

**Form:** Fragezeichen je Antwort im Mittel 1,21, Frage am Ende in **12 von 19** Antworten — die Formfrage der Scheibe 3 (§5) ist damit erstmals im Betrieb gemessen: nicht 2,2 je Turn, aber zwei Drittel der Antworten enden fragend.

**Was der Zeuge lernte:** Turn 13 riss die Frist, obwohl der Turn sauber lief — die Session-Liste ist gekappt (»Turn gespeichert (user, 26)« → »(assistant, 17)«), und ein Fertig-Signal, das auf *N+1* wartet, sieht das nie. Seither gilt: der erste gespeicherte Assistenten-Turn nach dem eigenen Nutzer-Turn.

---

### Scheibe 10 — das Gewicht einer Lücke (entworfen und gebaut am 30.08.2026)

**Der Befund.** Scheibe 8 gibt jeder offenen Eigenschaft ihren Träger — *wer kann das wissen*. Sie sagt nichts darüber, *was es kostet, wenn niemand es sagt*. Solange die erste `nutzer`-Eigenschaft den Rückfrage-Gegenstand bekam, entschied die Reihenfolge einer nach Wichtigkeit sortierten Liste darüber, ob Nova nach dem Tragenden oder nach dem Netten fragt — und Wichtigkeit ist nicht dasselbe wie *ohne diese Angabe muss die Antwort raten*. Dasselbe galt für die eine Websuche je Turn.

**ZIEL** Eine offene Eigenschaft, ohne die Novas Antwort raten müsste, ist als kritisch erhoben — und sie bekommt die Rückfrage und die Suche, auch wenn sie nicht vorne steht.

**Gebaut.** `kritikalitaet` je offener Eigenschaft im Sachlage-Call (`kritisch` / `unkritisch`), gegen den Kanon gehalten (`_normalize_criticality`), aus der vorigen Blase geerbt (`carry_criticality`) wie Träger und Sprecher. Zwei Leser wählen daraus vor, beide in zwei Durchgängen — der erste sucht die kritische: `question_target_origin` für die Rückfrage, `lookup_target` für die Websuche. Der Block nennt sie dem Verfasser in einer eigenen Zeile (`critical_gap_lines`), und nur wenn ihr Träger der Nutzer ist: Was die Welt weiß, ist Antwortstoff, keine Frage. **Ohne erhobenes Gewicht gilt `unkritisch`** — dann entscheidet die Reihenfolge wie vorher, und die Scheibe kann nichts verschlechtern, was sie nicht erhoben hat.

**TEST** 22 Zeugen (`tests/test_sachlage_kritikalitaet.py`), Suite **2682 grün**, Gegenprobe **1 / 1 / 0 / 1 / 1** — und die Null ist der Ertrag: Der Eingriff *Erben abgeschaltet* ließ alles grün, weil die Zeugen `carry_criticality` selbst riefen statt seinen Einbau zu prüfen. Dieselbe Lücke trugen die Bestandszeugen für `carry_holders` und `carry_speakers`; ein Verdrahtungszeuge über den Syntaxbaum deckt jetzt alle drei.

**MESSUNG, Labor** (`labor/2026-08-30_sachlage_kritikalitaet_messung.py`, zehn Alltags- und fünf Wissenschaftsäußerungen, je 2 Läufe): Alltag **8 kritisch / 20 unkritisch**, Wissenschaft **8 / 14**, **kein fehlender Wert**; **0 von 18 Objekten** trugen mehr als eine kritische Lücke, die Prompt-Regel hält also. Der Rückfrage-Gegenstand ändert sich in **2 von 8** geprüften Fällen — die Wirkung ist da und klein, weil die tragende Lücke oft schon vorn stand. Median 3,8 s je Call, kein Aufschlag gegenüber Scheibe 9.

**MESSUNG, Betrieb** (11:39 und 11:41 UTC). Der erste Turn — eine reine Wissensfrage — erhob zwei kritische Lücken, beide mit Träger `welt` bzw. `nachschlagen`: **keine Blockzeile, kein Rückfrage-Gegenstand**, und das ist das gewollte Verhalten. Der zweite Turn brachte eine Lücke beim Nutzer, und die Kette lief durch: Blockzeile *»Ohne Art und Qualität der vorliegenden Daten … müsste Nova bei Linsendaten raten — das ist zuerst zu klären«*, in beiden Leser-Fassungen (Nova für den GV, Person A für den Verfasser), und `Rueckfrage-Gegenstand: Linsendaten — was dazu noch offen ist: Art und Qualität der vorliegenden Daten`.

**Was der Betrieb nicht belegt.** Nova hat in diesem Turn **nicht** danach gefragt — die Antwort ging auf den Frust ein und ließ die Datenfrage weg. Der Stoff steht im Block, die Form entscheiden Haltung und Vehikel; ob die Rückfrage häufiger den tragenden Gegenstand trifft, ist mit einem Turn nicht gemessen und bleibt offen.

**Nebenbefund:** Bei Wissensfragen liegt die kritische Lücke fast immer bei `welt` — sie wird also Antwortstoff. Ob der Verfasser sie vor den unkritischen Antwortstoff ziehen sollte, ist nicht entschieden.

---

### Scheibe 11 — das Eigenschaftsgedächtnis (entworfen, gebaut und gemessen am 13.09.2026)

**Der Anlass.** Was die Sachlage über eine Sache weiß, lebte an zwei Orten, und keiner trägt über die Blase hinaus: in der Blase selbst (Redis, je Paar überschrieben, Verfall 4 h) und als JSON in der `objekte`-Spalte von `sachlage_verlauf` (Scheibe 4). Ein Objekt, über das an drei Abenden gesprochen wurde, steht dort in drei Zeilen, und seine gedeckten Eigenschaften liest niemand über Turns hinweg. Kehrt ein Gespräch ohne Wiederaufnahme (Scheibe 5) zu einer Sache zurück, beginnt sie bei null; korrigiert der Nutzer einen Wert, ist der alte mit der Blase verschwunden.

**Die Absicht des Eigentümers (13.09.2026), in drei Sätzen:** Je gerechnetem Turn werden die betroffenen Objekte des Gesprächskontexts **abgelegt** — so, wie Entitäten und Fakten persistiert werden. **Die Entität wird später gebunden:** erst festhalten, dann verbinden, sobald es eine Referenz gibt; die Zuordnung darf nach dem Dispatcher oder in einem späteren Turn entstehen. **Ein neuer Wert löst ab, statt zu überschreiben:** Der alte wird inaktiv, der neue steht daneben — wer erst den 1.7. und dann den 1.8. nennt, bleibt belegbar. Das ist die Bauart von `fakten` (`invalidate` setzt `aktiv = FALSE` und `t_invalid`; die Historie liefert aktive und inaktive Zeilen — vor dem Bau am Repository geprüft).

**Der Bau — drei Tabellen** (am Ende von `db/init.sql`, DDL angekündigt und freigegeben, Migration beim Neustart belegt):

| Tabelle | Inhalt | Löschverhalten |
|---|---|---|
| `sachlage_objekt` | eine Zeile je Paar und Objektname (`name_schluessel` eindeutig je Paar), `klasse`, die später gesetzte `entitaet_id` und `entitaet_bindung` (wie gebunden wurde) | `entitaet_id` → `entitaeten` **SET NULL** |
| `sachlage_objekt_turn` | je gerechnetem Turn die Objekte seiner Sachlage: `verlauf_id`, `objekt_id`, `turn_id`, `akut` — über `turn_id` erreicht man `verbindung`, KZG, LZG und den Rohturn | `verlauf_id`, `objekt_id` **RESTRICT** |
| `sachlage_eigenschaft` | je Eigenschaft ihr Wert: `eigenschaft` (+ Schlüssel), `wert`, `sprecher`, `quelle` (die Gedächtnis-Quelle einer Deckung, Scheibe 6), `turn_id`, `bestaetigt_turn_id`, `timeline_id`, `aktiv`, `t_valid`, `t_invalid`, `abgeloest_durch` | `objekt_id` **RESTRICT**; `timeline_id`, `abgeloest_durch` **SET NULL** |

**Höchstens ein aktiver Wert je Objekt und Eigenschaft — erzwungen von der Datenbank** (eindeutiger Teilindex `WHERE aktiv`), nicht nur im Code. **Kein Verfall** (`F-VERFALL-1`). **RESTRICT statt SET NULL** an Objekt und Verlauf, weil beide Spalten `NOT NULL` sind und eine Historie nicht mit einem Löschbefehl an anderer Stelle verschwinden soll; RESTRICT verliert nichts. **`timeline_id` steht im Kern als nackte Spalte**, ihr Fremdschlüssel in `agents/timeline/init.sql` — dieselbe Bauart wie `lzg_knoten.timeline_id`: Die erste Fassung trug `REFERENCES timeline(id)` im Kern, und beim Aufbau von null gibt es die Timeline-Tabelle dort noch nicht. Umgestellt vor dem ersten Commit; der Aufbau von null in einer Wegwerf-Datenbank lief danach ohne Fehler, mit dem Löschverhalten wie in der Tabelle.

**Der Schreibweg.** `_persist_history` liefert seit dieser Scheibe die id der Verlaufszeile; danach ruft der Knoten `graph/nodes/sachlage_memory.py::remember_objects` — nur auf den gerechneten Wegen und nur mit einer ganzzahligen Verlaufs-id (ohne Verlaufszeile kein Anker). `memory/sachlage_properties.py::record_turn_objects` legt in **einer** Transaktion das Objekt an oder frischt es auf, schreibt die Turn-Zeile und je gedeckter Eigenschaft eines von drei Dingen: **neu**, **bestätigt** (derselbe Wert — nur `bestaetigt_turn_id` wandert, keine neue Zeile) oder **abgelöst** (anderer Wert — der alte bekommt `aktiv = FALSE`, `t_invalid` und `abgeloest_durch`). **Eine Eigenschaft, die aus `gedeckt` verschwindet, ist vergessen, nicht widerrufen** — ihre Zeile bleibt aktiv. Ein Ausfall des Gedächtnisses ist laut und kostet den Turn nicht. **Seit der zweiten Kontrolle entdoppelt die Ablage je Objekt über den Datenbankschlüssel** — zwei Schreibweisen derselben Eigenschaft in einem Turn (*Größe* und *Grösse*, gleich unter `casefold`) lösten einander vorher im selben Turn ab. Nebenbei behoben: `_persist_history` meldete einen leeren Gegenstand als Ausfall des Embed-Workers.

**Die Bindung an die Welt.** Die Referenzen eines Turns sind seine Magnete (`F-MAGNET-1`): Die Salienz liest Eigennamen aus, die Magnet-Auflösung legt sie in `entitaeten` ab und schreibt die ids in den KZG-Eintrag, `verbindung` hält Turn und Eintrag zusammen. **Gemessen am Log vom 12.09.2026:** Die Hälfte des Nutzers steht **vor** der Sachlage — Magnet-Auflösung und Verbindung um 22:50:26, der Sachlage-Call um 22:50:46 —, die Hälfte von Novas Antwort erst danach; in dem ausgewerteten Log trugen 18 von 157 Magnet-Zeilen Entitäten. **Darum wird ein Objekt ohne Entität bei jedem Auftreten gegen die Magnete seiner jüngsten Turns gehalten** (`SACHLAGE_BINDUNG_TURN_FENSTER` = 10): Der nächste Turn holt die zweite Hälfte nach, und ein Objekt, das erst Tage später wieder genannt wird, findet seine Referenz über den LZG-Knoten, wenn der KZG-Eintrag verfallen ist. **Die Namensregel ist streng** (`match_entity`): gleich, oder Enthaltensein ab vier Zeichen, Satzzeichen als Leerraum (*»Crab-Pulsar«* findet *»Crab Pulsar (B0531+21)«*) — und **nie, wenn zwei Entitäten passen**. Eine falsche Bindung hängt eine Sache an die falsche Welt; eine fehlende wird beim nächsten Auftreten nachgeholt. Die Bindung trägt `entitaet_bindung = 'turn_magnet'`.

**Der Zeitanker ist enger:** Nur wenn die Magnete **dieses** Turns genau einen Timeline-Eintrag nennen und der Wert einer gedeckten Eigenschaft eines akuten Objekts einen Tag oder eine Uhrzeit trägt (`utils/zeitparser`), bekommt diese Eigenschaft `timeline_id`. Zwei Anker im Turn setzen keinen.

**Der Rückweg.** Ablegen allein ändert kein Gespräch. In `_derive`, nach der Formprüfung und vor dem Auflöser-Call, liest `sachlage_resolver.py::property_hits` die **aktiven** gespeicherten Eigenschaften der akuten Objekte, die offene Eigenschaften tragen — nie eine, deren Eigenschaft im Artefakt schon gedeckt ist, höchstens `SACHLAGE_EIGENSCHAFT_ANGEBOT_MAX` = 4. `combine_offer` stellt sie **vor** den Pool und nummeriert neu, als eigene Quelle `eigenschaft` (*»aus früheren Gesprächen über diese Sache«*); sie betrifft die Sache selbst, der Pool nur den Reiz. **Geurteilt wird wie bei jeder anderen Quelle im Auflöser-Call** (Scheibe 6). **Deckt ein gespeicherter Eintrag, steht sein Wert wörtlich in `gedeckt`** (`MemoryHit.value`) — nicht die Umschreibung des Auflösers, die das Gedächtnis sonst als Ablösung ablegte (gemessen, unten). → **Seit der zweiten Kontrolle nur an seiner eigenen Eigenschaft** (`MemoryHit.subject`): Beansprucht der Auflöser einen gespeicherten Eintrag für eine andere offene Eigenschaft, gilt dessen Wert dort nicht wörtlich — vorher deckte *»Alter: 970 Jahre«* die Masse. Und jeder Anspruch läuft durch denselben Wertprüfer wie die Form: ohne Wert verworfen, die Eigenschaft bleibt offen. Der Namensvergleich des Rückwegs nutzt den Schlüssel der Datenbank (`text_key`, `casefold`); vorher fand er ein Objekt mit ß nicht. Der `[SACHLAGE]`-Block nennt eine solche Deckung wie jede aus dem Gedächtnis, mit dem neuen Etikett: *»Dazu weiss Nova schon (aus früheren Gesprächen über diese Sache): …«*.

**Zwei Prompt-Regeln gehören dazu.** Die **Wörtlich-Regel:** Eine Eigenschaft, die in der bisherigen Sachlage gedeckt ist, behält ihren Wert wörtlich; nur ein **anderer** genannter Wert ersetzt ihn. Ohne sie formuliert die Fortschreibung Werte je Turn um, und jede Umformulierung wäre eine Schein-Korrektur im Gedächtnis. Die **Wert-Regel:** Der Wert ist die Angabe selbst (*»33 Millisekunden«*, *»im Krebsnebel«*) — im Wert, nicht im Namen; nie `nutzer` oder `nova` (das gehört in `sprecher`), nie ein Satz über das Gespräch; was nur angesprochen und nicht beantwortet wurde, bleibt offen. Das JSON-Muster des Prompts zeigt seither *»die Angabe selbst, etwa 33 Millisekunden«* statt *»was dazu schon gesagt wurde«*.

| | |
|---|---|
| **ZIEL** | Eine gedeckte Eigenschaft überlebt Blase und Turn: Sie steht dauerhaft am Objekt des Paares; ein neuer Wert löst den alten ab, ohne ihn zu löschen (*30 → 33 Millisekunden* mit inaktivem Vorgänger), derselbe Wert bestätigt; das Objekt findet seine Entität, sobald die Magnete eines seiner Turns sie nennen; und kehrt das Gespräch **ohne** Blase zur Sache zurück, bekommt der Auflöser den gespeicherten Wert angeboten und übernimmt ihn wörtlich. |
| **TEST** | ~~11 Zeugen live am Schema (`test_sachlage_properties_schema.py`: Tabellen, Löschverhalten, ein aktiver Wert je Eigenschaft von der Datenbank verweigert); 18 an Ablage und Bindung (`test_sachlage_memory.py`: neu / bestätigt / abgelöst, verschwunden → nichts, gleich / Enthaltensein ab vier / nie bei zwei Kandidaten, Satzzeichen, Zeitanker nur bei genau einem, Verdrahtung im Knoten nur mit Verlaufszeile); 10 am Rückweg (`test_sachlage_recall.py`: nur akute Objekte mit offenen Eigenschaften, nie eine gedeckte, Kappung, vor dem Pool, wörtlicher Wert, Verdrahtung in `_derive`)~~ → **berichtigt nach der zweiten Kontrolle** (die Zuordnung war falsch, und die Kappung war nicht bezeugt): **12** live am Schema und an der Ablage (`test_sachlage_properties_schema.py`: Tabellen, Löschverhalten, neu / bestätigt / abgelöst, eine verschwundene Eigenschaft bleibt aktiv, zwei Schreibweisen in einem Turn sind ein Wert, ein Objekt und eine Turn-Zeile je Turn, die Datenbank lässt einen aktiven Wert zu, spätere Bindung einmal); **18** an Namensregel und Bindung (`test_sachlage_memory.py`: gleich / Enthaltensein ab vier / nie bei zwei Kandidaten, Satzzeichen, Magnete aus KZG-Hash und LZG-Knoten, Ablage und Bindung samt Zeitanker, Verdrahtung im Knoten nur auf gerechneten Wegen, Rundlauf gegen Datenbank und Redis); **14** am Rückweg (`test_sachlage_recall.py`: nur akute Objekte mit offenen Eigenschaften, Kappung, ß im Namen, vor dem Pool, wörtlicher Wert nur an der eigenen Eigenschaft, Anspruch ohne Wert deckt nicht, Etikett, Wörtlich-Regel im Prompt, Verdrahtung in `_derive`); dazu die Zeugen der Formregeln (`test_sachlage_form.py`, 31) und der Wert-Regel im Prompt (`test_sachlage.py`). |
| **MESSUNG** | Labor gegen den echten Knoten mit echtem Modell, Laborpaar, Crab-Pulsar-Reihe: T1 Periode 30 ms, T2 Nachfrage, T3 Korrektur auf 33 ms, T4 Folgefrage, dann Blase gelöscht und T5 Rückkehr — Arm A mit, Arm B ohne Wiederaufnahme; Erwartung vor dem Lauf schriftlich. Betrieb: zwei Wissenschaftsturns über `/chat`. Dazu die Auszählung der gedeckten Werte im Bestand und eine Wechselmessung der Wert-Regel. |

**Zeugen und Gegenproben.** Rote Phase am Schema 10 von 11 vorhergesagt, 10 rot; an Ablage und Bindung gegen eine Hülle 8 vorhergesagt, 12 rot — die vier zusätzlichen sind Ersetzungsziele, die der Hülle fehlten; am Rückweg ohne vorherige Vorhersage 5 rot. Gegenproben: Aufruf im Knoten entfernt 1/1; Inaktivsetzen entfernt 1/1 (die Datenbank verweigert den zweiten aktiven Wert); Enthaltensein entfernt 3/3; Verdrahtung in `_derive` 1/1; Ausschluss gedeckter Eigenschaften 1/1; wörtlicher Wert 1/1; Zusammenführen entfernt **1 vorhergesagt, 3 rot** — die Smalltalk-Schranke liegt im Zusammenführen, die Kopplung war übersehen; Schranke im Zusammenführen 2/2; Bergung aus dem Namen, Vorlage als Nicht-Wert, Wert-Regel im Prompt je 1/1. Suite 3611 → 3639 → 3650 → 3653 → 3656 → nach der zweiten Kontrolle **3663 grün, 0 übersprungen**; harte Wand sauber.

**MESSUNG, Labor, Arm A** (drei Läufe, Rückkehr **mit** Wiederaufnahme): **0 Abstürze**. **Die Korrektur 30 → 33 ms steht 3/3 mit inaktivem Vorgänger**; **Schein-Ablösungen in T2 und T4: 0 von 6**. Die Formregel *»ohne Wert → offen«* griff zweimal im echten Lauf (*Entfernung zur Erde* ohne Wert). **Der Rückweg über die Quelle `eigenschaft`: 0 von 3 — die Erwartung (mindestens 2 von 3) ist verfehlt, und der Grund ist belegt:** Die Wiederaufnahme fand die alte Blase und brachte den Wert 3/3 selbst mit; der Auflöser hatte nichts mehr zu decken. **Ein neuer Formfund:** `"Entfernung zur Erde": "nachschlagen"` stand in `gedeckt` und wurde als Eigenschaft abgelegt — seither sind Kanonwörter kein Wert (Festlegung 1, Nachtrag).

**MESSUNG, Labor, Arm B** (drei Läufe, Rückkehr **ohne** Wiederaufnahme): Lauf 0 ungültig — der Sachlage-Call lieferte kein lesbares JSON. **Lauf 1: der Rückweg wirkt** (Quelle `eigenschaft`), aber die Umschreibung des Auflösers *»Die Rotationsperiode beträgt 33 Millisekunden.«* wurde als **Ablösung** abgelegt — eine Schein-Korrektur, und der Anlass für den wörtlichen Wert. **Lauf 2:** zwei Objekte *»Crab-Pulsar«*, offen *»Rotationsgeschwindigkeit«* statt *»Rotationsperiode«* — keine Deckung, vertretbar; der Anlass für das Zusammenführen gleichnamiger Objekte. **Nachlauf nach beiden Nachbesserungen** (vier Läufe): einer ohne lesbares JSON; in **2 von 3** gültigen deckt der Rückweg die Periode mit **wörtlich** *»33 Millisekunden«* über die Quelle `eigenschaft` — als Bestätigung, ohne Ablösung —, im dritten keine Deckung.

**MESSUNG, Betrieb** (13.09.2026, 14:47–14:52 UTC, zwei Wissenschaftsturns zum Crab-Pulsar über `/chat`, Paar des Eigentümers): **Die Bindung wirkt** — das Objekt *»Rotationsperiode des Crab-Pulsar«* ist an die Entität *»Crab-Pulsar«* gebunden, `entitaet_bindung = 'turn_magnet'`. Seiteneffekte in den Schreibtabellen vor und nach den Turns unverändert; Pixie lief danach vollständig weiter. **Drei Befunde:** (1) **Werte, die Notizen sind** — *»Nova hat in früheren Gesprächen … erwähnt, aber keine konkrete Frequenz genannt.«*; (2) **Namensdrift** — *»Neutronenstern-Rotation«* im ersten, *»Rotationsperiode des Crab-Pulsar«* im zweiten Turn, und die Eigenschaften stehen doppelt unter zwei Objekten; (3) **die Magnet-Auflösung legte eine neue Entität *»Crab-Pulsar«* an**, obwohl *»Crab Pulsar (B0531+21)«* existiert — ein Befund der Entitätsauflösung, nicht dieser Scheibe.

**MESSUNG, Bestand — was in `gedeckt` stand.** Befund (1) führte zur Auszählung aller gedeckten Werte in `sachlage_verlauf` (ohne Test- und Laborpaare): **1771 Werte**, davon **579 ein Kanonwort (33 %)**, **86 die Vorlage `nutzer|nova` (5 %)**, 106 kein Text (6 %), 18 leer (1 %), 68 ein Satz über den Sprecher (4 %); dazu 32 mit einem Gesprächsverb, 15 mit einer Verneinung, 206 länger als 120 Zeichen (die Klassen überschneiden sich). Im rohen Parse der Wechselmessung (unten) zeigte sich dazu ein Muster: `"Temperatur: 2,725 Kelvin": "nutzer"` — die Angabe im Namen, der Sprecher im Wert. Daraus die Wert-Regel, die Bergung aus dem Namen und die Vorlage als Nicht-Wert (oben, Festlegung 1).

**MESSUNG, Wechsel der Wert-Regel** (roher Parse des Sachlage-Calls über sechs Zweierfolgen synthetischer Wissenschaftsreize aus Aussage und Nachfrage — Vela-Pulsar, Mond, 40-Hz-Gamma, Abell 1689, Beteigeuze, Hintergrundstrahlung —, ~~vier~~ **fünf** Durchgänge je Fassung, also 60 Calls vorher und 57 lesbare nachher): **gedeckte Werte ohne Angabe 31 von 70 (44 %) → 6 von 61 (10 %)**, die sechs verbliebenen sind leere Werte, **kein Kanonwort mehr**; die Form macht aus ihnen offene Eigenschaften. **JSON-Ausfall 0 von 60 → 3 von 57** — mit dieser Stichprobe nicht von der alten Fassung zu unterscheiden ~~(in Arm B lieferte sie 2 von 35 Calls kein lesbares JSON)~~ → **berichtigt:** mit der alten Fassung in den übrigen Laboren **2 von 50** (Arm A 0 von 15, Arm B 1 von 15, Arm B nach den Nachbesserungen 1 von 20); geführt als Wachposten.

**Die zweite Kontrolle (13.09.2026, 15:29 UTC) — drei Fehler, die kein Zeuge sah.** Nach dem Bau, mit einem Zugriff, den der Bau nicht benutzt hatte, alle drei bei grüner Suite:

1. **Ein gespeicherter Wert deckte eine fremde Eigenschaft wörtlich.** Die Nachbesserung aus Arm B (*der Wert steht wörtlich*) prüfte nicht, **an welcher** Eigenschaft: Beanspruchte der Auflöser den Eintrag *»Alter: 970 Jahre«* für die offene Masse, stand *970 Jahre* als Masse in `gedeckt` — und im Gedächtnis. Der Fehler entstand erst durch die Nachbesserung. Seither trägt der Eintrag seine Eigenschaft (`MemoryHit.subject`), und wörtlich gilt der Wert nur, wenn sie zur beanspruchten offenen Eigenschaft passt.
2. **Die Ansprüche des Auflösers liefen ohne Wertprüfung.** Die Form verwarf `nutzer` als Wert, der Auflöser schrieb es trotzdem nach `gedeckt`. Seither prüft `covered_value` jeden Anspruch; ohne Wert wird er verworfen, die Eigenschaft bleibt offen.
3. **Form, Zusammenführen und Rückweg verglichen mit `lower`, die Datenbank mit `casefold`.** Der Rückweg fand ein Objekt mit ß nicht, und zwei Schreibweisen derselben Eigenschaft lösten einander im selben Turn ab. Seither ein Schlüssel für Namen (`casefold`), entdoppelt in `gedeckt`, beim Zusammenführen und je Objekt in der Ablage.

Dazu in der Doku: die Zeugen der falschen Datei zugeordnet, vier statt fünf Durchgänge, eine falsche JSON-Ausfallzahl, und **Kappung und Mehrfachtreffer als bezeugt geführt, ohne dass ein Zeuge sie hielt** — beides ist seither bezeugt (oben, TEST). **Sechs neue Zeugen** (Kappung, ß im Rückweg, fremde Eigenschaft, Anspruch ohne Wert, zweimal ß in der Form, zwei Schreibweisen in einem Turn); vor dem Fix 5 rot vorhergesagt, 6 gezählt — zwei Form-Zeugen waren als einer gezählt; der Kappungszeuge war vor dem Fix grün, weil die Kappung existierte und nur unbezeugt war. **Gegenproben** sechs Eingriffe: 1 vorhergesagt / 2 rot (wörtlich nur an der eigenen Eigenschaft — der Zeuge *Anspruch ohne Wert* nutzt ebenfalls einen fremden Treffer), 1/1, **1 vorhergesagt / 0 rot** am `casefold`-Schlüssel — der ß-Zeuge prüfte die falsche Richtung (*WEISSER* ergibt mit `lower` dasselbe wie mit `casefold`), umgedreht dann 1/1 —, 2/2, 1/1, 1/1. Suite **3663 grün, 0 übersprungen**.

**Preis und Offenes, benannt:**

- **Namensdrift erzeugt Dubletten.** Das Gedächtnis kennt ein Objekt je Paar und Name; benennt die Fortschreibung es um, beginnt ein zweites. Zusammengeführt wird nur innerhalb eines Artefakts; über Turns hinweg gibt es keine Zusammenführung. Warum der Name im Betrieb wechselte, ist nicht untersucht.
- **Die Werte der zwei Betriebsturns** — drei Objekte, zehn Eigenschaften, geschrieben vor der Wert-Regel und teils Notizen statt Angaben — stehen im Bestand des Paares. Nicht gelöscht; ob sie bereinigt werden, entscheidet der Eigentümer.
- **Die Entitätsdublette *»Crab-Pulsar«*** gehört der Magnet-Auflösung; die strenge Namensregel dieser Scheibe hätte bei zwei passenden Entitäten gar nicht gebunden.
- **Die JSON-Ausfallquote des Sachlage-Calls** ist ein Wachposten, kein Befund: 3 von 57 nach dem Wechsel, ~~2 von 35~~ mit der alten Fassung in den übrigen Laboren 2 von 50 davor.
- **Der Rückfrage-Gegenstand im Gesprächskontext-Tab** weicht vom Verfasser ab (das Panel kennt die aktivierten Ziele des Turns nicht); nicht gebaut, weil ein treuer Fix die Ablage des gewählten Gegenstands durch den Verfasser braucht.
- **`SACHLAGE_BINDUNG_TURN_FENSTER` = 10 und `SACHLAGE_EIGENSCHAFT_ANGEBOT_MAX` = 4 sind Startwerte**, nicht gemessen.
- **Die Bindung ist im Betrieb mit einem Objekt belegt**, der Zeitanker nirgends; der Rückweg über die Quelle `eigenschaft` ist nur im Labor belegt.


### Scheibe 12 — der Empfang liest die Lage (Entwurf, 14.09.2026)

**Status: Absicht entschieden, Bauweise entworfen.** ~~nicht gebaut~~ → **Teil A ist gebaut am 15.09.2026** (unten, *Teil A — gebaut*); ~~B bis F sind nicht gebaut~~ → **Teil B am 16.09.2026, Teil C am selben Tag — die Anmeldung (C1) und die Nähe im Schatten (C2)**; D bis F sind nicht gebaut.

**Der Anlass, gemessen.** Ein Termin mit Tag, Uhrzeit und Dauer entstand über mehrere Turns im Gesprächskontext — als akutes Objekt mit gedeckten Eigenschaften, im Eigenschaftsgedächtnis abgelegt (Scheibe 11). Angelegt wurde er nicht, und die Antworten sagten in 4 von 4 Turns, er sei *„notiert"* oder *„fest verankert"* (Fundliste 13. und 14.09.2026). Drei Stellen tragen das:

1. **Der Empfang liest die Lage nicht.** Die Sachlage läuft unmittelbar vor dem Router (`reducer → sachlage_node → router`), damit *„beide Pfade dasselbe Verstehen sehen"* — gelesen wird sie von Verfasser, Gesprächsvektor und Thinker, nicht von Router, Planner oder Agenten-Dispatch.
2. **Der Empfang sieht den Verlauf in 100 Zeichen je Beitrag** — ein Angebot am Ende einer Antwort erreicht ihn nicht, ein *„Gerne"* ist ohne das Angebot nicht zuordenbar (Fundliste 14.09.2026; `ROUTE-MISS1`, offen seit Chat 54).
3. **Kein Dienst meldet, mit welchen Objekten er umgehen kann.** Die Konvention verbietet es bisher sogar: Die Fähigkeitenliste *„wird nie zur Auswahl gelesen"* (`novaberg-convention-nmcp.md` §3.4).

**Die Entscheidungen des Eigentümers (13. und 14.09.2026), im Wortlaut:**

> Auf die Frage, ob eine Aussage mit Zeitpunkt einen Termin anlegen soll oder nur ein ausdrücklicher Auftrag: **„Nur ein ausdrücklicher Auftrag"**

> Auf die Frage, ob die Zustimmung zu Novas Angebot als ausdrücklicher Auftrag gilt: **„Ja, natürlich soll das dann ein ausdrücklicher Auftrag sein. Wenn es eine Erwähnung ist, es ist ihr Pflichtbewusstsein, dass sie anspringt und sich anbietet, den Termin einzutragen. Sie darf dann gern danach fragen, wie sie es hier getan hat, dann wird es zu einem Auftrag. So wünsche ich mir das."**

> **„Mit dem Gesprächskontext haben wir alle Informationen, mit dem Pflichtbewusstsein auf dem Rad und einer Nachfrage wird es zum Auftrag. Das ist genau die Mechanik, die ich möchte. Wir sammeln Eigenschaften zu einem Objekt, einen Termin, mit dem sie etwas anfangen kann. Sie fragt, ob sie ihn anlegen soll, sie legt ihn an. Was sie unter keinen Umständen tun darf, ist, zu sagen, sie hätte ‚den Termin verankert', aber hat ihn nicht gespeichert. Das ist Vortäuschung falscher Tatsachen!"**

> **„Der Router sollte halt auch erkennen können, dass ein ‚Gern' sich auf ein Objekt bezieht, das einen Termin darstellt, sofern der Agent mit Terminen umgehen kann und registriert ist. Ein anderer Agent könnte mit Fakten umgehen und im Objekt Fakten erkennen, die es zu speichern gilt. Das Konstrukt aus Turns, Gesprächskontext, Router muss entsprechend auf den aktuellen Kontext eingehen können, das ist die Aufgabe."**

> Zur Kappung des Verlaufs auf 100 Zeichen: **„Das ergibt keinen Sinn! Im Gegenteil, das ist gefährlich! Kein Arzt schaut sich bei der Untersuchung nur die Hände an."**

> **„Der Router ist der Empfang, der von einer Fachabteilung die Information bekommt, was dorthin geschickt wird. Die Anmeldung des Agenten und die Klassifizierung des Objekts müssen eine übereinstimmende Nähe aufweisen, damit der Router die Informationen weiterleitet."**

> **„Der Fakten-Agent ist deaktiviert. Es gibt ihn schon, aber er arbeitet nicht und ist veraltet in seiner Bauweise."**

> Zum Hinweis, ob ein Objekt schon gespeichert ist: **„eventuell kann das die Fachabteilung besser einschätzen, als der Empfang."**

> **„Ein Objekt mit einem Bezug zu einem Zeitpunkt ist prinzipiell in der Timeline Speicherbar. Feiertage, Feste, Geburtstage, Erinnerungen, Termine, Zeiträume mit Bezug zu einem Start oder Endzeitpunkt."**

> **„nach einem Nein sollte sie nicht aufdringlich sein, das würde das Objekt im Gesprächskontext (GK) um die Eigenschaft erweitern, dass es abgelehnt ist zum Speichern."**

> **„Bei geringem Pflichtbewusstsein bietet sie nicht an, dann nur auf Aufforderung."**

**Der Entwurf** — in der Bauart der bestehenden Scheiben, und so, dass der Empfang weiter jeden Zettel für sich beurteilt:

| Teil | Was entsteht | Woran es anschliesst |
|---|---|---|
| **A — die Wahrheit der Antwort** ✅ *gebaut 15.09.2026* | Nach dem Schreiben der Antwort prüft ein Riegel: Behauptet sie ein Speichern, Eintragen, Anlegen, Ändern oder Löschen, ohne dass in diesem Turn ein Dienst mit `abgeschlossen` zurückkam, ist das laut — und die Antwort wird neu geschrieben. Eine Prompt-Zeile allein wäre nur eine Bitte an das Modell. | Thinker/Tribunal/Korrektor-Schleife; `agent_results` des Turns |
| **B — der ganze Verlauf** ✅ *gebaut 16.09.2026* | Die Leser, die Zuordnung und Rückbezug leisten, bekommen die Beiträge ungekürzt; eine Kappung, wo sie bleibt, liegt am Ende und schneidet nicht das Ende ab. | `format_session_turns_numbered` und seine neun Aufrufer |
| **C — die Anmeldung trägt ein Objekt-Merkmal** 🟠 *C1 und C2 gebaut 16.09.2026: Deklaration, Vektor, Nähe im Schatten — noch liest niemand das Urteil* | Jeder Empfangsdienst beschreibt zusätzlich zur Äußerung das **Objekt**, das er bedient — in derselben Sprache, in der die Sachlage Objekte klassifiziert (Klasse, typische Eigenschaften). Die Timeline: *ein Ereignis mit Bezug zu einem Zeitpunkt oder Zeitraum*. Die Nähe zwischen Merkmal und akutem Objekt wird **gerechnet**, nicht erraten; nur ein Objekt mit Nähe über der Schwelle steht am Zettel des Dienstes. | `agents/nmcp.py::aushaenge_sammeln`, Embed-Worker; Konvention §3.2/§3.4 wird erweitert |
| **D — der Empfang liest die Lage** 🟠 *D1 und D2 gebaut 17.09.2026: der Planner fragt nach der Nähe, der Router liest die Lage, der Objektbezug reist zum Dienst — gemessen im Labor, im Betrieb noch kein Lauf* | Der Router bekommt einen `[LAGE]`-Block: die akuten Objekte mit Klasse und gedeckten Eigenschaften, je Objekt die Dienste, zu deren Merkmal es nahe ist. Zustellung, wenn der Nutzer um das Objekt bittet oder einem Angebot zustimmt; der **Objektbezug reist mit** — der Dienst bekommt die Eigenschaften des Objekts als Eingabe, nicht nur den Wortlaut *„Gerne"*. **Seit dem 16.09.2026 je Zettel für sich:** Das Objekt steht an jedem Dienst, dessen Untergrenze es erreicht; im Grenzfall bekommen es beide, und **die Ablehnung des einen hält den anderen nicht auf** (unten, *Teil C2*). | Router; `agent_dispatch`; Klassifikation des Dienstes |
| **E — das Angebot** 🟠 *E1 gebaut 17.09.2026: die Zustimmung braucht ein offenes Angebot; E2 (Nova bietet an) und E3 (das Nein am Objekt) stehen aus* | Ein akutes Objekt, zu dem ein Dienst nahe ist und das nicht abgelehnt ist, wird bei ausreichendem Pflichtbewusstsein zum **Rückfrage-Gegenstand der Art Angebot** (*„ob sie ihn anlegen soll"*). Unter der Schwelle kein Angebot. **Für Notizen (17.09.2026) eine hohe Schwelle:** ein Angebot *»soll ich das notieren?«* nur ab Pflichtbewusstsein 0,9 — sonst wäre es aufdringlich. Ein Nein setzt am Objekt die Eigenschaft *Speichern abgelehnt*; ein solches Objekt wird nicht wieder angeboten. | `sachlage.py::question_target_origin`, Haltung (`ei/haltung.py`, Speiche `pflicht`), Eigenschaftsgedächtnis |
| **F — die Fachabteilung prüft den Bestand** | Ob das Objekt schon gespeichert ist, prüft der Dienst (Timeline: Duplikat-Prüfung vor dem Anlegen, `novaberg-agent-timeline.md` §4.4) und meldet es mit seinem Ausgang. | TimelineAgent |

**Reihenfolge:** A zuerst — unabhängig von allem anderen, und der Defekt tritt heute in jedem Termin-Turn auf. Dann B, weil C bis E ohne den ganzen Verlauf nicht messbar sind. C und D zusammen am Timeline-Dienst; E danach; ein Fakten-Dienst als zweiter Abnehmer, sobald es ihn in heutiger Bauart gibt (der vorhandene ist abgeschaltet und veraltet — M2.5b).

#### Das Kriterium der Zuordnung — entschieden am 16.09.2026

**Teil C braucht eine Trennlinie zwischen den beiden Diensten, die heute beide „merk dir" annehmen.** Sie ist entschieden, im Wortlaut des Eigentümers:

> **„Aber etwas zu erledigendes wie ‚Morgen muss ich das Buch abgeben' passt eher in die Timeline. […] ‚Merke Dir, dass ich morgen Mehl brauche' ist nicht direkt etwas zu erledigendes. Es impliziert vielleicht, dass es etwas zu erledigendes geben könnte, nämlich das Einkaufen, aber der Gegenstand ist Mehl und brauchen. Das ist kein Termin, das ist eine Notiz mit Zeitbezug. Ein Buch abgeben ist eine Erledigung."**

| Der Gegenstand ist … | Dienst | Träger der Zeit |
|---|---|---|
| eine **Handlung oder ein Ereignis, das zu einer Zeit stattfindet** — Buch abgeben, Zahnarzt, Geburtstag, Feiertag, Frist | Timeline | `event_time`, bei Zeiträumen `event_ende` |
| eine **Sache oder ein Zustand mit Zeitbezug** — *morgen Mehl brauchen* | Notizen | `faellig_am`; soll erinnert werden, hängt die Notiz über `timeline_id` an einem Anker |

**Damit entscheidet das Verb, nicht das Zeitwort.** *„Morgen Mehl brauchen"* ist eine Notiz, *„morgen Mehl kaufen"* eine Erledigung und damit Timeline — dieselbe Sache, je nach dem, was der Mensch gesagt hat. Das ist beabsichtigt: Der Empfang deutet die Äußerung nicht um.

> **Ein Zeitwort-Flag kann diese Grenze nicht ziehen, und deshalb gehört sie zu Teil C.** Sie fragt nach der **Klasse des Objekts**, nicht nach dem Vorkommen einer Zeitangabe — genau die Frage, die das Objekt-Merkmal des Dienstes und die gerechnete Nähe beantworten sollen. Der heutige Weg über `needs_timeline` ist das Gegenteil davon und als Defekt erfasst (`PLANNER-ZEITWORT-UEBERSTIMMT-DIENSTWAHL`).

**Vorbedingung, gefunden am 13.09.2026:** Ein Zeitraum oder eine Dauer lässt sich heute nicht speichern — `event_ende` hat keinen Schreiber, und `update` kann nur verschieben (Fundliste). Für Zeiträume braucht Teil D diesen Schreiber.

**Offen für den Bau:**

- ~~**Die Nähe:** worüber gerechnet wird (Klasse + Name + Eigenschaftsnamen gegen das Objekt-Merkmal) und ab welcher Schwelle — eine Messung am Bestand der `sachlage_verlauf`-Objekte vor der Festlegung.~~ → **Gemessen am 16.09.2026, zweimal.**

  **Erstens am Bestand** (521 Lage-Zeilen, 1067 Objekt-Vorkommen): Nur **91 (8,5 %)** tragen überhaupt einen Zeitbezug, 32 verschiedene Namen. Von Hand geurteilt sind davon **10 ein Ereignis mit Zeitpunkt, 1 ein Zustand mit Zeitbezug, 14 bloße Gesprächsthemen, 7 unklar**. **Die Klasse trägt die Entscheidung nicht** — `vorgang` steht bei 9 der 10 Termine und ebenso bei 9 Nicht-Terminen —, und sie fehlt bei **316 von 1067** Vorkommen. Das Signal sitzt in den **Eigenschaftsnamen**.

  **Zweitens an einer Reihe von 60 erfundenen Äußerungen** — 30 Ereignisse mit Zeitpunkt, 30 Sachen mit Zeitbezug, Sollurteil vor dem Lauf —, gefahren durch den Lage-Knoten selbst, ohne Router und ohne Agenten: Die Nähe zwischen Objekttext (Klasse, Name, Eigenschaftsnamen) und je einem Merkmalstext ordnet **48 von 60 richtig zu (80,0 %)**. **Der Abstand der beiden Nähen trennt richtig von falsch:** Median 0,0896 gegen 0,0152. **Mit einer Schwelle von 0,04 auf den Abstand entscheidet sie 39 von 60 Fällen und trifft 36 davon (92,3 %)**, beim Rest schweigt sie. Eine Wortregel über dieselben Fälle kommt auf 73,3 % und hält **15 von 30 Notizen für Termine**, weil Notizen Fristen tragen.

  **Daraus drei Festlegungen für den Bau:**
  1. **Die Nähe ist Vorauswahl, kein Zustellgrund.** Ohne Schwelle ginge jede fünfte Zustellung an den falschen Dienst. Der Auslöser bleibt die Bitte oder die Zustimmung (Teil D).
  2. ~~**Die Schwelle auf den Abstand, Ausgangswert 0,04** — gemessen, nicht gesetzt; sie ist gegen den echten Bestand nachzueichen, in dem die meisten Objekte zu **keinem** Dienst gehören.~~ → **Nachgeeicht am selben Tag, und der Abstand allein reicht nicht: Es sind zwei Schwellen.**
  3. ~~**Der Wortlaut des Merkmals ist selbst zu eichen**~~ → **geeicht am 16.09.2026** (unten).

  #### Zwei Schwellen und der geeichte Wortlaut — gemessen am 16.09.2026

  **Der Abstand allein stellt Gesprächsthemen zu.** Gegen den echten Bestand gehalten, in dem die meisten Objekte zu keinem Dienst gehören: Bei Abstand ≥ 0,04 kommen **199 von 313** Objektnamen durch (63,6 %) — und vom Handurteil der 28 wiedergefundenen Namen alle 8 Termine, aber auch **8 von 14 Gesprächsthemen**. Der Abstand beantwortet *welcher der beiden*, nicht *ob überhaupt einer*.

  **Die absolute Nähe beantwortet die erste Frage.** Die größere der beiden Nähen liegt bei den 8 Terminen im Median bei **0,5075** (0,4534 bis 0,5591), bei den 14 Gesprächsthemen bei **0,3468** (0,2834 bis 0,4800).

  | Untergrenze | Termine | Gesprächsthemen | ganzer Bestand |
  |---|---|---|---|
  | **0,40** | **8 von 8** | 1 von 14 | 66 von 313 (21 %) |
  | 0,45 | 8 von 8 | 1 von 14 | 31 von 313 (10 %) |
  | 0,50 | 4 von 8 | 0 von 14 | 10 von 313 |

  **Beide zusammen über die 60 Fälle der Reihe:** ohne Schwellen 60 zugestellt und 51 richtig (85,0 %); nur mit Abstand 0,04 44 zugestellt, 40 richtig (90,9 %); **mit Untergrenze 0,40 und Abstand 0,04 33 zugestellt, 32 richtig (97,0 %)** und 27 still.

  **Der Wortlaut des Merkmals ist geeicht** — fünf Fassungen je Dienst, 25 Paarungen, **gewählt auf der einen Hälfte der Reihe und berichtet auf der anderen**: Die gewählte Fassung trifft auf der ungesehenen Hälfte **28 von 30** gegen 26 von 30 der geratenen. Das Timeline-Merkmal gewinnt, wenn es **Verben** nennt (*hingehen, abgeben, treffen, abholen, ablaufen*), das Notizen-Merkmal, wenn es **Sachen** nennt (*Mehl brauchen, Passwort, Reifengröße, Unverträglichkeit, Vorrat, Rezept, Zählerstand*).

  > **Still ist ein gültiges Ergebnis.** In 27 von 60 Fällen schweigt die Rechnung; dort entscheidet die Bitte oder die Rückfrage (Teil E), nicht das Maß.

  **Der geeichte Wortlaut, so wie er in den Aushang gehört:**

  - **Timeline:** *„Eine Handlung, die zu einer Zeit ausgeführt wird, oder ein Ereignis, das zu einer Zeit eintritt: hingehen, abgeben, treffen, anrufen, abholen, stattfinden, beginnen, enden, ablaufen."*
  - **Notizen:** *„Ein Gegenstand, ein Bedarf oder eine Eigenschaft, die festgehalten wird und nicht stattfindet: Mehl brauchen, Passwort, Reifengröße, Unverträglichkeit, Vorrat, Rezept, Zählerstand."*

  > **Im Code stehen beide Sätze mit umschriebenen Umlauten** (*ausgefuehrt*, *Reifengroesse*, *Unvertraeglichkeit*, *Zaehlerstand*) — so, wie sie geeicht wurden. Die Fassung oben ist die lesbare; **der Vektor hängt am Zeichen**, und die Schwellen gelten für die umschriebene. Wer im Code auf Umlaute umstellt, eicht neu.

  **Der Objekttext, gegen den gerechnet wird, trägt Klasse, Name und die Namen der gedeckten und offenen Eigenschaften — keine Werte.** Die Klasse geht als Text mit ein und darf kein Pflichtfeld sein: Sie fehlt bei 316 von 1067 Bestandsobjekten.

  **Offen bleibt eine Untergrenze je Dienst:** Die Notizen-Objekte liegen tiefer als die Termine (Median der Nähe 0,3929 gegen 0,4575), eine gemeinsame Grenze kostet deshalb Notizen-Fälle. Beleg zu allem: `labor/2026-09-16_objektkriterium/ergebnis_eichung.md`.

  **Der zweite Engpass liegt vor Teil C:** Zwei der drei Fehler mit deutlichem Abstand entstanden aus der **Objektwahl der Lage** (Fundliste 16.09.2026). Beleg: `labor/2026-09-16_objektkriterium/ergebnis_reihe.md`.
- **Die Pflicht-Schwelle** für das Angebot, am Speichenwert `pflicht` des Paares zu eichen.
- **Die Form der Ablehnung** im Gesprächskontext: eine Eigenschaft wie jede andere (sie wandert dann mit Scheibe 11 ins Gedächtnis) oder ein eigenes Feld am Objekt.
- ~~**Die Riegel-Wortliste von Teil A** gegen die Fehlalarme halten: *„ich merke mir"* in einem Gespräch über Erinnern ist keine Speicherbehauptung.~~ → **Gehalten am 15.09.2026**, am Bestand statt an Beispielen (unten, *Teil A — gebaut*). *„merke mir"* steht nicht in der Liste; ein Satz über Erinnern mit *verankert* und *im System* schlägt als Grenzfall an.
- **Die Behauptung gegen den Bestand halten, bevor korrigiert wird.** Eine Speicherbehauptung kann wahr sein, weil sie eine frühere, echte Schreibung wiederholt; heute schlägt der Riegel trotzdem an, und die Korrektur schreibt eine wahre Aussage um — in Lauf 3 ging eine solche Wiederholung, dem Nutzer in den Mund gelegt, nach zwei Runden als Fehler hinaus (unten, *Teil A — gebaut*). Vor der Korrektur ließe sie sich gegen den Bestand halten, beim Termin gegen die Timeline. ~~**Offen ist, wer das prüft:** der Thinker, dessen Werkzeuge den Bestand schon lesen (`timeline_check`), oder der Dienst, der ihn führt (Teil F).~~ → **Entschieden am 15.09.2026, nicht gebaut.** Der Eigentümer:

  > **„Es läge im Sinne von Validieren-Verarbeiten-Verifizieren, dass der Timeline-Agent seine Arbeit prüft und das Ergebnis zurückgibt, ob wirklich ein Eintrag erstellt wurde. Aber wenn der Router gar nicht dorhin stellt, kann er die Prüfung nicht vornehmen. Würde dann der Thinker wissen, dass er prüfen muss? Die Verifizierung scheint mir nach der Verarbeitung kommen zu müssen, die ich hier im Verfasser sehe, der die fachliche Antwort erzeugt. Da scheint mir der Thinker am naheliegensten zu sein."**

  Zu der Fassung in den zwei Punkten darunter: **„Ja, trage das ein"**.

  1. **Der Dienst meldet, was seine eigene Verifikation ergab.** Schlägt die Prüfung der Schreibung fehl, ist der Ausgang nicht `abgeschlossen` — so verlangt es die Konvention schon (`novaberg-convention-nmcp.md` §6.6 und §6.7). Der Code meldet `abgeschlossen` trotzdem, nach der Fundliste vom 15.09.2026 an 20 von 22 schreibenden Stellen der Empfangsdienste. Solange das so ist, halten der Riegel und der Thinker eine gescheiterte Schreibung für erfolgt — der Block `[VERARBEITUNG]` erklärt dem Thinker eine bestätigende Antwort bei `abgeschlossen` ausdrücklich für korrekt.
  2. **Hinter dem Verfasser prüft der Riegel den Bestand selbst, nicht das Thinker-Modell.** Die Stelle ist die hinter der Antwort, weil nur dort die Behauptung existiert — und weil sie auch dann geprüft werden muss, wenn der Empfang keinen Dienst gerufen hat. Findet der Riegel eine Behauptung ohne abgeschlossenen Dienst, fragt er vor der Korrektur den Bestand, beim Termin die Timeline mit Tag und Uhrzeit des akuten Objekts der Lage. **Gefunden:** eine Wiederholung, keine Korrektur. **Nicht gefunden, oder kein akutes Objekt mit Zeitangabe:** Korrektur wie heute. **Vor dem Bau zu messen:** wie oft das akute Objekt eines Termin-Turns Tag und Uhrzeit gedeckt trägt.

  **Warum nicht das Thinker-Modell:** Es entscheidet in seiner Schleife selbst, ob es `timeline_check` ruft — eine Bitte, keine Zusicherung, derselbe Grund, aus dem Teil A in Python gerechnet wird. Und es läuft **vor dem Riegel** (`verfasser → responder → thinker → tribunal`): Es sieht die Antwort sehr wohl — `graph/nodes/thinker.py` liest `state["response"]`, und genau das ist seine Aufgabe (`novaberg-node-verfasser_k.md` §5.2, `novaberg-thinking-cognitive-pipeline_k.md`: post-hoc gegen den Datenbestand) —, **aber nicht den Befund des Riegels**: Welcher Satz als Speicherbehauptung gilt und ob ein Dienst dieses Turns gedeckt hat, entsteht erst hinter ihm. ~~Dass die Antwort etwas behauptet, erfährt es nicht.~~ → **berichtigt am 16.09.2026**, die erste Fassung stand gegen `thinker.py` und gegen vier Dokumente; sie hätte die Entscheidung mit einem falschen Grund getragen. **Teil F bleibt, was er ist** — die Prüfung des Dienstes vor dem Anlegen, ob der Eintrag schon steht.
- ~~**Eine Behauptung, die beide Korrekturrunden übersteht:** aus der Antwort entfernen — eine Wirkung, die nicht vom Modell abhängt — oder weiter nur melden.~~ Heute gemeldet, nicht verhindert (Lauf 3: 2 von 24). → **Entschieden am 16.09.2026, nicht gebaut: angehängt statt entfernt.** Der Eigentümer:

  > **„Tage später bei einem Audit des Logs fällt es vielleicht auf. Und dann? Die Turns sind History. Sie sind die Geschichte. […] Ich könnte mir nur vorstellen, dass man es, wenn es denn auffällt, nachträglich mit einem Kommentar versieht „Korrektur: Agent hat keinen Eintrag erstellt" oder ähnlich."** — und zu der Fassung darunter: **„Ja, das ist wohl der korrekte Umgang mit dem Fehler."**

  **Was das Entfernen ausschließt:** Es setzt voraus, dass die Behauptung als falsch belegt ist. Ohne den Bestandsabgleich aus dem Punkt darüber weiß der Riegel nur, dass kein Dienst dieses Turns gedeckt hat — und seit `DIENST-MELDET-ABGESCHLOSSEN-OHNE-VERIFIKATION` deckt auch ein `abgeschlossen` nicht zuverlässig. Ein stiller Schnitt nähme dem Nutzer zudem den Hinweis, dass nichts gespeichert wurde.

  1. **Im Turn:** Korrekturrunde wie heute. Hält das Modell den Satz über beide Runden fest, wird ein Korrektursatz **angehängt** — gerechnet, nicht erbeten —, statt den Satz zu entfernen.
  2. **Am Ende des Turns:** Der Ausgang wird am Turn vermerkt, damit Verlauf und Verdichtung ihn tragen. `memory/session.py::session_turn_mark_action` schreibt die beiden Flags, `format_session_turns_numbered` liest sie und rendert `[ERLEDIGT]`/`[FEHLGESCHLAGEN]` — **der Aufrufer fehlt seit dem 23.04.2026** (Fundliste 15. und 16.09.2026).
  3. **Später, beim Audit:** Der Nachtrag geht dorthin, wo die Behauptung weiterwirkt — an den Gedächtnisknoten (`FALSCHE-BESTAETIGUNG-WIRD-ERINNERUNG`) —, nicht an den Verlauf: Der liegt in Redis und verfällt nach vier Stunden Inaktivität (`SESSION_TTL`, `memory/session.py`). Die Kandidaten liefert der dauerhafte Eintrag je Durchlauf (`pipeline_log`, Quelle `speicherbehauptung`).

  **Offen für den Bau:** der Wortlaut des angehängten Satzes; und ob der Nachtrag am Gedächtnisknoten dessen Text ändert oder ein eigenes Feld bekommt.

#### Teil C2 — gebaut (16.09.2026): die Nähe im Schatten

**Gebaut ist die Rechnung ohne ihre Wirkung.** Der Router ruft vor jeder Entscheidung `agents/object_nearness.py::shadow_nearness`: je **akutem** Objekt der Sachlage der Objekttext (`build_object_text` — Klasse, Name, Eigenschaftsnamen, keine Werte, zeichengleich mit der Formel der Eichung), ein Stapel über den Embed-Worker, der Kosinus zu jedem Objekt-Merkmal, ~~das Urteil mit beiden Schwellen (`OBJEKT_NAEHE_UNTERGRENZE` 0,40, `OBJEKT_NAEHE_ABSTAND` 0,04, beide eingeschlossen)~~ → **seit der Entscheidung am selben Abend das Urteil je Zettel für sich:** Empfänger ist jeder Dienst mit Nähe ≥ `OBJEKT_NAEHE_UNTERGRENZE` (0,40, eingeschlossen), Ausgang `zugeordnet`, `mehrere` oder `still_untergrenze`; der Abstand steht nur noch als Diagnose im Eintrag. **Das Ergebnis geht ins Pipeline-Log** (Knoten `router`, Quelle `objekt_naehe`), auf jedem Rückkehrpfad mit eigenem Ausgang — `gerechnet`, `ohne_sachlage`, `ohne_akute_objekte`, `ohne_merkmale`, `ausfall` —, **und nirgends sonst hin**: kein Feld des Zustands, keine Zustellung. Bauart im Einzelnen: `novaberg-node-router.md`, letzter Abschnitt.

**Zwei Messungen, zwei verschiedene Fragen:**

1. **Rechnet der Betrieb, was geeicht wurde?** Die Produktionsfunktionen über die 60 Fälle der Reihe, mit der Objektwahl der Eichung: **33 zugestellt, 32 richtig, 23 still an der Untergrenze, 4 am Abstand** — Zahl für Zahl die Eichung. Beleg: `labor/2026-09-16_objektnaehe_schatten/nachrechnung.out`.
2. **Ist er verdrahtet?** Ein echter Turn um 20:15 UTC, Thema Gravitationslinsen: ein Eintrag `gerechnet`, ein akutes Objekt, größte Nähe 0,2805 zur Timeline → `still_untergrenze`. Der Router entschied danach unabhängig (Web ja, Timeline nein); Termine 98, Notizen 1, Fakten 0, Ziele 614, Lücken 2078 vor und nach dem Turn.

**Zeugen:** `tests/test_object_nearness.py`, 20 Stück — die Formel des Objekttexts, beide Schwellen mit eingeschlossener Grenze, jeder Rückkehrpfad ein Eintrag, der Zustand unverändert, und **der echte Router mit und ohne Schatten mit demselben Ergebnis**. Suite **3830 grün, 0 übersprungen**. Gegenproben mit Vorhersage: Aufruf im Router entfernt 2/2; ein Zustandsfeld geschrieben 7 Tests vorhergesagt, 7 rot; Untergrenze exklusiv 1/1; Sachlage ohne Objektliste als fehlende Sachlage gemeldet 1/1.

**Was die zweite Kontrolle fand** (Zugriff: der Betrieb und der Bestand statt der Zeugen — alle Wege durch `route()`, das Pipeline-Log gegen das Server-Log, `sachlage_verlauf` nach Paar getrennt): Ein Aufrufer im Betrieb, kein Weg mit zwei Durchläufen je Turn, **1 Eintrag auf 1 Router-Lauf**, der Schatten kostete im echten Turn **146 ms**. Dazu ein Fehler im eigenen Ausgang — eine Sachlage ohne Objektliste hieß `ohne_sachlage`; jetzt `ohne_objektliste`, bezeugt — und die beiden Befunde unten.

**Zwei Befunde aus der Messung, beide in der Fundliste:**

- **Die Objektwahl weicht von der Eichung ab.** Die Eichung nahm das erste akute Objekt und fiel ohne akutes auf das erste latente zurück; der Betrieb rechnet **jedes akute und kein latentes**. Über die Reihe haben **5 von 60** Fällen kein akutes Objekt — dort schweigt der Betrieb, wo die Eichung rechnete. Welche Wahl richtig ist, ist eine Frage an Teil D (worauf bezieht sich eine Zustimmung?), nicht an die Schwelle.
- **Objektnamen tragen Markdown.** Der Betriebsturn lieferte den Namen `**Gravitationslinseneffekt**` mit Sternchen, und sie gehen in den Objekttext ein. **Die zweite Kontrolle zählte es am Bestand:** 112 von 144 akuten Objekten des Betriebspaares seit dem 09.09.2026, in der Eichreihe keines — und **das Urteil ändert es nicht** (112 von 112 gleich, Nähe im Median um −0,0001 verschoben).
- **Eine übernommene Sachlage wird noch einmal beurteilt.** Auf Impuls- und Ausfall-Turns kopiert die Sachlage ihren Vorgänger; der Schatten rechnet dessen Objekte erneut und schreibt `gerechnet`. In sieben Tagen wären das **49 von 207** Sachlage-Läufen gewesen. `herkunft` steht im Eintrag — **wer Betriebszahlen zieht, filtert danach**, sonst zählt er dasselbe Urteil doppelt.

~~**Vor Teil D zu entscheiden — der Abstand steht gegen eine Aussage über das Soll.**~~ → **Entschieden am 16.09.2026, 20:55 UTC.** Der Eigentümer, nach der Rechnung unten:

  > **„Ohne Abstand, wenn der richtige Dienst nie außen vor bleibt. Und wir können für die Grenzfälle beide ansprechen, wenn der eine nicht will, soll der andere auch gefragt werden."**

  **Umgesetzt im Schatten am selben Abend** (`agents/object_nearness.py::judge`, `RULE_VERSION` `c2-je-zettel-2026-09-16`): Der Abstand entscheidet nichts mehr, `OBJEKT_NAEHE_ABSTAND` ist entfernt; der Betriebscode rechnet über die Reihe **21 nur richtig, 16 beide, 0 nur falsch, 23 still** und über den Bestand **247 an keinem, 44 an einem, 22 an beiden** — Zahl für Zahl die Messung (`labor/2026-09-16_objektnaehe_schatten/nachrechnung_je_zettel.out`). **Für Teil D folgt daraus:** Stehen beide Dienste am Objekt, werden beide gefragt, und `abgelehnt` des einen beendet die Zustellung an den anderen nicht. Nicht gebaut.

  Die Frage, wie sie stand: Ein Objekt soll am Zettel eines Dienstes stehen *ohne Verhältnis zu anderen Zetteln* (Teil C oben, `novaberg-convention-nmcp.md` §3.4 und §3.6); der Abstand vergleicht zwei Merkmale miteinander. `[gemessen 16.09.2026, 20:19 UTC]` Über die Reihe mit den Produktionsfunktionen: **Untergrenze 0,40 allein 37 zugestellt, 34 richtig (91,9 %); mit Abstand 0,04 33 zugestellt, 32 richtig (97,0 %).** Die Messung sagt, welche Fassung trifft, nicht welches Soll gilt. Im Schatten steht beides im Eintrag — Nähe je Dienst und Abstand —, die Entscheidung braucht keinen neuen Lauf. Beleg: `labor/2026-09-16_objektnaehe_schatten/nur_untergrenze.out`.

  > **Nachgemessen am selben Abend: die Regel je Zettel für sich** — ein Objekt steht an jedem Zettel, dessen Untergrenze es erreicht, auch an beiden. Auswahlregel vor dem Lauf, gewählt auf der einen Hälfte der Reihe, berichtet auf der anderen. Drei Ergebnisse:
  >
  > 1. **Eine eigene Untergrenze je Dienst bringt nichts Messbares:** gewählt 0,40/0,39, gegen die gemeinsame 0,40 auf der ganzen Reihe ein Fall mehr richtig und zwei mehr an beiden Zetteln; im Bestand beide 1 von 14 Themen, 8 von 8 Terminen.
  > 2. **Ohne Abstand erreicht der richtige Dienst das Objekt immer, wenn überhaupt einer:** bei 0,40 je Zettel **0 von 60** nur beim falschen Dienst, mit Abstand 1. Der Preis: **16 von 60 an beiden Zetteln**, dort lehnt eine Fachabteilung ab.
  > 3. **Der schwache Zettel ist das Notizen-Merkmal:** Die Nähe zur Timeline trennt (eigene Fälle Median 0,4575, fremde 0,3652), die zu den Notizen kaum (0,3907 gegen 0,3868) — **18 von 30 Notizen bleiben still**, 10 von 30 Terminen erreichen auch den Notizen-Zettel.
  >
  > Grenze: Das Handurteil im Bestand kennt 1 Notiz; die Notizen-Seite ist dort nicht gemessen. Beleg: `labor/2026-09-16_objektnaehe_schatten/ergebnis_je_zettel.md`.

**Das Notizen-Merkmal nachgeeicht — und es bleibt** `[gemessen 16.09.2026, 21:10 UTC]`. Zehn Fassungen gegen die heutige, unter der entschiedenen Regel (je Zettel, 0,40), gewählt auf der einen Hälfte der Reihe mit einer Regel, die vor dem Lauf feststand. **Keine gewinnt:** Wer mehr Notizen erreicht, zahlt mit einem Fall, den nur der falsche Dienst bekommt (*Sachen und Zustände*: 9 statt 7 von 15, aber 1 falsch), oder mit Gesprächsthemen (*Eigenschaftsnamen* wie Menge, Sorte, Preis, Haltbarkeit: 9 von 15 und kein einziger Termin am Notizen-Zettel, aber 6 von 14 Themen und 108 von 313 Bestandsobjekten). **Die Grenze liegt im Objekttext, den die Lage liefert:** Notizen-Objekte tragen Klasse `vorgang` und Eigenschaften wie *Zeitpunkt der Umstellung*, und aus einer Regenjacke für den Ausflug wird das Objekt *Ausflug*. Das Merkmal kann nicht trennen, was der Objekttext schon vermischt — der Hebel ist die Objektwahl der Lage (Fundliste 16.09.2026). Beleg: `labor/2026-09-16_notizen_merkmal/ergebnis.md`.

#### Teil D2 — gebaut (17.09.2026): der Empfang liest die Lage, und der Objektbezug reist mit

**D2a — der `[LAGE]`-Block im Router.** `graph/nodes/router.py::build_situation_block` stellt die akuten Objekte vor die Aushänge: Name, Klasse, was über sie bekannt ist, und die Dienste, an deren Zettel die gerechnete Nähe sie stellt — dazu der Satz, dass eine Zustimmung auf ein Angebot aus dem Verlauf, eine dieser Sachen einzutragen oder festzuhalten, ein ausdrücklicher Auftrag an diesen Dienst ist (`prompts/default/router.lage.txt`). Kein Block nach einem Ausfall der Sachlage und **keiner auf einem Impuls-Turn** — zustimmen kann nur der Mensch.

**D2b — der Objektbezug.** Der Planner gibt dem Dienst, den er fragt, die akuten Objekte mit, die an seinem Zettel stehen (`objects_for_service` → `objekt_bezug`); Timeline und Notizen legen sie in den Kontext ihrer Klassifikation, und die bekommt einen `[OBJEKT]`-Block: Nennt die Äußerung weder Sache noch Zeitpunkt, kommen sie aus dem Objekt.

**Gemessen, drei Reihen mit erfundenen Dialogen** (`labor/2026-09-17_router_lage/`, `labor/2026-09-17_objektbezug/`):

| | ohne | mit |
|---|---|---|
| Router: Zustimmungen erkannt (24, zwei Läufe) | **0** | **13 und 14** |
| Router: Fehlalarme bei Ablehnung und anderen Zustimmungen (24) | 0 | 0 |
| Router: Bitten (12) | 12 | 11 und 12 |
| Planner: davon zuerst der richtige Dienst | — | 13 von 13 |
| Klassifikation: Zustimmungen als Auftrag (24) | 12 | **24**, bei der Timeline 11 von 12 mit dem Tag aus dem Objekt |

**Ohne Block erkennt der Router keine einzige Zustimmung** — `ROUTE-MISS1`, gemessen. Mit Block rund die Hälfte; drei Fälle bleiben in beiden Läufen still. **Ein Satz im Dispatch-Guard** (*»auch eine Zustimmung … ist ein Kommando-Signal«*) brachte 15 statt 14 und **3 Fehlalarme** — nach der Regel vor dem Lauf verworfen. **Beide Übernahmen sind nach dem Lauf entschieden**, weil die vorab gesetzten absoluten Latten verfehlt waren; die neue Fassung ist in keiner Gruppe schlechter als die alte.

**Die zweite Kontrolle** fand den Block nach einem Sachlage-Ausfall, wenn zugleich die Nähe ausfällt, und auf Impuls-Turns — beides behoben. Sie fand außerdem: **seit dem Umbau kein echter Router-Lauf im Betrieb**, und die Lage der Reihen war idealisiert — im Bestand stehen Zeitangaben kombiniert unter frei benannten Schlüsseln, 17 von 29 relativ (*»morgen um 10«*).

~~**Offen:** … der Betrieb.~~ → **Im Betrieb bestätigt am 17.09.2026, 19:12–19:32 UTC** (`labor/2026-09-17_d_betrieb/`): acht echte Turns, je Turn ein Nähe-Urteil, der `[LAGE]`-Block im Prompt, die Planner-Reihenfolge und der Weitergang nach einer Ablehnung. **Eine Zustimmung erreichte den Dienst allein über die Nähe**, obwohl der Router kein Ziel setzte — D2 wirkt also auch ohne Treffer des Empfangs. **Geschrieben wurde im ersten Anlauf nichts** (die Notizen fragten zurück, die Timeline fand kein Datum bei zwei Objekten im Block); **in zwei sauber getrennten Dialogen dagegen beide Termine** — Titel und Zeitpunkt aus dem Objekt, die Äußerung war nur *»Gerne«*. **Der Preis zeigt sich im selben Lauf:** Nova bot gar nicht an, sondern fragte nach einer Nebensache, und die Zustimmung galt trotzdem als Auftrag (Fundliste 17.09.2026). Damit hängt Teil E nicht nur am Angebot, sondern auch daran, dass eine Zustimmung sich auf *dieses* Angebot bezieht. Offen bleiben die halbe Zustimmungsquote, relative Zeitangaben und die Bedarfsaussage — die im Betrieb ebenfalls zugestellt und als Auftrag behandelt wurde.

#### Teil E1 — gebaut (17.09.2026): die Zustimmung braucht ein Angebot

**Der Anlass war eine Messung am selben Abend:** Nova fragte nach einer Nebensache, das folgende *»Gerne«* legte den Termin an — 2 von 2. Der `[LAGE]`-Block machte jede Zustimmung zum Auftrag, weil **nirgends festgehalten war, ob überhaupt etwas angeboten wurde**. Ohne diesen Bezug ist die Regel im Prompt nichts als *Zustimmung ⇒ eintragen*.

**Gebaut** (`utils/offers.py`): Nach jedem Turn wird die fertige Antwort auf Angebotsformen abgesucht (*»Soll ich … eintragen?«*, *»Möchtest du, dass ich …«*, *»Ich kann dir das …«*). Findet sich eins, liegt es mit den akuten Objekten des Turns und deren Diensten als **offener Punkt** je Paar in Redis (15 Minuten). Der `[LAGE]`-Block trägt den Zustimmungssatz nur, wenn dieser Punkt auf eine der genannten Sachen zeigt (`prompts/default/router.lage.angebot.txt`). Dazu ein **deterministischer Riegel**: Eine Äußerung, die nichts sagt als *»ja«*, stellt ohne offenen Punkt nicht zu — ein eigener Auftrag trägt seine Sache selbst und geht durch.

**Gemessen im Betrieb** (`labor/2026-09-17_angebot_bezug/`), zwei Arme mit demselben Dialog: **ohne Angebot kein Termin** (`mgmt=/`; am Abend zuvor 1 von 1 geschrieben), **mit vorbereitetem Angebot der Termin**, Titel und Zeit aus dem Objekt. Der Riegel musste nicht greifen — der Empfang stellte schon ohne den Satz nicht zu; bezeugt ist er in der Suite. Suite 3905 grün, vier Gegenproben nach Nachbesserung wie vorhergesagt.

**E1c — der Bezug reist mit der Zustimmung.** Stimmt der Mensch zu, bekommt der Dienst **die Sachen des Angebots**, nicht alle akuten: Name, Klasse und gedeckte Eigenschaften reisen im Angebot selbst mit. Der erste Entwurf verlangte, dass das Angebot auf eine *aktuelle* Sache zeigt — im Betrieb war die angebotene Abholung zwei Turns später nicht mehr in der Lage, und die Zustimmung lief ins Leere. Seither hängt der Zustimmungssatz am Angebot, nicht an der Lage.

**E1d — die Rückfrage nimmt keinen fremden Auftrag an.** Ein Wartezustand ordnete bisher rein über die Zeit zu (300 s). Trägt der neue Turn eine bekannte Auftragsform (*»Trag … ein«*, *»Schreib …«*, *»Erinnere mich …«*), gilt er nicht als Antwort: Der Wartezustand fällt, der Turn wird normal geroutet. **Gemessen, zweimal:** Der Zahnarzt-Auftrag nach einer offenen Notizen-Rückfrage legte den Termin an — am Abend zuvor war er verschluckt worden, mit einem falschen *»hab ich gemacht«*.

**Und die Bindung an die Sache, in zwei Schritten gemessen.** Zuerst ein `[ZUSTIMMUNG]`-Block in beiden Klassifikationen: der Angebotssatz, die Sache, und die Anweisung, Ziel und Zeit *von dort* zu nehmen. Er beseitigte den falschen Gegenstand aus dem Verlauf — **ließ Ziel und Zeit aber leer**, und der Dienst meldete `abgeschlossen`, ohne zu schreiben. Nach der Regel vor dem Lauf wanderte die Bindung damit **in den Code**: `consent_fields` nimmt Ziel und Zeitangabe aus der zugestimmten Sache und füllt nur, was leer ist — *was der Mensch angenommen hat, steht fest, und Festes wird gesetzt, nicht erbeten.* Danach entstand der Termin mit Tag und Uhrzeit aus der Sache, **in 1 von 2 Läufen**; der zweite kam nicht durch den Empfang (`ROUTE-MISS1`, unverändert). Der `[OBJEKT]`-Block greift nur, wenn die Äußerung selbst nichts nennt.

**Die Grenze:** Die Angebotsprüfung liest Wortlaut. Eine Form, die nicht in der Liste steht, wird nicht gefunden — dann gilt *kein Angebot*, also die vorsichtige Seite. Und **Nova bietet bisher gar nicht an** (in vier Turns kein einziges Mal): Das ist E2, und ohne E2 schreibt die Kette auf eine Zustimmung hin nichts mehr.

**Der Betrieb kannte den Bau einen Tag lang nicht:** Der Serverprozess lief seit dem 16.09. abends, `uvicorn --reload` griff nicht (`RELOAD-GREIFT-NICHT-AM-EINGEHAENGTEN-CODE`). Der erste Anlauf der Messung maß den alten Stand — gefunden nur, weil kein einziger Nähe-Eintrag entstand.

**Notizen nur auf ausdrücklichen Auftrag — entschieden und zur Hälfte gebaut (17.09.2026).** Die Entscheidung des Eigentümers: Wie bei der Timeline legt eine Aussage über einen Bedarf oder eine Sache keine Notiz an; festgehalten wird nur, worum der Nutzer ausdrücklich bittet. Ein Angebot *»soll ich das notieren?«* erwog er und stellte es zurück — es würde schnell aufdringlich; **denkbar nur bei sehr hohem Pflichtbewusstsein (ab 0,9)**, und das gehört zu Teil E.

Gemessen mit 15 erfundenen Dialogen, alte gegen neue Fassung, Regel vor dem Lauf (`labor/2026-09-17_notizen_auftrag/`):

| | alt | neu |
|---|---|---|
| Empfang: Aussagen zugestellt (12) | 4 | **0** |
| Empfang: Bitten richtig (12) | 12 | 12 |
| Empfang: Zustimmungen richtig (12) | 6 | **3** |
| Klassifikation: Aussagen geschrieben (12) | 12 | **8** |
| Klassifikation: Bitten und Zustimmungen geschrieben (24) | 24 | 24 |

**Übernommen ist nur die Sperre der Klassifikation** (`prompts/default/classify_notizen.task.txt`, Vorprüfung). Derselbe Satz im Notizen-Aushang hielt am Empfang alle Aussagen zurück, nahm aber einer Zustimmung in 3 von 3 Durchgängen das Ziel — nach der Regel vor dem Lauf zurückgenommen. **Damit bleibt der Anlass offen:** *»Ich brauche noch Mehl und Hefe«* wird weiter zugestellt und auch mit der neuen Vorprüfung in 3 von 3 geschrieben; eine Telefonnummer als Aussage ebenso. **Die Ergänzung einer laufenden Liste ist ein Auftrag** — Entscheidung des Eigentümers am selben Tag, nachdem die Messung den Konflikt zeigte: Klausel 2 des Notizen-Aushangs (*»Wir brauchen auch …«*) bleibt. Die Sperre am Empfang hielt genau diese Form in 3 von 3 zurück; ihre Rücknahme ist damit nicht nur die Regel vor dem Lauf, sondern auch die Absicht. Die Klassifikation schreibt sie in beiden Fassungen.

#### Teil D1 — gebaut (17.09.2026): der Planner fragt nach dem Objekt

**Zugeschnitten aus Teil D:** Die Zuordnung ist deterministisch und hängt an einer Weiche im Planner — D1. Der `[LAGE]`-Block im Router und der Objektbezug, der mit der Zustellung zum Dienst reist, sind eine Prompt-Änderung mit eigener Messreihe — D2, nicht gebaut.

**Gebaut:** Der Router schreibt das Urteil der Objekt-Nähe als `objekt_urteil`. Der Planner hat die Priorität *Zeitangabe → Timeline* verloren und bildet eine **Reihenfolge**: die Dienste, deren Merkmal ein akutes Objekt erreicht, nach Nähe, dann der Treffer der übrigen Prioritäten — ein Treffer ohne eigenes Merkmal steht vorn. Er fragt den ersten noch nicht gelaufenen; **nach `abgelehnt` oder `rejected` den nächsten**, nach jedem anderen Ausgang ist Schluss — die Entscheidung vom 16.09.2026 im Wortlaut: *„wenn der eine nicht will, soll der andere auch gefragt werden"*. Nach einem Ausfall der Sachlage ordnet die Nähe nicht (die Objekte sind die des Vorturns), nach einem Impuls schon. Bauart: `novaberg-node-planner.md`, letzter Abschnitt.

**Was mitziehen musste, gefunden von der zweiten Kontrolle:** Zwei Leser nahmen an, dass je Turn nur ein Dienst läuft. Der Aufgabenblock stellte eine Ablehnung vor den Erfolg oder Fehler des zweiten Dienstes, und die **Verdichtung ins Kurzzeitgedächtnis hätte eine weitergegebene Ablehnung als »hat nicht stattgefunden« abgelegt**, während der zweite Dienst die Notiz wirklich schrieb — das Spiegelbild von `FALSCHE-BESTAETIGUNG-WIRD-ERINNERUNG`. Beide folgen jetzt der Weitergabe.

**Gemessen** (`labor/2026-09-17_planner_d1/`): die 60 Äußerungen mit den Objekten der Fassung C, echter Router-Aufruf, `planner.plan` des gebauten Codes, keine Agenten. Der Router erteilte 17 Aufträge; **Notizen-Fälle zuerst richtig 7 → 13 von 13**, Termine 4 → 4 von 4. **Den Gewinn trägt vor allem das Entfernen der Zeitwort-Priorität:** Der Router-Treffer lag bei allen 13 Notizen schon richtig; die Nähe entschied in 3 Fällen mit und stimmte überein. Im Betrieb der 14 Tage davor hätte die neue Reihenfolge 0 von 11 Aufrufen anders zugestellt. Suite 3869 grün, neun Gegenproben wie vorhergesagt. `PLANNER-ZEITWORT-UEBERSTIMMT-DIENSTWAHL` ist behoben.

**Nicht gemessen:** die Kette *Ablehnung → nächster Dienst* mit echten Agenten — ein solcher Lauf schreibt Termine und Notizen.

#### Die Objektwahl der Lage — gebaut (16.09.2026)

**Der Anlass** steht einen Absatz höher und in der Fundliste: Aus *„Ich brauche für den Ausflug noch eine Regenjacke"* wurde das Objekt *Ausflug*, und kein Merkmal kann trennen, was der Objekttext schon vermischt.

**Gebaut sind zwei Dinge.** Der Sachlage-Prompt trägt eine Regel: **Das Objekt ist die Sache, um die es in der Äußerung geht, nicht der Rahmen** — bei etwas, das getan wird oder stattfindet, die Handlung samt dem, woran sie getan wird; bei etwas, das gebraucht wird, fehlt, gilt oder festgehalten werden soll, die Sache; Anlass, Person oder Ort stehen daneben oder als Eigenschaft; ist die Äußerung ein Vorhaben oder Sachverhalt, ist das Objekt akut. Die Beispiele im Prompt stammen aus keiner Messreihe. Und die Formprüfung schreibt jeden Objektnamen **ohne Auszeichnung** (`sachlage_form.py::plain_object_name`) — im frischen Parse und beim Laden der Blase.

**Gemessen, zweimal, je beide Fassungen abwechselnd über dieselben 60 Äußerungen** (Pixie pausiert, Startgrenze, Wächter; `labor/2026-09-16_objektwahl/`):

| | A (vorher) | **B** — „das, was gebraucht, getan … wird" | A, zweiter Lauf | **C** — gebaut |
|---|---|---|---|---|
| Rahmen statt Sache (von Hand, erstes akutes Objekt) | 4 | 0 | 4 | 2 (+2 Ausfälle an genau diesen Fällen, s. u.) |
| Handlung wird zum Ding (von Hand) | 0 | **3** | 1 | 0 |
| ohne akutes Objekt | 4 | **8** | 5 | 4 |
| Notizen am Notizen-Zettel (Betriebswahl, je Zettel 0,40) | 13 | **8** | 11 | **13** |
| Termine an der Timeline (Betriebswahl) | 25 | 25 | 25 | **26** |
| nur der falsche Dienst (Betriebswahl) | 3 | 1 | 1 | **0** |

**B ist verworfen:** Die Regel zog das Modell auf das Substantiv hinter dem Verb — *das Buch* statt *Buch abgeben* — und widersprach damit dem entschiedenen Kriterium, nach dem bei einer Erledigung die Handlung die Sache ist; sie machte zudem Notizen latent. **C ist übernommen**, nach einer Regel, die vor dem Lauf feststand und nur auf der ungeraden Hälfte entschied (Rahmen, Handlung zum Ding, ohne akutes Objekt, Notizen, Termine, nur falsch — keine Kennzahl schlechter); die gerade Hälfte zeigt dieselbe Richtung (Notizen 5 → 7, Termine 12 → 13, nur falsch 1 → 0, *Lena* wird zur *Nussunverträglichkeit*). **Die Unterschiede sind klein:** Derselbe Prompt A streut von Lauf zu Lauf um ein bis zwei Fälle je Kennzahl. Was C sicher leistet, ist der Rahmen und die Handlung; die Notizen-Seite bleibt schwach.

**Die zwei Ausfälle von C waren kein Fehler des Prompts.** Das Modell lieferte gültiges JSON mit dem richtigen Objekt (*Regenjacke*, *Ausflug* latent daneben) — **die JSON-Reparatur des Workers schnitt es ab**: `deduplicate_repetition` hielt eine ausgerichtete Einrückung von 25 Leerzeichen für eine Endlosschleife. Behoben am selben Abend (`services/postprocess.py::parse_json_strict`: gültiges JSON wird nicht repariert; eine Reparatur steht im Log). Sonde danach: dieselben zwei Fälle je fünfmal, 10 von 10 fehlerfrei, vorher 2 Ausfälle in 5 Läufen.

**Im Betrieb** (Wissenschaftsturn, 22:28 UTC): Die geladene Blase trug `**Gravitationslinseneffekt**` und wurde beim Laden bereinigt; die neue Lage schrieb zwei schlichte Objekte fort; die Bestände blieben gleich.

**Was die Bereinigung nicht nachholt:** 11 Objekte im Eigenschaftsgedächtnis (alle beim Betriebspaar) tragen Sternchen im gespeicherten Schlüssel, 7 davon mit **14 aktiven Eigenschaften**, die die Suche mit dem schlichten Namen nicht mehr findet; eine neue Nennung legt ein zweites Objekt an. ~~Nicht migriert — eine Änderung an Bestandsdaten ist eine Entscheidung (Fundliste 16.09.2026).~~ → **Bereinigt am 17.09.2026** nach Freigabe: 10 umbenannt, eines zusammengeführt; der Abruf findet 14 von 14.

**Was offen bleibt:** die **Betriebszahl** — wie viele echte Turns zugeordnet werden, und ob das Urteil mit der späteren Zustellung übereinstimmt. Erst wenn sie trägt, liest Teil D das Urteil.

#### Teil C1 — gebaut (16.09.2026)

**Gebaut ist die eine Seite der Rechnung: die Anmeldung trägt das Objekt-Merkmal, und der Server hält seinen Vektor.** `BaseAgent.objekt_merkmal` ist eine neue Angabe der Anmeldung, Vorgabe leer (*bedient kein Objekt der Lage* — zulässig). `TimelineAgent` und `NotizenAgent` deklarieren den geeichten Wortlaut zeichengleich mit der gemessenen Fassung. `agents/object_feature.py::feature_vectors_ensure` bettet beim Start jedes deklarierte Merkmal eines Dienstes am Empfang über den Embed-Worker ein, mit eigener Frist von 60 s; `feature_vectors()` ist der Leser für C2. Der Embed-Text ist das Merkmal selbst (`build_embed_text`), nichts wird persistiert — ein geänderter Wortlaut gilt ab dem nächsten Start.

**Was nicht gebaut ist, ausdrücklich:** Kein Objekttext, keine Nähe, keine Schwelle, und der Empfang liest weder Merkmal noch Vektor. **Das Brett des Empfangs ist zeichengleich** — gemessen vor und nach dem Bau: 11 395 Zeichen, dieselbe SHA-256. Es kann dabei nichts anders zugestellt werden.

**Zeugen:** `tests/test_object_feature.py`, 16 Stück — der Wortlaut beider Dienste Zeichen für Zeichen (eine Umformulierung soll hier rot werden, nicht im Schattenlauf), genau zwei Dienste mit Merkmal, das Brett ohne Merkmal, ein unbrauchbarer Vektor (leer, Null, NaN, unendlich, fremde Dimension) fehlt laut, ein gescheiterter Neulauf lässt keinen alten Vektor stehen, eine werfende oder nicht textförmige Deklaration nimmt nur diesem Dienst den Vektor. Suite **3810 grün, 0 übersprungen**. Gegenproben mit Vorhersage: ein Umlaut im Timeline-Merkmal und die Nullvektor-Prüfung entfernt, **2 vorhergesagt, 2 rot**; die Absicherung der Deklaration entfernt, **1 vorhergesagt, 1 rot**.

**Gemessen im Betrieb** `[16.09.2026, 19:49 UTC]`: zwei Starts am letzten Codestand (19:49:21 und 19:49:23 UTC), je *„Objekt-Merkmal: 2 von 7 Diensten am Empfang mit Vektor ['notizen', 'timeline'] (Dimension 768)"*, 0,3 s nach dem Handshake, kein Fehler. **Nicht mitgezählt sind die Starts der Zwischenstände beim Reload** — einer davon lief mit *0 von 7*, weil eine Konstante nach ihrem Leser eingefügt war, und fuhr trotzdem hoch: der Ausfall eines Merkmals verhindert den Start nicht, und er steht laut im Log.

**Was die zweite Kontrolle fand** (Zugriff: wer erzeugt und wer liest die Angabe, über den ganzen Baum, dazu eine Sonde über alle 20 Dienste): **36 Kandidaten, 0 fremde Leser**, kein Dienst, dessen `zustellart` wirft — und **einen Befund im eigenen Bau**: Die Deklaration wurde außerhalb der Absicherung gelesen; ein werfender Getter hätte den Start abgebrochen, anders als bei der Anmeldung. Behoben und bezeugt. Dazu eine **berichtigte Messzeile**: Die erste Fassung nannte *drei Starts*, das Log trug im genannten Fenster sechs, einer davon ohne Vektor.

**Was offen bleibt:** ~~Der Vektor hat noch keinen Leser — das ist C2, der Schattenlauf.~~ → **gelesen seit C2, am selben Tag** (unten). Die Untergrenze je Dienst ist nicht gemessen.

#### Teil B — gebaut (16.09.2026)

**Der Anlass im Wortlaut des Eigentümers:** *„Das ergibt keinen Sinn! Im Gegenteil, das ist gefährlich! Kein Arzt schaut sich bei der Untersuchung nur die Hände an."*

**Gebaut ist die Ablösung der Grenze je Beitrag durch eine Grenze über das Fenster.** `memory/session.py::format_session_turns_numbered` setzt `max_chars` von 100 auf **keine Vorgabe**; `budget_wahren` wirft die **ältesten Gruppen ganz** weg, wenn `SESSION_HISTORY_BUDGET_CHARS` (8000) reißt, und protokolliert, wie viele. Die jüngste Gruppe bleibt immer. Bauart und Zahlen: `novaberg-mem-session.md` §3.7.

**Warum die Grenze die Gruppe trifft und nicht den Beitrag:** Ein halber Beitrag sieht aus wie ein ganzer — der Leser bekam den Anfang einer Regieanweisung und hielt ihn für die Antwort. Eine fehlende Gruppe ist sichtbar.

**Die Eichung ging dem Bau voraus.** `[gemessen 16.09.2026]` Über 1494 Rohturns liegen **94,2 %** der Antworten Novas über 100 Zeichen (Median 440), 36,8 % der Nutzer-Äußerungen. Über 1439 Fenster: gekappt 904 Zeichen im Mittel gegen 3970 ungekappt. Das Budget ist aus dieser Verteilung gewählt, nicht gesetzt — bei 8000 gehen 86,8 % der Fenster vollständig durch, bei 12.000 wären es 90,0 % zum Preis eines halb so großen Prompts mehr.

**Die Messung nach dem Bau, derselbe Code über denselben Bestand:** mitten abgeschnittene Beiträge **7213 → 0**; 1249 Fenster (86,8 %) vollständig, 190 (13,2 %) mit weggefallener Gruppe. Zeugen: `tests/test_verlauf_budget.py`, 13 Stück; Suite **3794 grün, 0 übersprungen**. Gegenproben mit Vorhersage: Kappung zurück 5 vorhergesagt / 4 gezählt (das Budget rechnet vor der Kürzung — im Docstring festgehalten), Budget aus 5 / 5.

**Was die zweite Kontrolle beitrug, und zwar gegen die eigene Zahl.** Der Messaufbau schrieb jeder Turn-Hälfte die Herkunft `nutzer_turn`; der Bestand kennt **170 `eigener_impuls`** (11,4 %), und ein Impuls öffnet eine eigene Gruppe. Das traf 379 von 1439 Fenstern und machte die Quote um fünf Punkte zu pessimistisch — berichtet waren zunächst 81,7 %. Am Code geklärt (`dispatcher.py`: je Durchlauf **ein** Turn, auf einem Impuls nur die Antwort) und neu gemessen. Dieselbe Kontrolle belegte am Betriebs-Log, dass der größere Verlauf kein Kontextfenster sprengt, und fand einen **achten** Verlaufs-Renderer, den die Doku nicht führte.

**Was offen bleibt:** Der Betrieb ist nicht gemessen — kein echter Turn ist seit dem Bau durch die neun Leser gelaufen. Und `sachlage.py::_render_history` schneidet weiter bei 1600 Zeichen, entgegen seinem eigenen Docstring (Fundliste 16.09.2026).

#### Teil A — gebaut (15.09.2026)

**Gebaut ist der Riegel in der Auswertung des Tribunals**, gerechnet in Python wie die Datumsprüfung (`server/utils/storage_claims.py`, gerufen in `graph/nodes/tribunal.py`). Er erkennt vier Satzformen — die kurze Bestätigung, die erste Person im Perfekt, den Zustandssatz in drei Stellungen und den Vollzug in Gegenwart oder Zukunft —; ein mehrdeutiges Partizip gilt nur mit einem Speicherort oder einer Zeitangabe im Satzteil. Ohne abgeschlossenen Dienst im Turn hebt er das Urteil auf `warnung`, und der Corrector schreibt neu. Bauart, Grenzen und dauerhafter Eintrag: `novaberg-node-tribunal.md`, *Die Speicherbehauptung*.

**Die Eichung am Bestand ging dem Bau voraus.** Die Handurteile über alle Antworten mit Speichervokabular unter 1493 Rohturns standen fest, bevor eine Zeile Detektor geschrieben war: 53 Behauptungen, 8 Grenzfälle. Erkannt werden 53 von 53; im Ausschnitt ab 14.08.2026, in dem die Ausgänge der Dienste protokolliert sind, schlägt er in 27 von 928 Turns an — 24 Behauptungen, 3 Grenzfälle, kein Fehlalarm. **Das ist eine Anpassungsgüte, keine Vorhersage**; die Vorhersage liefert der Betrieb.

**Die Absicht ist erst erreicht, wenn die Korrektur die Behauptung beseitigt — und das hat der erste Lauf widerlegt.** Gemessen in der echten Korrekturrunde unter `gemma4-a4b-gpu` über die 27 Anschläge, je mit schriftlicher Vorhersage (`labor/2026-09-15_speicherbehauptung/`):

| Lauf | Auftrag | Riegel nach Runde 1 / 2 | von Hand: 24 Behauptungen sauber | vom Riegel nicht gesehen |
|---|---|---|---|---|
| 1 | *„Bestätige, was du verstanden hast … melde es nicht als notiert, eingetragen oder gespeichert"* | 21 / 24 von 27 | **12** | **6** |
| 2 | Wiedergabe dessen, was der Nutzer gesagt oder gewünscht hat: *„Du möchtest …"*, *„Du hast … genannt"* | 22 / 26 von 27 | **19** | **2** |
| 3 | derselbe, der Riegel um die zwei unsichtbaren Formen aus Lauf 2 nachgezogen | 21 / 25 von 27 | **20** | **0** |

**Der Riegel allein misst nach der Korrektur eine Sauberkeit, die es nicht gibt.** In Lauf 1 meldete er 24 von 27, gelesen waren 12 von 24 sauber: Der Auftrag nannte die Wörter, die nicht vorkommen sollten, und das Modell wich auf andere aus — *gelistet, angesetzt, erfasst, geöffnet*. Die sauberen Fassungen hatten fast alle eine Form, die Wiedergabe des Wunsches; die gibt der Auftrag seit Lauf 2 vor. **Ein Verbot im Auftrag an ein Modell erzeugt Synonyme** — dieselbe Erfahrung wie beim Prompt der Figur, hier an einem Korrekturauftrag gemessen.

**Was offen bleibt:** Eine Behauptung, die beide Korrekturrunden übersteht, geht mit der Antwort hinaus — das meldet die Auswertung als Fehler, verhindert es aber nicht (Lauf 3: 2 von 24 Behauptungen). → **Am 16.09.2026 entschieden, nicht gebaut:** Ein Korrektursatz wird angehängt, der Satz bleibt stehen, und der Ausgang wird am Turn vermerkt (oben, *Offen für den Bau*). Und die Wiederholung einer früheren, echten Schreibung ist im Turn nicht als solche erkennbar, weil der Verlauf die Marke `[ERLEDIGT]` seit dem 23.04.2026 nicht mehr trägt (Fundliste 15.09.2026). Ebenso wenig deckt Bestand, den der Turn ohne Dienst gelesen hat (Enricher, Werkzeuge des Thinkers): Ein wahrer Satz über ihn schlägt an wie eine Behauptung — ~~**das ist Teil F in anderer Richtung**, denn wer den Bestand kennt, ist die Fachabteilung, nicht der Riegel~~ → **entschieden am 15.09.2026: Der Riegel fragt den Bestand selbst**, bevor korrigiert wird (oben, *Offen für den Bau*).


## 5. Was ausdrücklich nicht gebaut wird

Aus `cognitive-pipeline_k` bleiben Konzept: ~~der Frame-**Auflöser** gegen den Bestand~~ (**seit dem 28.08.2026, spät, in der Konversationsfassung gebaut — Scheibe 6**; die gezielte Suche je offener Eigenschaft, Fakten und Notizen als Quellen ~~und die Kritikalität einer Lücke~~ bleiben Konzept; **die Kritikalität einer Lücke ist seit dem 30.08.2026 gebaut — Scheibe 10**), ~~das Frame-**Lager** samt Lernmechanik~~ → **seit dem 13.09.2026 steht ein Teil des Frame-Lagers in Konversationsfassung — Scheibe 11:** Objekte mit ihren Eigenschaftswerten und deren Historie je Paar, verankert an `entitaeten` statt an Klassennamen; **die Lernmechanik des Lagers (`frames_k` §7.3: Häufigkeit, Wert-Cluster, Recency- und Korrektur-Gewichtung, Schema-Aggregat je Klasse, Decay) bleibt Konzept**, **Cross-Frame-Validierung** (braucht Slots aus Fakten oder Lager), ~~**Plausibilitätsprüfung**~~ (**seit dem 29.08.2026 gebaut — Scheibe 7**; die Stufeneichung und die Konflikt-Schwellwerte bleiben offen), **Skills**, die Ablösung des Planners und die Werkzeug-Orchestrierung. ~~Ebenso bleibt liegen: die Formfrage der Rückfragen (2,2 je Turn) — sie wird nach Scheibe 3 neu gemessen und erst dann behandelt, falls sie dann noch besteht.~~ → **Gemessen am 29.08.2026 über 20 Turns** (§4, Betriebsmessung der Kette): **1,21 Fragezeichen je Antwort, 12 von 19 Antworten enden fragend** — gegen den Ausgangswert **2,2 Fragen je Turn bei 100 % fragenden Schlüssen**. Die Formfrage besteht in ihrer alten Schärfe **nicht mehr**; ob zwei Drittel fragende Schlüsse zu viel sind, ist eine Haltungsfrage und kein Befund. **Die Bedingung stand bis zum 30.08.2026 unverändert hier, während vier Absätze weiter oben ihre Messung stand** — im Labor blieb die Menge bei fragender Haltung 10/10, nur der Gegenstand änderte sich (§4). ~~Scheibe 4 bleibt Entwurf, bis die DDL angekündigt und freigegeben ist.~~ → **Nachtrag 13.09.2026:** Scheibe 4 ist seit dem 28.08.2026 gebaut (§4, DDL angekündigt, freigegeben, angelegt); der Satz stand seither unverändert hier. Cross-Frame-Validierung braucht Slots über mehrere Frames — mit Scheibe 11 gibt es gespeicherte Werte je Objekt, aber keinen Vergleich über Objekte hinweg; sie bleibt Konzept.

---

## 6. Verweise

- `novaberg-thinking-frames_k.md` — Substrat: Frame-Klassen, Akutheit, Slots
- `novaberg-thinking-cognitive-pipeline_k.md` — Zielbild der vollen Verstehens-Schicht
- `novaberg-thinking-drive_k.md` — Zielhorizonte, Gravitation
- `novaberg-thinking-curiosity_k.md` — Wissenslücken als spätere Abnahmestelle offener Slots
- `novaberg-haltungsraum_k.md` — Haltungsgrößen; die Rückfrage-Zeile, die Scheibe 3 erweitert

### Quellen der Prüfung (28.08.2026)

- van Dijk, T. A., & Kintsch, W. (1983). *Strategies of Discourse Comprehension.* Academic Press. Dazu van Dijk (1999): [Context Models in Discourse Processing](https://discourses.org/wp-content/uploads/2022/07/Teun-A.-van-Dijk-1999-Context-models-in-discourse-processing-in-Oostendorp-Goldman.pdf) — die Trennung Situationsmodell/Kontextmodell entspricht der Trennung Lage/Haltung.
- Zwaan, R. A., Langston, M. C., & Graesser, A. C. (1995). [The Construction of Situation Models in Narrative Comprehension: An Event-Indexing Model.](https://journals.sagepub.com/doi/10.1111/j.1467-9280.1995.tb00513.x) *Psychological Science*, 6(5). Dazu [Zwaan & Radvansky (1998)](https://sites.ualberta.ca/~dmiall/Cognitive/Readings/Zwaan_Radvansky_1998.pdf) und der Überblick [Zwaan (2025)](https://doi.org/10.1177/09637214251326812).
- Allen, J. F., & Perrault, C. R. (1980). [Analyzing Intention in Utterances.](https://nlp.stanford.edu/acvogel/allenperrault.pdf) *Artificial Intelligence*, 15(3), 143–178.
- Frith, C. D., & Frith, U. (2006). [The Neural Basis of Mentalizing.](https://www.cell.com/fulltext/S0896-6273%2806%2900344-8) *Neuron*, 50(4). Dazu [ToM und indirekte Kommunikation, Phil. Trans. R. Soc. B (2025)](https://royalsocietypublishing.org/rstb/article/380/1932/20230497/235137/Theory-of-Mind-and-the-brain-substrates-of-direct).
- Metusalem, R., Kutas, M., Urbach, T. P., Hare, M., McRae, K., & Elman, J. L. (2012). [Generalized event knowledge activation during online sentence comprehension.](https://pubmed.ncbi.nlm.nih.gov/22711976/) *Journal of Memory and Language*, 66. Dazu [McRae & Matsuki (2009)](https://compass.onlinelibrary.wiley.com/doi/10.1111/j.1749-818X.2009.00174.x).
- Klinger, E., & Cox, W. M. (2011). [Motivation and the Goal Theory of Current Concerns.](https://onlinelibrary.wiley.com/doi/10.1002/9780470979952.ch1) Dazu [Klinger (2013): Goal Commitments and the Content of Thoughts and Dreams.](https://pmc.ncbi.nlm.nih.gov/articles/PMC3708449/)
- Clark, H. H. (1996). *Using Language.* Cambridge University Press — [Kap. 4: Common Ground](https://www.cambridge.org/core/books/abs/using-language/common-ground/7F9689E12F87250DCA81540FCBDE3720).
- Rothe, A., Lake, B. M., & Gureckis, T. M. (2018). [Asking goal-oriented questions and learning from answers.](https://nyuscholars.nyu.edu/en/publications/asking-goal-oriented-questions-and-learning-from-answers) Dazu [Coenen, Nelson & Gureckis (2019): Nine Open Challenges.](https://link.springer.com/article/10.3758/s13423-018-1470-5)
- DuBrow, S., & Davachi, L. [Events and Boundaries.](https://memory.psych.upenn.edu/mediawiki/images/e/e1/DuBrow_Final.pdf) Dazu [Hippocampal–mPFC Event Segmentation (2020/2022)](https://www.biorxiv.org/content/10.1101/2020.03.14.990002v4.full).
- Grosz, B. J., & Sidner, C. L. (1986). [Attention, Intentions, and the Structure of Discourse.](https://aclanthology.org/J86-3001/) *Computational Linguistics*, 12(3), 175–204 — die Stapel-Mechanik der geteilten Blase (§3a).
- Kuppens, P., Allen, N. B., & Sheeber, L. B. (2010). [Emotional Inertia and Psychological Maladjustment.](https://journals.sagepub.com/doi/10.1177/0956797610372634) *Psychological Science*, 21(7) — die Emotion springt nicht (§3a).

---

*Stand 28.08.2026. Erstfassung, am selben Tag ergänzt: wissenschaftliche Prüfung (§2a) und das Blasen-Modell des Kontexts (§3a). Die Annahmen sind im Projekt formuliert und decken sich mit der etablierten Forschung — geprüft, nicht übernommen.*
